# 后端进度

> 最后更新：2026-09-18 · 配套：`进度-项目.md`（总览）、`进度-前端.md`。


## 模拟会话离线回放与失联对账（2026-09-18）

`emulagent_api` 的 `rpc/session.history` 在宿主不在线时改为离线回放
`events.sse`（复用 `vulnagent_api._history_from_sse`，模拟会话事件格式与
挖掘完全一致，另透传原始 `ts` 给前端显示时刻）。此前看任何历史会话都会
`ensure` 冷启一个 dsh Node 宿主——实测点一次旧会话就常驻一个
`fwgraph-emul-web` 进程（30 分钟空闲才回收），冷启动期间前端只有
「加载对话…」空面板。mux 对失联会话（running 但 runner.pid 指向的进程
已死且宿主不在线）直接回空闲流，不再借 ensure 复活。会话摘要新增
`host_alive`（is_live 或 pid 存活），前端据此标「失联」并提供停止；
`/emulagent/sessions/{sid}/stop` 对失联会话照常置 done。测试
`test_emulagent_api.py` 8 项（离线回放/ts 透传/失联标记/mux 防冷启/停止）。
顺带修 `test_webui` 契约断言与新主题/教程/仪表盘现状不同步的 4 处漂移。

## 漏洞库 API（2026-09-01）

`pipeline.vulnlib` 把条目写到 `data/vulnlib/advisories/<id>.json` + `index.jsonl`。字段含中文 `description`、`vuln_point`（入口→函数→危险调用）。`vulnlib_api`：`GET/POST /vulnlib`、`POST /vulnlib/import`、`GET /vulnlib/nday`、`PATCH/DELETE /vulnlib/{id}`。删除要管理员。N-day 按厂商规则 + 产品词与 `job.firmware` 重叠打分。dsh：`fw_vulnlib_search`、`fw_vulnlib_nday`。测试 `test_vulnlib_api` 10 项。


## rootfs_elf 补函数汇编（2026-08-28）

`ida_worker` 在 `decompile/*.c` 之外写 `functions/<addr>.asm`（与 `ida_export.py` 同行格式）。适配层改写 header、从 index 补 size；缺 IDA 产物时用 objdump 按 ELF+size 回填。`scripts/backfill_asm.py` 给已有任务补 `.asm`。测试 `test_rootfs_elf_decompile` + `TestFunctionSource` 通过。

## 工作台打开会话不再等宿主（2026-08-25）

`session.history` 在 dsh host 未在跑时直接读 `events.sse`（丢掉 stream 中间帧）。
已结束会话的 mux 立刻 idle，不 spawn node。前端先回填再决定是否连 mux。

## AFL++ 与 call_trace 可调用（2026-08-25）

`fw_request_fuzz` 宿主回退时不再依赖 PATH 上的 `afl-fuzz`（systemd 用户服务找不到 `~/AFLplusplus`）。
runner 从 `AFL_FUZZ` / PATH / `$AFL_REPO/afl-fuzz` 解析绝对路径。沙箱仍因宿主 `afl-qemu-trace` 要 GLIBC_2.43 而回退宿主。
`fw_call_trace` 把插件的 `in`/`out` 映射成 CBM 的 `inbound`/`outbound`；stderr 里的 allocator warn 不再把已有 JSON 打成 502。

## IDA 投放目录（2026-08-25）

用户把 IDA 拷到 `deploy/docker/ida-drop/`（容器 `/opt/ida-drop`）或 `data/ida/`，
`config.ida_dir()` 按 idat / libidalib.so 自动识别，无需改 `IDA_DIR`、无需重建镜像。

## Docker 内置 rootfs_elf（2026-08-25）

编排器镜像拷入 `tools/ida-no-mcp/rootfs_elf`（即 rootfs_elf.tar.gz 那套、无许可文件）。
`run_job` 不再因缺少 `IDA_DIR` 直接失败；带许可的 idat 只留给 raw/PX4。

## Docker 交付带上 EMBA（2026-08-25）

`EMBA_BACKEND=docker` 时解包改为 `docker run embeddedanalyzer/emba:2.0.3a`（特权兄弟容器），路径走 `SANDBOX_HOST_PREFIX`。
compose `--profile tools` 拉取该镜像；编排器镜像仍不含 36GB 分析层。

## 上传自动解密（2026-08-25）

任务状态增加 `decrypting`。解包前先跑 `pipeline.fwdecrypt`：识别格式、剥离已知容器/压缩包装，写 `data/decrypt/<job>/decrypt.json`。
`GET /decrypt`、`GET /jobs/{id}/decrypt` 供进度页轮询。解不开的加密容器标 `identified`，仍把原文件交给 EMBA。

## 协议逆向分析接口（2026-08-25）

`POST /protocol/decode` 拆用户粘贴的十六进制/Base64。
`GET /jobs/{id}/protocol-reverse` 汇总协议标识、算法命中、深度分析和攻击面评分。
`GET /jobs/{id}/protocol-reverse/functions/{md5}/{addr}` 对单个函数做实现路径归纳。

## 仪表盘量化序列（2026-08-24）

`GET /dashboard` 增加危害等级/类型/可达性分布、trace 命中数，以及近 14 日 findings/traces/sessions/jobs 序列。原有计数字段保持兼容。

## 空结果也写会话报告（2026-08-24）

`_write_report` 在 0 条 finding 时仍写中文报告（统计 + 最近 trace 摘要）。
`GET /vulnagent/sessions/{sid}/report` 现取现写，不再因「没有入库」404。
人设要求对用户只说中文、一步最多两个工具；429 的 hunt_next 写明配额不是固定 24。

## httpd qemu-user 启动：预置 SysV shm（2026-08-24）

httpd `shmget(0x2f)` 用 `IPC_CREAT|000`，残留 000 段后 `shmat` EACCES → SIGSEGV。
覆盖/exec 前 `seed_sysv_shm` 建成 0666（key 0x2f / 43511），docker `--ipc=host`。
实测预置后 `shmat` 成功，进程不再立刻崩，并会尝试 bind :80。

## 动态配额/前缀 md5/图谱 502/浏览 rootfs（2026-08-24）

当日 trace 默认 24 条把挖掘卡死；默认改为 96，qemu-exec 日配额 48、会话预算 24。
`binary_md5` 接受唯一前缀。`fw_call_trace` 遇 CBM 502 回 unavailable + hunt_next，不再摔工具。
`fw_browse_firmware` 会落到 squashfs-root，agent 用 `usr/sbin` 这类客户路径能读到。
429 回 `status=quota` 而不是连抛。watch_hunt 默认 HTTP :8000。

## fw_get_trace lossless JSON；via=net 不被 input_path 改成 file（2026-08-24）

`compactTrace` 曾带 `error: undefined` / 空字段，harness 报 `value is not lossless JSON`，agent 轮询断掉。现在 `asJson` 只回可序列化对象。
`via=net` 即使带了 `input_path` 仍走网络触发。空差分 hunt_next 不再催重复同一条。
qemu_exec 日志里 SIGSEGV、外层 SIGKILL 的标 `crash_kind=startup`。

## 系统配置增加 UI_ONBOARD_TOUR（2026-08-24）

`GET/PUT /system/config` 白名单加入 `UI_ONBOARD_TOUR`（缺省 `"1"`）。关为 `"0"` 后前端不再自动弹出新手教程。

## GitHub finding 按 ELF md5 并进任务报告（2026-08-24）

仓库里 Archer 挖掘 finding 带的是另一台分析机的 job id。报告生成按
`binary_md5 ∈ 本任务 manifest` 关联，再生 Archer 任务报告后报告中心能看到这 18 条。

## fw_request_trace 不再 90s 超时 / 误 cancel（2026-08-24）

平台 TRACE_LOCK 同时只跑一条。插件遇 409 立即返回 `status=busy`，不再连重试到 90s。
`watch_hunt` 把 `fw_request_trace` 当在飞工具，不再 50s 空闲就 session.cancel
（那会把并行调用打成 aborted before dispatch）。动态工具 timeout 提到 180s。

## 损坏的 dsh 会话不再卡死工作台（2026-08-24）

空工具名会话的 `tool/result.callId=""` 过不了新版 harness 校验，
`session.history` 报 `SessionPersistenceCorruptionError`。
resume/history 遇到损坏日志时隔离旧目录并开新 dsh 会话；history 返回空列表而不是 502。

## 修复 unknown tool ""（2026-08-24）

网关后续参数分片带 `id=""` / `name=null`，把第一片里的 `fw_*` 名覆盖成空串，
BlockAssembler 以 block-end 为准，于是执行 `unknown tool ""`。
`plugin-events` 在 `llm/stream` 上按 index 记住非空 id/name 再交给 agent-loop；
标题 LLM 与主对话不再共用同一份 thinking buffer。
分析机 DeepSeek Harness 升到 `0.1.1-rc.2`，translate 同样忽略空覆盖。

## 启动崩溃不得放弃；暂停可续跑（2026-08-23）

- `qemu_exec` 与 trace 共用 `seed_guest_tmp`（`/tmp/dec-model.conf`），避免 httpd 启动即 SIGSEGV。
- 没喂 payload 的 crash 标 `crash_kind=startup`：hunt_next 要求 `fw_request_trace`，禁止跳过该 ELF。
- `POST /vulnagent/sessions/{sid}/resume`：用户消息拉活误标 done 的会话；看门狗 `session.prompt` 对 done 仍 409。

## 动态结果变成下一刀，不给用户看验收（2026-08-23）

- `fw_get_trace` 压成 compact + `hunt_next`（有差分 → 读函数并 record_finding；空差分 → 改 via=stdin）。
- `fw_list_traces` 只回 with_diff/recent，不再把几百条摘要塞进模型。
- 后续静态工具会反复带上未消化的 `hunt_next`，直到换入口或入库。
- persona/skill 禁止把 hunt_next、trace 表、能力验收写进对用户可见回复。

## docker stdin 重定向修到 rootfs/tmp（2026-08-23）

- 专用会话 `s-mt5k23tt-aaed` 实测：`via=stdin` 三次失败（`cannot open /tmp/fwgraph-stdin`），`input_path` 成功（`a432876827e2` 非空差分 45，`c30ceb41dcef` 空差分 0）。
- 原因：docker 外层 `sh` 在 chroot 外，容器 `/tmp` 是 tmpfs；stdin 文件写在 `out_dir`（bind 到 `rootfs/tmp`）。现与 `qemu_exec` 对齐，重定向 `{rootfs}/tmp/fwgraph-stdin`。
- 修复后同一 agent 重测：`f55640905b63` status=`ok_empty_diff`，`request.via=stdin`，`trigger_result.kind=stdin` sent=5，`diff.function_count=0`。
- 模块测试：`test_docker_stdin_redirects_from_rootfs_tmp`（TestTraceDocker 6 通过）。

## stdin/文件差分 trace（2026-08-23）

- `fw_request_trace` 支持 `via=stdin`（payload 走 qemu stdin）和 `input_path=/tmp/<file>`。网络空差分时走解析器路径，不做全系统仿真。
- persona/skill/看门狗续跑提示已接上。模块测试：TestTriggerTrace 18 通过；插件 via=stdin 单测通过。

## 到 max_turns 询问是否继续（2026-08-23）

- 到顶把会话设为 `awaiting_continue`（不直接 done），工作台弹出「继续挖掘 / 结束本轮」。
- `POST /vulnagent/sessions/{sid}/continue` 放宽上限并投续跑 prompt；结束仍走 stop。
- watchdog 到顶打 `WAIT`，不再自动 stop。
- 测试 522 通过 + 1 跳过。

## 挖掘有 max_turns 硬顶；stop 后不能被续跑拉活（2026-08-23）

- `session.prompt` 计 `turns`，到 `max_turns` 返回 409；watchdog 到顶就 `stop`，不再无限【编排】续跑。
- `stop` 带 `discard`，`status=done` 的会话 `ensure` 拒绝重生。
- `payload_hex` 接受 `0x` 前缀和奇数位（左补 0）。`record_finding` 把 id 写进 session state。
- 测试 522 通过 + 1 跳过。

## watchdog 不再把收工空闲刷成 STUCK；会话 trace 192（2026-08-23）

- 轮询 `fw_get_fuzz_run`/`fw_get_trace` 或文案含「轮询」且空闲 <180s 不当卡住。
- stdout `STUCK` 只在空闲 ≥180s 时打，避免每分钟刷屏。
- 插件 `MAX_TRACES_PER_SESSION=192`，对齐日配额 384。

## 会话 trace 预算 96（2026-08-23）

- 插件 `MAX_TRACES_PER_SESSION` 24/48 会在日配额还有余量时把 hunt 卡死。提到 96，对齐 `TRACE_DAILY_PER_JOB=192`。重启 dsh host 后计数清零。

## 空差分不再重跑攻击路径（2026-08-23）

- `_trace_worker` 只在 `ok` / `ok_trigger_error`（有差分）后刷新 attack_paths。`ok_empty_diff*` 不再加载 28MB `symbols.json`，避免编排器 RSS 随 trace 次数涨到数 GB。

## payloads_hex 多包会话；插件 fetch 重试（2026-08-23）

- `fw_request_trace` 增加 `payloads_hex`：同一 TCP/UDP 连接按序发送多步协议（SSH banner 再 KEX、SMB negotiate 再 Session Setup），trigger_result.kind 为 `tcp_conversation`/`udp_conversation`。
- 插件 `fw()` 对 `fetch failed`/超时/5xx 最多重试 3 次，避免编排器忙时 agent 把传输层故障写成「平台不可用」并收工。
- `GET /jobs/{id}/traces?limit=` 只返回最近 N 条（total 仍是全量）。watchdog 用 `limit=8`。
- 测试 518 通过 + 1 跳过。

## qemu-exec 区分超时/崩溃；trace 日配额 48（2026-08-23）

- `fw_qemu_exec`：SIGKILL/137 记 timeout，应用退出码 1/255 记 error，只有 SIGSEGV 等才 crash。
- `TRACE_DAILY_PER_JOB=48`，payload 协议注入不再一天 24 条就 429。minidlna 8200 算 HTTP 端口。

## 协议 payload trace 与 qemu-exec PoC（2026-08-23）

- `fw_request_trace` 增加 `payload` / `payload_hex`：在已映射的宿主端口上发送原始协议字节（SSH/SMB/FTP/SOAP POST/DNS）。省略时按端口发默认探针（SSH banner、FTP USER、SMB negotiate、DNS query），不再只丢 `\r\n`。
- 新工具 `fw_qemu_exec` / `fw_get_qemu_exec`：qemu-user 单次跑二进制，stdin 或 `/tmp/<file>`，返回 crash/rc/signal/output_tail。会话预算 8，当日配额 24。
- `fw_list_binaries`：manifest 精简列表（md5/path/arch）。persona/skill 禁止再写「缺少动态能力」。
- payload 里的 `\\r\\n` 会解开成真正的 CRLF。插件热加载需重启 dsh host。测试 513 通过 + 1 跳过。

## identification 带 md5；非 HTTP trigger；收尾更快续跑（2026-08-23）

- `GET /jobs/{id}/identification` 按路径/basename/文件内容把 manifest md5 补到每条输入（dropbear 与 scp 同哈希时也能对上），并在 `input_types` 加 `binary_md5=`，旧插件压缩视图也能看见。
- 覆盖 trigger 按端口分流：80/8080 HTTP GET；21/22/445 TCP probe；53/547 UDP probe。agent 给 ftp 传 `request_path=/` 不再误走 HTTP。
- docker 把特权端口映射到 `10000+guest`（22→10022），避免和宿主 sshd 抢 127.0.0.1:22 导致 rc=125。
- 函数级 fuzz：hook 写 MIPS `t9`、设 `AFL_MAP_SIZE`；handshake 失败文案改为建议整二进制（AFL++ qemu persistent 官方只保证 x86/arm）。
- 插件预算 fuzz 12（对齐当日配额）；watchdog 空闲 50s / 写「本轮状态」25s 就续跑。测试 506 通过 + 1 跳过。

## 会话进行中就能看到 findings；HTTP 带 Host（2026-08-23）

- `GET /vulnagent/sessions/{sid}` 按 `session_id` 扫 findings 目录，挖掘还在跑也能列出入库漏洞（不再等 idle-reap）。
- HTTP trigger 带 `Host: 192.168.0.1` 和 Referer（部分 httpd 无 Host 直接 RST）。
- `fw_get_fuzz_run` 在 `running` 时提示继续轮询，不要把 120s 跑当成卡死。
- `DSH_HOST_IDLE_SECONDS=7200`，避免一轮结束后 30 分钟被 reap。测试 500 通过 + 1 跳过。

## MIPS fuzz hook 能编过、HTTP trigger 能进 guest（2026-08-23）

- `fw_fuzzhook.c` 在 `__FWHOOK_MIPS__` 下补 `TARGET_MIPS`（qemuafl `struct mips_regs` 被这个宏挡住），参数寄存器按 `target_ulong*` 写 a0–a3。hook 源比 `.so` 新会重编译。函数级 fuzz 同时设 `AFL_ENTRYPOINT`（否则 qemu 必须自然执行到该 BB，守护进程几乎到不了，forkserver handshake 失败）。
- docker trace 改为容器内 `chroot` 进固件 rootfs（`--user 0 --cap-add SYS_CHROOT,NET_BIND_SERVICE`），guest 的 `/etc` `/web` 是固件的；拷宿主 qemu 进树。
- `/tmp/dec-model.conf` 预置 0666（httpd 用 mode 020 创建后再只读打开会 EACCES 然后 SIGSEGV）。
- 已知边界：该 httpd 随后 `shmat` EACCES，qemu-user 仍 SIGSEGV，HTTP 仍 RST；覆盖是启动路径，不是登录页处理函数。
- 宿主 AFL qemu-trace 需 GLIBC 2.43，沙箱镜像 24.04 只有 2.39：fuzz 自动回退宿主并写警告。整二进制模式已跑通（dhcp6s 12s / 5287 execs）；函数级 persistent 在 MIPS 上仍 handshake 失败。
- 409 busy 不再扣当日配额（先抢 TRACE_LOCK 再 check_quota）。默认配额 trace 24 / fuzz 12。
- HTTP trigger 对 RST/拒接重试 3 次。插件预算 trace 12 / fuzz 6；`fw_get_fuzz_run` 失败带 next。测试 499 通过 + 1 跳过。

## 编排器重启后 running trace 会收尸（2026-08-22）

- GET/LIST traces 时，超过 `2*TRACE_RUN_TIMEOUT+30s` 仍 `running` 的记录标 failed（worker 随进程被杀）。避免挖掘 agent 空转轮询、后续 trace 409。

## qemu docker 不盖 /etc /lib，忽略固件 host RPATH（2026-08-22）

- docker trace 不再把固件 `etc`/`lib` 绑到容器同名路径：盖 `/etc` 会让 `--read-only` 挂不上 hostname（docker rc=125、空 coverage）；盖 `/lib` 会换成固件 uClibc，镜像 qemu 报 `stat: no such file`。
- 容器内 guest 绝对路径一律改写到 rootfs 宿主路径；ELF 搜索仍走 `qemu -L`。
- `inspect_dynamic_linker` 跳过带 `..` / 非 guest 的 DT_RPATH（minidlna 残留编译机路径），DT_NEEDED 仍从 `/lib` 解析。

## 静态堆砌后强制动态提示（2026-08-22）

- 连续 3 次静态 fw_* 仍未 `fw_request_trace`/`fw_request_fuzz` 时，后续静态结果带 hint，要求立刻跑 qemu trace。
- persona/skill：识别到首选入口 md5 后立刻 trace，禁止先读完全部 surface/函数。

## 动态验证必须跑工具、沙箱可写（2026-08-22）

- persona/skill 禁止空口“盘点 qemu/fuzz”；空 `fw_list_traces` 会提示立刻 `fw_request_trace`，并补上 `fw_get_trace` 轮询。
- 会话 cwd 改为 `sessions/<sid>/workspace`（可写），不再指向解包目录。沙箱启用 bash/write/edit；web 和子代理仍禁用。
- 固件树只读仍走 `fw_browse_firmware`。
- 新会话默认 `approval_policy=auto`（`FWGRAPH_APPROVAL=auto`），无人值守时可直接跑 `fw_request_trace`/`fw_request_fuzz`；`ask` 才弹审批。
- `POST /jobs/{id}/trace` 允许空/省略 argv（httpd 这类无参数守护进程）。
- Docker 覆盖：固件 `/usr/bin/*` 走 rootfs 宿主路径；不把固件 `etc/lib` 盖到容器上（否则 qemu 无法启动，rc=125）。
- 覆盖跑完立刻释放 `TRACE_LOCK`，攻击路径刷新不再堵住后续 `fw_request_trace`（否则全是 409）。

## 会话删除/归档与立即进对话（2026-08-22）

- `POST /vulnagent/sessions` 先写目录并立刻 202，host 启动和首条 prompt 在后台；工作台拿到 sid 就能进当前窗口。
- `DELETE` 先 discard host（mux 不能再 spawn），杀进程组，重试 rmtree，避免删不掉。
- `PATCH /vulnagent/sessions/{sid}` `{archived}`；`POST /vulnagent/sessions/purge-archived` 统一删除已归档。
- 生成中随时可 `POST .../stop`（含尚未 boot 完的会话）。

## 删除挖掘会话（2026-08-22）

- `DELETE /vulnagent/sessions/{sid}`：若在跑则先停 host，再删会话目录；不删任务 findings。

## 挖掘会话先返回再投递首条 prompt（2026-08-22）

- `POST /vulnagent/sessions` 在 host 就绪后立刻 202，首条 prompt 后台投递，工作台可以先接 mux 再收流式输出。

## 报告列表只保留任务漏洞报告（2026-08-22）

- `GET /reports` 不再列出会话 `report.md`，避免和 `job-*.md` 漏洞报告重复。会话文件仍可按 id 打开，但产品入口只有一份。

## 真实挖掘与报告字段（2026-08-22）

- dsh host 注入 `NODE_EXTRA_CA_CERTS`（编排器自签证书），Node `fetch` 才能打 `https://127.0.0.1:8000` 的 fw_* 接口。
- `record_finding` schema/POST 体补上 `call_chain`、`poc`；服务端这两项必填。入库后刷新任务漏洞报告。
- Archer C7 实测：2 条 critical cmdi，任务报告 `data/reports/job-3a1f3c822c22.md` 与会话 `report.md` 均含调用链和 PoC。

## session.history 缺 sessionId / 编排器重启后卡住（2026-08-22）

- 工作台回填 `session.history` 不带 sessionId；host 重启后若 resume 失败曾把内存里的 id 清空，harness 返回 `bad-request: invalid payload for session.history`，界面卡在 Deep diving。
- `dsh_host.rpc` 始终注入非空 `sessionId`；resume 失败则新建会话并落盘，不再把 id 抹掉。
- mux 与 history 并发不再双开 host（按 sid 串行 boot）；spawn 前清掉 `runner.pid` 孤儿；stop 先 cancel 再杀进程组。
- 已结束会话为回放再拉起 host 时，列表状态仍是 done，不占挖掘并发、不把 Deep diving 挂死。
- 挖洞方法只在 dsh persona + `fwgraph-firmware-hunt` skill，不再拼进用户消息。

## 挖掘编排与调用链/PoC 报告（2026-08-22）

- `POST /firmware` 可带 Form `task`，写入 job 供前置任务保存。
- 工作台挖掘默认 `mode=dynamic`、`max_turns=80`（上限 200）；不再在前置流水线结束后自动开挖。
- 任务报告改成「漏洞报告」：摘要 + 调用链/PoC，去掉清单/输入面/路径大表。
- dsh persona 要求每条 finding 必须有 call_chain 与 poc，并尝试 trace/fuzz。

## Archer C7 全自动链实测与两处卡点（2026-08-22）

固件 `gujian/ArcherC7v2_en_us_3_15_4_up(260427).bin` 走 `POST /firmware?auto=1`（job `3a1f3c822c22`）跑通：解包 133 MIPS ELF → 输入识别 → 反编译 63/64 → 图谱 23201 函数 / 69952 节点 → 攻击路径 50 条（AI 分诊 20）→ 路由 63 全 ok → **15 个攻击面 + 5 条授权链** → 综合报告。终态 `routed`。

修了两个会让流程「看起来卡住」或产物变差的问题：

1. **binwalk 抠片当服务**：squashfs 里的 `*_0_elf.raw` / `*_offset_crc32.raw` 被 `services.lookup` 下划线前缀命中成 httpd/vsftpd，公网输入 25 条（大量重复）且污染 `entry_files`。`discover.is_carve_artifact` 跳过这些文件后输入 15 条，攻击面从历史快照的 0 变成 15。
2. **整库 `source.c` 拖死超时**：rootfs_elf 在按函数导出之前先 `decompile_many` 写整文件 `source.c`，samba_multicall（约 1.6 万函数）把 `IDA_TIMEOUT=1800` 吃光，界面长时间停在「反编译中」。工作台不用这份整库 dump。现 `--skip-source`，只导 `decompile/*.c`。

## 会话摘要带 job_id（2026-08-22）

- `GET /vulnagent/sessions` 摘要增加 `job_id` / `mode` / `engine`，工作台侧栏按固件任务分组会话。

## 挖矿引擎统一为 DeepSeek Harness（2026-08-21）

- `POST /vulnagent/sessions` 只 spawn dsh；缺 `DSH_REPO` 返回 503。builtin 循环仅保留给 `vulnagent` CLI 离线测试。

## 漏洞报告强制写出调用链与 PoC（2026-08-21）

- finding 可存 `poc` / `call_chain`（也接受 `source_summary`→`sink_function`、`exploit_sketch` 兜底）。
- 会话报告与任务综合报告都用中文「调用链」「漏洞 PoC」两节；agent 的 `record_finding` 说明要求补全这两项。

## 路由扫描不再依赖已取消的 idb 路径（2026-08-21）

- rootfs_elf 反编译不写 `data/idb/<job>/<md5>.i64`，`routes.runner` 对缺失 IDB 直接 `idat` 空路径导致整次分析 `graph: IDA route scan failed ... rc=1`。
- 现按 `*.i64` → `database.i64` → 把 ELF 拷到 `data/idb/<job>/<md5>` 解析输入；单个二进制失败/超时只记结果，不再中断整 job。
- `POST /jobs/{id}/routes` 不再要求已有 `data/idb/<job>/` 目录。

## 反编译强制 rootfs_elf + 提高并发（2026-08-21）

- ELF 不再走 `idat -S ida_export.py`。`decompiler._decompile_binary` 对 ELF 调 `tools/ida-no-mcp/rootfs_elf/ida_worker.py`（与 `/home/tankuku/Allzip/rootfs_elf.tar.gz` 同源，idalib），再经 `pipeline/decompile/rootfs_elf_adapt.py` 写成 `functions/<addr>.c` + `symbols_raw.json`，下游图谱/函数页不变。
- raw/PX4 仍用 `ida_export.py`（rootfs_elf 只开 ELF）。
- `.env` `IDA_WORKERS` 从 1 提到 3（与 `.env.example` 默认一致）。

## 空固件 / 加密固件不再误报反编译失败（2026-08-21）

- 解包后 manifest 无 ELF 立即失败，文案说明「未发现 ELF」；若 binwalk 看到 OpenSSL salted 加密则点明无法展开。
- `GET /jobs/{id}/logs` 合并 EMBA 与反编译错误供事件流。
- 启动挖掘时把 `hunt_session_id` 写进 job。

## 自动链解包不再误标 done（2026-08-21）

- `_should_mark_unpack_done`：`auto=1` 或默认 `AUTO_DECOMPILE=1` 时解包结束不写 `done`，避免首页把解包当成整条流水线完成。

## 分析档位 low / high / xhigh（2026-08-21）

- `pipeline/profiles.py`：三档（反编译规模、是否 CFG、是否攻击路径 AI 分诊、路径深度/条数、挖掘 mode/轮次）。缺省 `high` 对齐原全自动。
- `POST /firmware?profile=` 写入 job；`GET /analysis-profiles`；任务列表/详情带 `profile`。
- Low：最多 12 个网络相关二进制、跳过 graphext 与 AI 分诊。XHigh：全量反编译、更深路径、40 条 AI 分诊。
- 自动链在只反编译子集时仍继续建图（去掉 `only_md5s is None` 门禁）。

## 借鉴 Firida 的证据纪律（2026-08-21）

不接入 Firida 三生产者 / NAS 门禁。只把能嵌进现有流水线的契约搬过来：

- `pipeline/evidence.py`：跨工具连接键是 `job_id + binary_md5 + 规范 0x 地址`；动态结果信封 `observed_in_window` / `verified_in_single_trace` / `static_only`；`compose()` 只拼接调用方已取事实，不代查、不改写 IDA/图。
- 攻击路径：节点打 `evidence_address`；分打 `danger_calls` × `entry_outdegree` 小幅加权（默认 0.15/0.05，不压过 source/sink 权重）；`cross.apply` 写 attribution（verified 仍要求**一条** trace 覆盖整链）。
- identification `processing_chain` 增加 `unresolved_needed` / `needed_complete`（缺库只记缺口，不 fail-closed）。
- `/graph/query`：`attack_surface` brief 带 address/attribution；`trace_flow` 带 envelope；新 op `compose_evidence`。函数 brief 带 `decompile_gap`（缺伪代码 ≠ 代码不存在）。Frida 摘要 attribution=`observed_in_window`。
- vulnagent：`fw_compose_evidence`；identification 精简视图带 processing_chain；system prompt / Phase 1 禁止路径名 join、禁止把 window 观察说成 request-caused。

## 图谱摄入 / 攻击路径 / AI 分诊（2026-08-21）

- `pipeline/graph/ingest.py`：`build_tree` 增量；`CBM_GIT=0` 或 `files_copied > CBM_GIT_MAX_FILES` 跳过 git；git 超时 600s。
- `pipeline/attack/edges.py`：CBM CALLS + CFG call 并入 `surface.analyze`；`edge_stats` 写入路径摘要。
- `_graph_worker`：ingest → graphext → attack → routes。
- `pipeline/llm.py` + `pipeline/attack/ai_review.py`：Top-N 路径 JSON 分诊（Anthropic/OpenAI 双风格、默认 16384 tokens、5 条一批）；`AUTO_ATTACK_AI`（有 key 默认开）；`POST /jobs/{id}/attack-ai` + 配额 `attack_ai`。
- `/graph/query attack_surface brief=true` 带压缩 `ai_review`。LLM 失败不导致 attack failed。

## 删除 AI 标注 / AI 增强（2026-08-21）

- 删除 `pipeline/ailift/` 与 `pipeline/decompile/ai_enrich.py`；编排器去掉 `/ailift`、`/aienrich*`、`source?ai=1`、`_ailift_worker`、AILIFT 信号量、`aienrich` 配额、`AI_MAX_FUNCS_PER_JOB` 配置白名单。
- `_decompile_worker` 自动链与 `POST /jobs/{id}/auto` 在 `decompiled`/`ailifted` 后直接 `_graph_worker`。
- vulnagent 双引擎去掉 `fw_request_reanalysis` 与 function source 的 `kind=ai`；`agent.json` 同步。
- 删除 `test_ailift.py`、`test_aienrich.py` 及对应配额/信号量用例；e2e 去掉 ailift 步骤。

位置：VM `~/firmware-graph/fwgraph`（Python 3.14 venv，FastAPI 编排）+ `~/firmware-graph/vulnagent`（Node 22 挖矿 agent）。

## Phase 3 容器化部署轮（2026-08-21）

- **改动清单**：
  - 新增 `deploy/docker/`（仓库根，沙箱 Dockerfile 从 `fwgraph/deploy/docker/` 迁来）：`Dockerfile`（编排器多阶段镜像：node:22-slim 构建 webui dist → python:3.12-slim 运行时，实建 2.28GB）、`Dockerfile.sandbox`（实建 991MB；ubuntu:24.04 + qemu-user 8.2.2 + AFL++ 5.03a + frida-server 17.17.0，非 root uid 1000）、`entrypoint.sh`（自签 TLS 证书持久化到 /data/certs → 后台 cbm-ui（缺席仅告警）→ 前台 uvicorn）、`docker-compose.yml`（orchestrator 服务 8000:8000 + build-only sandbox 服务 `profiles:["build"]`，bind mount `./data:/data` + 对应 `SANDBOX_HOST_PREFIX`）、`README.md`（中文部署/迁移手册）、`ida/`、`cbmbin/` 可选 build-context 占位目录；仓库根新增 `.dockerignore`；`.gitignore` 加 `deploy/docker/data/`。
  - 编排器镜像 Dockerfile 单独钉装 frida 17.17.0、tree-sitter 0.26.0、tree-sitter-c 0.24.2（requirements.txt 缺这三个，未改 requirements.txt）。
  - 沙箱镜像构建期修复：libc6-dev（-lm 链接）、gcc-13-plugin-dev（afl-cc 插件模式）、uid 1000 占用（先 userdel）、afl-fuzz 加 PATH。
  - `pipeline/sandbox/docker_backend.py` 新增 `host_mount_path()`：`SANDBOX_HOST_PREFIX` 非空时把挂载源路径的 `$FWGRAPH_DATA`（默认 /data）前缀改写为宿主路径——解决编排器容器经 docker.sock 起兄弟容器时宿主 daemon 解析路径不一致问题。
  - `pipeline/fuzz/runner.py`：docker 路径对 work 目录 chmod 0777（编排器容器 root 建目录、沙箱 uid 1000 要写）。
  - 部署方式现为两种：宿主机直跑（`run_orchestrator.sh`，保留不变）与 Docker Compose（`deploy/docker/`）。
- **构建与冒烟验证**：两镜像构建成功；compose `up -d` 后容器 healthy，`curl -k` `/`→200、`/login`→200、`/jobs` 无 token→401；编排器容器内经 docker.sock 起兄弟沙箱容器 + `host_mount_path` 改写挂载端到端通过；冒烟后已 down。测试基线 **492 通过 + 1 已知污染项（`test_large_tree_selects_fast_mode`）+ 1 跳过**，与改动前一致。
- **已知限制**：
  - 本机 docker 29 无 buildx，`--build-context` 需用户级插件（已装 `~/.docker/cli-plugins/docker-buildx` v0.29.1）；同样环境的部署机需照做。
  - cbm 二进制 glibc 不兼容（宿主编译需 GLIBC_2.43 > 镜像 debian trixie 的 2.41），容器内 cbm-ui 起不来，entrypoint 告警跳过、编排器照常。
  - frida 本地沙箱（`pipeline/frida/runner.py` 自行 `docker run -d`）的 `-v` 不经 `SANDBOX_HOST_PREFIX` 改写：容器化部署下本地 frida 插桩暂不可用，remote 模式无碍。
  - buildkit 缓存：`COPY --from=<命名上下文>` 缓存键不含上下文内容，切换 ida/cbmbin 上下文需 `--no-cache`。
  - 沙箱容器 uid 1000 与宿主服务用户 uid 需对齐（挂载文件权限）。
  - 容器内 `.env` 里宿主侧绝对路径（如 `EMBA_DIR`）无效，需按容器路径配置。
  - fuzz hook 等宿主编译产物挂入沙箱镜像的 glibc 兼容性（宿主编译 → ubuntu24.04 容器）。

## Phase 2 Docker 沙箱执行层（2026-08-19，fuzz/frida/trace 容器化）

- 新增 `pipeline/sandbox/`（纯标准库）：`docker_backend.py` 提供 `docker_available()`（which+`docker info` 快检，进程内缓存，任何异常视为不可用——探针不向调用方抛）、`sandbox_image_present()`、`run_sandboxed()`（薄封装 `docker run --rm --read-only --cap-drop ALL --security-opt no-new-privileges --pids-limit 256 --memory 2g --cpus 2 --tmpfs /tmp`，返回 Popen 供调用方沿用等待/杀进程；`timeout` 触发看门狗线程 `docker rm -f` + killpg 兜底；容器名经 `proc.sandbox_container` 暴露）、`backend_for(component)`（`SANDBOX_BACKEND` docker|userns|none 默认 docker，组件级 `<COMPONENT>_SANDBOX_BACKEND` 覆盖；docker 不可用时 fuzz/frida→none 宿主直跑、trace→userns 原有裁决）。`image.py`：`SANDBOX_IMAGE`（默认 `fwgraph-sandbox:local`）+ `ensure_image()` + `BUILD_HINT`。
- 新增 `deploy/docker/Dockerfile.sandbox`：ubuntu:24.04 + qemu-user/gcc/make/git-core/python3；构建期编译 AFL++ 核心（容器内 `/opt/AFLplusplus/afl-fuzz`）；python3 urllib+lzma 下载解压 frida-server（`FRIDA_VERSION` build-arg，默认 17.17.0 对齐 venv 实装 frida；requirements.txt 未固定该包）；非 root 用户 sandbox(uid 1000)。
- fuzz（`pipeline/fuzz/runner.py`）：docker 后端且镜像在时 afl-fuzz 走 `run_sandboxed`——rootfs/AFL_PATH/afl-qemu-trace 软链目标/hook 全部宿主容器同路径挂载（rootfs 与运行时 ro，work rw），`--network none`，只看门狗 `seconds+120`；env 仅透传 `AFL_*/QEMU_*/FUZZHOOK_*`。回退宿主路径时 `fuzz.json` 写 `sandbox_backend=none` + 中文 `sandbox_warning`（区分 docker 不可用/镜像缺失）。
- frida（`pipeline/frida/runner.py`）：本地模式 docker 化——`docker run -d --rm --network bridge --read-only --cap-drop ALL --cap-add SYS_PTRACE`（注入需 ptrace）起 frida-server 容器，`docker inspect` 取 bridge IP 后 `add_remote_device` 复用现有 attach/spawn；目标所在目录同路径 ro 挂载；try/finally `docker rm -f fwgraph-frida-<run_id>`。远程模式行为不变。
- trace（`pipeline/trace/qemu_cov.py`）：`trace_exec_mode()` = docker（选中且镜像在）→ 否则 sandbox_mode() 原裁决。docker 路径：rootfs 同路径 ro 挂载 + 顶层目录（bin/sbin/etc/lib/...，不含 usr 以保护镜像内 qemu）同名 ro 绑定对齐 guest 绝对路径文件语义，`qemu -L <rootfs>` 解析可执行文件与解释器；one-shot `--network none`，service 型 `-p 127.0.0.1:<port>:<port>`（trigger/就绪探针不动）；exec 日志经 `/out` rw 挂载取回后清理；结束 `docker rm -f fwgraph-trace-<run_id>`。meta 记 `sandbox_backend`/`sandbox_image`/`sandbox_limits`；userns/root 路径行为不变（root 警告保留）。
- 审计：三处运行 meta（fuzz.json / frida.json / trace.json 的 baseline/trigger meta）均落 `sandbox_backend`、镜像名、资源限额（`DEFAULT_LIMITS` 单一事实源）。
- 测试：新增 `orchestrator/tests/test_sandbox.py` 22 项（命令构造/回退矩阵/fuzz 包装与回退/trace docker one-shot+service/frida 本地容器与回退/镜像提示，全 mock 不碰真实 docker）。基线 488 通过 + 1 已知污染失败（同前）+ 1 跳过。真实 docker 冒烟（借用本地 python:slim 镜像，未构建沙箱镜像）：ro 挂载读取、`--network none` 隔离、看门狗 `docker rm -f` 均验证通过。
- 已知边界：容器内 uid 1000 + cap-drop ALL 无 DAC_OVERRIDE，挂载文件需权限位可读（EMBA 解出 0664 满足；trace 的 /out 已 chmod 777）；afl-qemu-trace 与 fw_fuzzhook_*.so 仍宿主编译后挂载，宿主 glibc 与 ubuntu:24.04 差距过大时需重建匹配镜像；trace docker 模式不做 chroot，guest 对 /usr 下运行期绝对路径读取落到容器镜像（固件库多在 /lib，影响面小）。

## Phase 1 统一配置层（2026-08-19，清除硬编码）

- 新增 `orchestrator/app/config.py`：`FWGRAPH_ROOT` / `data_dir()` / `env_str|env_int|env_bool` / `ida_dir()`（调用时解析，测试可 monkeypatch）；main/accounts/vulnagent_api 的 `FWGRAPH_DATA` 解析改走 config。
- `IDA_DIR` 不再默认写死路径：`decompiler.run_job` 与 `routes/runner.run_job(scan)` 未配置时 fail-fast（"未配置 IDA_DIR"）；`ida_export.py` 的 sig 兜底改 `IDA_SIG_DIR -> idadir("sig") -> $IDA_DIR/sig -> None 跳过`；`admin_api._ida_present` 优先 `IDA_DIR`。
- `EMBA_DIR_DEFAULT` 改为仓库布局推导（`fwgraph/` 同级 `emba/`）；`CBM_BIN` 默认 PATH 查找再兜底 `~/.local/bin`；`QEMU_BIN_DIR` 支持 env 覆盖（默认 /usr/bin）；fuzz runner 的 `FUZZ_MAX_SECONDS`/`AFL_REPO` 改函数内 lazy 读取。
- vulnagent `FWGRAPH_BASE_URL` 默认 `http://127.0.0.1:8000`；`run_orchestrator.sh` 自签证书 CN/SAN 由 `ORCH_CERT_CN`/`ORCH_CERT_SANS` 控制（默认 localhost / 127.0.0.1）。
- 安全：`setup.sh` 删除明文 sudo 密码（改为提示手动执行）；`accounts.ensure_initial_admin` 新增 `FWGRAPH_STRICT_SECRETS=1` 开关（未设 `ADMIN_INITIAL_PASSWORD` 时拒绝以默认口令引导，默认 0 不变行为）。
- `fwgraph/.env.example` 去除全部 `/home/tankuku`，新增 QEMU_BIN_DIR/ORCH_CERT_*/FWGRAPH_STRICT_SECRETS/SANDBOX_BACKEND（沙箱预留）；新增 `vulnagent/.env.example`；tools/ 五个 m2b 脚本改 `${VAR:-原路径}` 兜底。
- 测试基线不变：`pytest orchestrator/tests -q` = 466 通过 + 1 已知污染失败（`test_large_tree_selects_fast_mode`，单跑通过）+ 1 跳过。

## 安全/置信度强化轮（2026-08-15，审查驱动，六线并行）

**AI 增强（aienrich）修复**
- 根因：deepseek-v4-flash 是推理模型，先烧 reasoning_tokens 再输出正文；`AI_ENRICH_MAX_TOKENS` 默认 4096 导致大函数正文被饿死（`ValueError: empty content`，首批 17/18 失败）。默认提至 16384 后 18 中 11，32768 重试回收剩余。env 可调。
- 已知边界：`attack_only=true` 的目标集来自攻击路径；httpd 这类"路由全在库里"的二进制 targets=0（跨库归因的已知体现），跑前先确认目标二进制在攻击路径上（miniupnpd 17 处引用 → 18 目标）。
- resume 机制按 `<addr>.json` 存在性跳过已成功项，失败项自动重试。

**平台安全（main.py / webui.py / accounts.py / admin_api.py）**
- ORCH_TOKEN 轮换（旧值因 access log 泄露作废）；`?token=` query 只接受 `fws-` 会话 token（主 token 仅 Bearer header），uvicorn access log 装 Filter 脱敏 `token=`；`/cbmui` 代理支持会话 token（cookie/query）。
- TLS：`run_orchestrator.sh` 默认 `ORCH_SSL=1`，自签 `data/tls/{cert,key}.pem`（SAN：192.168.141.135/127.0.0.1/localhost）；vulnagent 侧 `.env` 走 `https://127.0.0.1:8000` + `NODE_EXTRA_CA_CERTS`（Node fetch 实测信任通过）。安全头中间件（nosniff/SAMEORIGIN/no-referrer；CSP 因 cbmui iframe 暂不加，代码注释说明）。
- owner 隔离：job/session/report 全量属主化，所有 `/jobs/{id}/*` 端点挂 `job_guard`（陌生人 404 不泄露存在性），`/jobs` 列表非 admin 只看自己且下发 owner 字段；存量数据 owner 回填 admin。
- 登录防爆破：`(username, IP)` 10 分钟 5 次锁 10 分钟（429 含剩余秒数）、不存在用户跑 dummy PBKDF2 抹时序、密码 ≥10 位+弱口令黑名单、bootstrap 默认口令打 `must_change_password`。
- 配额：`accounts.check_quota`（data/quotas.json 原子写，按 job×日，trace 10/fuzz 6/frida 6/aienrich 6，429 中文 detail）；四处 trigger 均审计+配额。
- 上传：流式计数 `MAX_FIRMWARE_BYTES`=2GB（413）、磁盘余量预检（507）、`DELETE /jobs/{id}`（owner/admin，running 409，连 CBM db 与 traces 清理）。
- 并发：反编译/AILIFT 全局信号量（`DECOMPILE_MAX_JOBS=2`/`AILIFT_MAX_JOBS=2`）；`_save_job` 原子写+ENOSPC 容忍；cypher op 写语句关键字白名单拒绝；`/system/info` 的 api_key 掩码仅 admin；`/logs*` 收 admin。
- trace 沙箱：userns 路径已实现（`unshare -Urm`），本 VM 内核禁用 → `TRACE_ALLOW_ROOT_CHROOT=1` 显式授权才走 root chroot，每次运行 meta 写中文警告。
- vulnagent_api：findings 服务端校验 API（`POST /vulnagent/findings` 201 / `PATCH .../{fid}`）：md5 对 manifest、CWE 严格单值、vuln_class 拒混类、置信度锚点（static/static-only 封顶 0.7 并记 `confidence_reported`、observed/verified 强制存在的 trace_id）、状态生命周期 draft/verified/disputed/retracted + history；会话 owner/max_turns（FWGRAPH_MAX_TURNS env 通道）/pid 落盘（重启后 MAX_PARALLEL 仍准、stop 可杀孤儿）；finding 归属改按 session_id（时间戳方案删除）；`_write_report` 异常写 events.sse+runner.log；前置门禁无 job 直接 400。
- report_export docx：H5/H6、链接、引用、水平线、未闭合 fence 兜底、有序列表层级。

**vulnagent / dsh 加固（vulnagent/）**
- dsh profile 整树禁用通用工具（bash/pwsh/fs/web/subagent/workflow 等，cordis.patch.yml + tools.guard 按名拦截双保险；`bash-sandbox` 行不能禁——shell 服务依赖，已注明）；新增 `fw_browse_firmware`（监禁 data/extracted/<job>/，realpath 双重防穿越、64KB、扩展名白名单+NUL 嗅探，无扩展名文本放行）；实 boot 断言 24 工具白名单（静态模式 21 个，动态三件套按 FWGRAPH_MODE 注册）。
- record_finding（tools.js + dsh 插件）改走服务端 API，本地预检镜像服务端规则，422 detail 原文给 AI 自愈；executeTool 按 session.mode 硬拒动态工具（含 resume）。
- 六阶段真实化：Phase3 dropped→PATCH retracted、verified→PATCH verified；Phase4 最后一行精确匹配 PASS；maxRounds LOOP 终局 findings 标「未经对抗验证」；Phase5 FINAL.md 按模板产出（PASS/LOOP 都有，LOOP 首行告警）；记忆 ±0.05 clamp [0.1,0.95] 原子写回 + 固件 tag 门控注入；`hunt --mode` 透传；phase4 文献固件域改写。
- 其他：state.json 补 job_id、idle 置 completed、agent.json model 块生效（env 优先）、system_prompt 写置信度锚点。
- 测试：`npm test` 27 全过（node:test 零依赖）。

**管线置信度（pipeline/inputs、surfaces、graphext、report.py）**
- inputs：证据四级分级（observed/config/kb_default/candidate）+ port_source/autostart/needs_confirmation/auth_posture 字段；etc 多变体合并+inittab+rcS 一层递归+全 ELF socket 提升；busybox 非监听 applet 特例表+multicall 导入并集不作证据+applet 存在性检查；落选留痕 excluded；gate2 独立复算对账可失败。MX12：5→39 输入、幻影 syslogd 消除、dnrd observed 找回。
- surfaces：busybox applet 锚定 BFS（`<applet>_main` 符号→名串 xref 两级），无法锚定降级 `applet_boundary: unresolved`（不再漫游）；GoAhead 路由注册模式恢复 endpoints + 跨库归因（libgo.so→httpd）；桩检测排除 final_handler；载体绑定真（伪 C 行号）/占位（carrier_hint）分离；授权链结构双要件（凭证比较+拒绝分支）+调用边排序+跨库追踪+配置姿态（anonymous_enable 等）。MX12：AS 克隆 0、AS-003 终点 TendaAte（conf 0.98）+8 路由、24 条授权链。
- graphext：LDR PC 三分支（=imm jump/[跳表] indirect_jump/弹栈 ret）、aarch64 助记符表、MIPS 延迟槽、TBB/TBH 建模、ADD/MOV-to-PC；done 记 cfg_stats（孤儿率/indirect_jump 数/cfg_failed）+ ast_stats（ERROR 率/截断数）。AST 预处理剥离 `__noreturn` 等属性宏。MX12：假 ret 29.7%→0.38%、AST ERROR 10.1%→0.09%。
- report.py：finding 字段 markdown 转义防注入；未关联发现挪附录带警示；结论证据触发（无无条件模板句）；分档带 remediation+confidence 二排；截断标注；excluded/unreadable 明细挪附录；**retracted finding 不进报告**。

## 新增：产品化后端（`orchestrator/app/accounts.py` + `admin_api.py` + `pipeline/report.py`，本轮）

- **账号体系（accounts.py，纯标准库）**：`data/users.json`（PBKDF2-HMAC-SHA256 100k 迭代 + 16 字节 salt，`hmac.compare_digest` 校验）；`data/sessions.json`（`fws-`+48hex token，7 天滑动 TTL，tmp+rename 原子写）；`data/audit.jsonl` 审计（`audit(user, action, detail)`）。首启自动创建 admin（`ADMIN_INITIAL_PASSWORD`，默认 `admin123`，日志打印提示）。所有路径从 `FWGRAPH_DATA` 动态解析，JSON 损坏容忍。
- **require_token 改造（main.py，兼容优先）**：空 ORCH_TOKEN → 放行（测试语义不变）；Bearer==ORCH_TOKEN → legacy admin principal；否则查会话表。返回 principal dict。新增 `require_admin`（非 admin 403）。既有端点签名零改动。
- **端点（admin_api.py，`setup(app, require_token, require_admin)`，注册在 webui SPA catch-all 之前）**：
  - 认证：`POST /auth/login`（公开）、`GET /auth/me`、`POST /auth/logout`、`POST /auth/password`（改密后吊销其他会话）。
  - 用户（admin）：`GET/POST /users`、`PATCH/DELETE /users/{u}`；最后可用 admin 不可禁用/降级/删除，不可自禁用/自删。
  - 系统：`GET /system/info`（组件健康 ida/emba/cbm/frida/dsh/afl_qemu×4 架构、LLM 掩码 key、disk/mem/loadavg）；`GET/PUT /system/config`（13 键白名单：AUTO_*、VULNAGENT_ENGINE、LLM_MODEL/BASE_URL、AI_MAX_FUNCS_PER_JOB、IDA_WORKERS、EMBA_TIMEOUT；存 `data/settings.json` 并即时 os.environ 生效，startup 时应用）。
  - 日志：`GET /logs`、`GET /logs/tail`（name 必须在列举集合内 + resolve 防穿越）；`GET /audit`（admin，倒序）。
  - 仪表盘：`GET /dashboard`（jobs_by_status/running/sessions/findings/reports/inputs/surfaces 计数）。
  - 报告：`POST/GET /jobs/{id}/report`、`GET /reports`（job 综合报告 + vulnagent 会话报告聚合，rid=`job-<id>`|`sess-<sid>` 严格正则）、`GET /reports/{rid}[/download]`。
- **综合报告生成器（pipeline/report.py，628 行）**：确定性中文 Markdown（不调 LLM），九章节：执行摘要（计数+发现按严重级统计+最高风险点一句话）/ 固件清单统计（架构、文件类型、checksec 六项防护汇总）/ 外部输入识别（含处理链库与 busybox 分发链）/ 攻击面详情（每 AS 路由链表+终点 handler+载体绑定+授权链引用，AS-AUTH 全链单列）/ 攻击路径 Top20（source→sink+经过函数）/ 动态分析结果（fuzz/frida run 表）/ AI 挖掘发现（每 finding 严重级/CWE/证据/利用思路/修复建议）/ 结论按严重级分档 / 附录（工具链+模型+平台名）；缺产物写「（无数据）」。产物 `data/reports/job-<id>.md`。
- **DOCX/PDF 导出（`orchestrator/app/report_export.py`）**：自写 markdown→docx 转换器（标题/管道表格 Table Grid/列表/代码块/粗体；正文宋体+Times New Roman、标题微软雅黑、eastAsia 两级设置）；`GET /reports/{rid}/export?fmt=docx|pdf`（登录即可，审计 report_export）；pdf 走 `soffice --headless --convert-to pdf`（已 apt 装 libreoffice-writer，600s 超时、全局锁、中文报错）；docx/pdf 按源 md mtime 缓存失效。实测 job 综合报告 docx 48.9KB、pdf 663KB/27 页，pdftotext 中文完整、pdftoppm 目检排版正常。
- **一键自动链**：`POST /firmware?auto=1`（job 存 `auto` 标志，jobs 列表/详情已暴露该字段）或 `POST /jobs/{id}/auto` 补跑；`_decompile_worker` 成功且非 only_md5s 时若 auto（或 `AUTO_FULL=1`）→ 自动 `_graph_worker`（链内已含 attack/routes/surfaces/graphext）；graph 链尾 auto job best-effort 生成综合报告。
- 审计覆盖：login_ok/login_fail、logout、password_change、user_create/update/delete、config_change、firmware_upload、report_generate、report_download。

## 动态分析双引擎（`pipeline/fuzz/` + `pipeline/frida/`）

- **产品设计**：x86 目标 → frida 本机/远程插桩；arm/mips 目标 → qemu+AFL++ **函数级 fuzz**（不整机模拟）。
- `pipeline/fuzz/`（AFL++ QEMU persistent 模式）：`fw_fuzzhook.c` 通用参数注入 hook（按 ABI 写 arg 寄存器 arm r0-r3 / arm64 x0-x7 / mips a0-a3 / x86_64 rdi..r9，输入放 guest 栈，`FUZZHOOK_ARGS` 环境变量驱动，一次编译复用）；`runner.py` 支持函数级（function+args）与整二进制（argv/@@）两种模式。
  - 关键实现事实：persistent 模式要求目标函数在**正常启动流程中可达**（forkserver 在该地址初始化，AFL_ENTRYPOINT 只是标记不会跳转）；PIE 需加加载基址；`AFL_QEMU_PERSISTENT_EXITS=1` 兜住调用 exit 的函数；`QEMU_LD_PREFIX` 指向 rootfs。
  - afl-qemu-trace 按架构逐个构建（`~/AFLplusplus/afl-qemu-trace-<arch>`，afl-fuzz 只认 AFL_PATH 下的固定文件名 → runner 自动建 per-arch 链接目录）。
- `pipeline/frida/`：本机模式（host 留空，`get_local_device`，x86 目标直接附加/拉起）+ 远程模式（host:27042 接真机/仿真）；hook 目标支持模块+导出符号或模块+偏移；命中事件（参数+24 字节快照）写 events.jsonl。已适配 frida 17 API（`Module.findGlobalExportByName`/`Process.getModuleByName` 回退链）。
- API：`POST/GET /jobs/{id}/fuzz[/{run_id}]`、`POST/GET /jobs/{id}/frida[/{run_id}]`（202 后台跑 + 轮询；独立锁，不占用 job 状态机）。
- 实测：frida 本机 hook /bin/cat 的 read（hooked=1 hits=2）；函数级 fuzz 合成 ARM 目标 30 秒 10 万次执行（3364/s，纯函数体）；goahead 类固件 daemon 因 init 环境依赖（需 chroot/网络）honest 失败并给中文提示。

## 工控协议模糊测试（`pipeline/protofuzz/`，M-ICS-1/2，2026-08-15）

- 定位：黑盒网络协议 fuzz，测试对象为**存活设备**（真机/仿真环境），与固件静态分析互补；参考博智工控漏洞挖掘平台 V3.2 软件化其主线，硬件依赖能力（总线卡/DI/AI/电源托管）明确不做。
- `protocols.py` 模板库 6 种：Modbus TCP(:502)/S7(:102)/OPC UA(:4840)/DNP3(:20000)/MQTT(:1883)/HTTP(:80)；模板=种子报文+字段锚点（offset/length/strategy）。
- `engine.py` 确定性变异：boundary/bitflip/fill/overflow（插入 64/256/1024B）/random（种子 PRNG）；基线种子报文在前作连通性自检；max_cases 截断。
- `monitors.py` 纯软件监视器：tcp_probe/icmp_ping（系统 ping）/protocol_probe（发未变异种子）。
- `runner.py`：connect-per-case 发送，outcome 分类（response/empty/timeout/refused/reset）；异常→监视器确认 confirmed/suspected→confirmed 复播 3 次统计复现率+恢复判定（博智循环分段的软件近似）；run.json 每 case 原子刷新；STOP 文件停止；`build_report` 生成中文 md（目标/统计/故障明细/结论）写入 data/reports/pf-<id>.md。
- API（`orchestrator/app/protofuzz_api.py`，先于 webui 注册）：`GET /protofuzz/protocols`、`POST /protofuzz`（202）、`GET /protofuzz`、`GET /protofuzz/{id}`、`POST .../stop`、`POST .../report`。
- 安全闸：acknowledge=true 强制授权确认；目标默认只允许私网/回环（`PROTOFUZZ_ALLOW_PUBLIC=1` 解锁公网）；配额 kind `protofuzz`（PROTOFUZZ_DAILY=8，job 或 pfuser:<name> 维度）；审计 trigger/stop/report 带目标 detail；owner 隔离（他人 404）。
- 报告中心集成：admin_api/report_export 的 `_RID_RE` 与路径解析支持 `pf-` 前缀，kind=protofuzz，DOCX/PDF 导出复用既有管线。

## 漏洞挖掘双模式 + AI 协作 + CFG/AST

- **上下文预算（P1/P2/P3，2026-08-15）**：实测 MX12：裸函数清单 28.7MB vs attack_surface top20 仅 12KB——图谱预计算已把检索面收窄 3 个数量级；在此基础上再压三刀：
  - **P2 函数分级检索**：新端点 `GET /jobs/{id}/functions/{md5}/{addr}/brief`——约 1KB 分诊卡（symbols.json 攻击面元数据 + 伪代码头部 8 行 + 危险调用行号（strcpy/system/recv 等 22 个 libc）+ 出边 callees（按二进制内函数名集过滤））；双引擎 `fw_get_function_source` 加 `brief=true`/`kind=brief`，工具描述与 agent.json 系统提示都改为「批量筛查先 brief，疑点再拉全文」。
  - **P3 图谱 op 瘦身**：`attack_surface` 加 `brief=true`（掉 chain 节点，只留 path_id/score/source/sink/消毒/观测标记；top20 实测 12KB→7.1KB）；`cfg`/`ast` 加 `max_nodes`/`max_depth` 截断（`_trim_cfg`/`_trim_ast`，带 truncated/total 标记）。
  - **P1 会话历史压缩**（仅 builtin 引擎）：`session.js` 导出纯函数 `compactHistory()`，每轮 LLM 调用前把超过 1200 字符的旧 tool_result 替换为首尾摘录占位（含工具名与「可用更精确参数重查」提示），最近 6 条与首条任务不动；幂等（前缀标记），压缩计入事件流；`VULNAGENT_COMPACT_THRESHOLD/KEEP_RECENT` 可调。dsh 引擎已自带 compaction-basic + tool-result-pruner（base bundle 挂载，已核实），无需处理。
- **双模式**：`POST /vulnagent/sessions` 收 `mode: static|dynamic`。纯静态模式收回动态工具（`fw_request_trace/fw_request_fuzz/fw_request_frida`，builtin 引擎在 `Session.enabledTools` 过滤，dsh 引擎在插件 `apply()` 按 `FWGRAPH_MODE` 不注册）；动静结合模式开放全部 24 个工具（新增 `fw_request_fuzz/fw_request_frida/fw_get_fuzz_run`）。两模式均产出中文标准报告（dsh 会话由 `vulnagent_api._write_report` 补齐 report.md）。
- **前置门禁**：启动会话要求目标 job 已完成图谱构建（graph_done）+ 攻击面分析（attack_done），否则 409；payload 可带 `job_id` 覆盖默认。
- **AI 协作通道**：`fw_request_reanalysis` 已于 2026-08-21 随 AI 增强一并删除。
- **AI 增强产物不回喂挖矿 AI（2026-08-15）**：增强 overlay 会重命名函数/变量，直接喂给挖矿 AI 会导致证据引用与地址/符号失真。现 `fw_get_function_source` 双引擎均默认返回 Hex-Rays 原始伪代码（唯一可引用证据），`ai=true`/`kind=ai` 显式索取才给 overlay 且响应带 caveat 警告；增强产物定位转为攻击面/调用链分析的辅助读物（前端攻击面/函数视图内查看），挖矿侧描述明确「不可引用其臆造符号」。
- **M4b CFG/AST（`pipeline/graphext/`）**：CFG 从函数级 .asm 解析基本块+边（cond/fall/jump/call/ret，精确助记符表避免 BFI/LDR 误判）；AST 用 tree-sitter C 语法解析伪 C（深度/节点数封顶）。产物 `data/graphext/<job>/cfg|ast/<md5>.json`；`/graph/query` 新 op `cfg`/`ast`；vulnagent + dsh 双引擎新增 `fw_get_cfg/fw_get_ast` 工具；`POST/GET /jobs/{id}/graphext`，graph 链自动跑（`AUTO_GRAPHEXT=1`）。MX12 实测：169 二进制、67045 个 CFG、55474 个 AST。

## 测试基线

`fwgraph/.venv/bin/python -m pytest orchestrator/tests -q` → **438 项：437 通过 + 1 跳过**。`test_large_tree_selects_fast_mode` 改为设置 `CBM_FULL_INDEX_MAX_FILES`，全量不再污染失败。`test_findings_api.py` 仍有 1 项全量下偶发污染性失败（单跑/整文件均过，隔离待做）。

## M6a 外部输入识别（`pipeline/inputs/`）

- `services.py` 已知服务知识库（60+ 守护进程：协议/默认端口/传输/输入类型/loopback_only/not_public 标记）；`discover.py` rootfs 扫描器；`elfchain.py` pyelftools 事实（DT_NEEDED→处理链库、未定义符号 socket 证据、exec 路径分发检测）；`runner.py` 组装 + 双验收门。
- 证据源（确定性，无 LLM）：systemd 单元 / init.d、rcS / inetd、xinetd / 守护进程配置（listen/bind/port 指令，nginx listen 全量解析）/ 已知服务二进制 / **web 端点扩展**（conf.d vhost、CGI 目录分组、webapi 模块组）。
- 多根扫描：`locate_roots` 发现主 rootfs + rd_root + packages 容器（SPK 明文 tar 自动解开；`5badbeef` 加密 SPK 记入 `metadata.unreadable`，不静默缺失）；`etc/etc_ro/etc.defaults` 三变体兼容（路由器固件 /etc 运行时符号链接布局）。
- busybox 多态：入口二进制是符号链接时解析到真实 ELF，argv[0] 分发记入 `dispatch_chain`。
- 未知守护进程提升规则：必须导入 `bind` + listen/accept/recvfrom 系符号（NETLINK 设备管理器 mdev/udevd 等已加排除表）；同 (协议,端口,传输) 别名合并；不可公开项（回环/unix/VPN/Wi-Fi AP 等）进 excluded 并带原因。
- **门禁**：gate 1 全部公网可达（回环/localhost 一律拒绝）；gate 2 发现的监听者恰好在 inputs 或 excluded 出现一次；`validate()` 程序化检查。

## M6b 逐输入攻击面（`pipeline/surfaces/`）

- 每 IN-xxx 导出一个 `AS-xxx.json` 到 `data/surfaces/<job>/information/`（随附 identification.json），授权链独立 `AS-AUTH-<NNN>.json` 并被面引用。
- 字段（严格按 `information/_schema.json`）：routing_path（listen→accept→parse→normalize→dispatch→handler，文件/函数/地址证据）、dispatchers/parsers/normalizers、final_handler（路由表 handler > 链上最深 handler 角色函数 > static-only 兜底）、carrier_bindings（getenv/sscanf/recv/cgi 访问器 → 变量，证据 `地址:行号`）、auth_chain_refs。
- 函数角色分类启发式（`classify.py`）：accept/parse/normalize/dispatch/handler/auth 六角色；busybox 链较粗，存在跨 applet 噪音（已记录，见项目文档已知限制）。
- 门禁：每 IN 恰好一个 AS、必填字段非空、授权链独立且引用闭环；输出 errors + warnings（static_only 等）。

## Orchestrator API 变更（`orchestrator/app/main.py`）

- 端点：`POST/GET /jobs/{id}/inputs`、`GET /jobs/{id}/identification`、`POST/GET /jobs/{id}/surfaces`、`GET /jobs/{id}/surfaces/{sid}`。
- 状态：`identifying`/`identified`、`surfacing`/`surfaced`；`STATUS_RUNNING` 同步。
- 自动链：提取完成 → `AUTO_INPUTS`（默认 1）；graph 链尾（attack/routes 后）→ `AUTO_SURFACES`（默认 1，best-effort 不翻转任务状态）。
- 修复：`extractor.py` `_chown_output` 支持 `EMBA_SUDO_PASSWORD`（原来 `sudo -n` 必败）。
- EMBA profile：`config/extract-only.emba`（黑名单制；EMBA 不允许手动选 P 模块）。

## vulnagent（M8，重构）

- **工具 20 个**（`src/tools.js`）：新增 `fw_get_identification` / `fw_list_surfaces` / `fw_get_surface`；`agent.json` 白名单与 system_prompt playbook 同步。
- **LLM 双协议**（`src/llm.js`）：`LLM_API_STYLE=anthropic`（/messages）| `openai`（/chat/completions，Anthropic↔OpenAI 消息与工具双向转换）；当前 .env 走 openai 风格接 opencode go。
- **六阶段打法**（xiaomin skill 整合）：`playbooks/vuln-hunt/`（references + strategies + 154 条记忆库 `memory/insights.jsonl`），`src/playbook.js` 驱动 Phase1 攻击面锁定 → Phase2 分面深挖 → Phase3 验证 → Phase4 对抗 challenge（PASS/LOOP 循环）→ Phase5 报告；`node src/cli.js hunt "预认证漏洞"`。
- **dsh 引擎（默认）**：`orchestrator/app/vulnagent_api.py` 的 `VULNAGENT_ENGINE=dsh` 默认，`--engine builtin`/环境变量可回退。

## DeepSeek Harness 移植（`vulnagent/dsh/` + VM `~/deepseek-harness`）

- 构建：pnpm 11.7（corepack shim 坏，用 `node ~/.cache/node/corepack/pnpm/11.7.0/bin/pnpm.cjs`；`~/.local/bin/pnpm` 包装器）；需 `pnpm add -Dw unrun`（tsdown 依赖解析）；`setup.sh` 一键复现。
- profile `fwgraph`（`~/.dsh/profiles/fwgraph`）：dsh-base + dsh-headless + 两个零依赖插件 + 挖矿 persona（方法论+证据纪律固化）。
- 插件 `@fwgraph/dsh-fwgraph-tools`：11 个 fw_* 只读工具 + record_finding（手写 JSON Schema，绕过 defineTool 的 DSL——网关拒绝裸属性表）。
- 插件 `@fwgraph/dsh-fwgraph-events`：挂 `llm/stream`、`tools/pre/post-execute` 瀑布钩子，reasoning-delta/text-delta/工具事件/usage 实时写 vulnagent 格式 events.sse（`{stream, block}` 快照语义），供前端流式渲染。
- 实测：dsh 会话对 AS-002(telnetd) 产出函数级评估并纠正静态分类噪音；会话经 UI 同路径 API 启动 0 报错。

## 运维要点

- **改完 Python 代码必须重启 orchestrator**（模块缓存）：`kill $(cat data/orchestrator.pid) && bash scripts/run_orchestrator.sh`。
- `run_orchestrator.sh` 现在会自动拉起 CBM UI 守护进程（`scripts/run_cbm_ui.sh`，幂等、best-effort）——图谱 layout 和 /cbmui 代理由它支撑，之前 orchestrator 单跑导致 layout 502。
- 前端改完在 `webui/` 跑 `npx vite build`（静态目录即时生效，无需重启）。
- 日志：`fwgraph/data/orchestrator.log`；EMBA：`data/extracted/<job>.emba.log`；审计：`data/audit.jsonl`；CBM UI：`data/cbm_ui.log`。
- 初始管理员：admin / `ADMIN_INITIAL_PASSWORD`（默认 admin123），生产部署请立即改密。

## 后端待办

- surfaces 角色分类降噪（busybox 按 applet 过滤调用链）；carrier_bindings 覆盖更多读取模式。
- M7 trace 在 MX12 上跑一轮，把攻击路径升级为 observed/verified。
- vulnagent hunt 模式与 dsh 引擎的轮次编排打通（当前 hunt 走内置引擎）。
