# 固件代码图谱系统 — 历史开发计划

> **现行目标覆盖本文件早期设想**：项目不是自动漏洞挖掘器，也不要求 AI 命名或语义标签后才能建图。当前事实与执行顺序以 `项目介绍.md` v1.5 和 `fwgraph/README.md` 为准：优先保留地址、原始 IDA 名称、伪 C、调用/usage、字符串、route 与 trace 等可审计证据，供上游 AI 用较少上下文自行分析；AILIFT 仅保留为默认不运行的兼容接口。

> 历史目标：固件解包 → 反编译 → 可选 AI 辅助 → codebase-memory-mcp 图谱 → 上游分析 AI / 前端。
>
> **还原质量双目标**：
> ① 整体——所有函数都要大致还原且有意义（可读伪 C + 有语义的命名，
> 不允许大片 `sub_XXXX` 无人处理、裸奔入库）；
> ② 重点——容易被遗漏的函数级漏洞点和攻击面
> （危险函数调用点、网络可达解析路径、命令执行/注入面、认证绕过点）优先深度还原。
>
> 开发机：Ubuntu 26.04 VM（192.168.141.135，8 核 / 7.2G 内存 / 73G 可用磁盘，已有 git/docker/python3）。
> 代码位置：`~/firmware-graph/`（两个参考项目已克隆于此）。

---

## 0. 现状调研结论（已完成的阅读）

### codebase-memory-mcp（下称 CBM）
- 纯 C 单二进制、零外部依赖（无 Neo4j/Qdrant），树内嵌 158 个 tree-sitter 语法 + SQLite（FTS5 + 768 维向量）。
- **C/C++ 是一等公民**：函数级节点（含 signature/complexity 等属性 JSON）、`CALLS` 边、struct→Class 节点；内置语义检索属于可选能力，大任务使用 FAST 结构索引时不生成相似性/语义边。
- 摄入方式：`codebase-memory-mcp cli index_repository --repo-path <目录>`，**不要求 git、不要求可编译**——直接索引"伪 C 文件树"即可。
- 对外接口：MCP（**仅 stdio**，15 个工具：`search_graph`/`query_graph`(Cypher 子集)/`trace_path`/`get_code_snippet` 等）+ CLI。这正好是给上游 AI 用的通道。
- 限制（设计时必须绕开的坑）：
  1. **无自定义元数据字段** → 函数地址/所属二进制/架构用"目录结构 + 命名 + 行首注释"编码，或索引后直接改 SQLite 的 properties JSON。
  2. `get_code_snippet` 查询时**从磁盘读源文件** → 伪 C 文件必须持久保留。
  3. Web UI 硬绑 127.0.0.1 且校验 Host/Origin 头 → 对外访问需自建反向代理改头，或自研前端；上游 AI 走 MCP/CLI 则不受限。
  4. 非 git 目录无 watcher，更新靠重跑 `index_repository`（哈希增量仍可用）；在伪 C 目录 `git init` 一次可白嫖 watcher。

### emba
- 纯 Bash 编排 + Docker（特权容器），模块分组 P（提取）/S（静态分析）/L（仿真）/F（报告）/Q（AI 连接器）。
- 提取链路：P02 识别约 25 种固件格式 → P55 unblob / P50 binwalk 递归 → P60 深度提取（4 轮）→ P99 输出**机器可读架构清单** `csv_logs/p99_prepare_analyzer.csv`（路径;ELF class;大小端;Machine;gcc 猜测;MD5）。
- 厂商加密固件专用解密器（Foscam/Buffalo/Zyxel/DJI/UEFI…）是其相对裸 unblob 的主要增量。
- **接入决策：用 EMBA docker 镜像只跑提取阶段**（自定义 profile 屏蔽 S/L/F/Q），消费 `firmware/` rootfs + `p99` CSV。轻量备选：直接 `pip install unblob`。

### LLM（已验证可用）
- 端点：`https://api.siliconflow.cn/v1`（OpenAI 兼容），模型 ID：**`deepseek-ai/DeepSeek-V4-Flash`**（注意必须带 `deepseek-ai/` 前缀，裸写会报 Model does not exist）。
- 该模型带 reasoning 输出（`reasoning_content`），prompt 设计时注意解析 `content` 字段；并发与额度以 SiliconFlow 账户限流为准。
- API key 存 `fwgraph/.env`（`LLM_API_KEY=...`），**不进 git**（`.gitignore` 排除）。

### IDA Pro 9.1（已安装，反编译引擎）
- 位置：`/home/tankuku/ida-pro-9.1/`（便携版），License 已激活，IDAPython 已切到系统 Python 3.14 实测可用。
- **无界面用法（必须 `TVHEADLESS=1`，否则 TVision 报错）**：
  ```bash
  # 批处理分析，生成 .asm 和 .i64
  TVHEADLESS=1 /home/tankuku/ida-pro-9.1/idat -A -B /path/to/sample
  # 跑 IDAPython 脚本
  TVHEADLESS=1 /home/tankuku/ida-pro-9.1/idat -A -S/path/to/script.py /path/to/sample
  ```
- 相对 Ghidra 的优势（本项目直接受益）：Hex-Rays 伪 C 质量更高；自带 **FLIRT 签名**（识别编译器运行时/libc 函数）+ **FLAIR** 工具可自制 sig；**TIL 类型库**提供 libc 原型（对应 Ghidra 的 .gdt）；**Lumina** 服务可拉取社区函数元数据（可选增强，视 license 权限）。
- 多架构覆盖 MIPS/ARM/ARM64/PPC/x86/x64 等固件主流架构。

---

## 1. 总体架构

```
┌────────────┐   ┌──────────────────┐   ┌─────────────────────────┐
│ Web 前端    │──▶│ 编排服务 FastAPI  │──▶│ 任务队列 (Redis/RQ 或    │
│ (对外 0.0.0)│   │ (0.0.0.0:8000)   │   │  Celery + 本地 worker)  │
└────────────┘   └──────────────────┘   └─────────────────────────┘
                            │  流水线 4 级
        ┌───────────────────┼────────────────────────────┐
        ▼                   ▼                            ▼
 [S1 解包]            [S2 反编译]                   [S3 AI 符号恢复]
 EMBA docker         IDA Pro 9.1 无头批处理          DeepSeek-V4-Flash
 (提取专用 profile)   + FLIRT 签名 + TIL 类型库      ★漏斗筛选(只送难/必)
 → rootfs + 架构CSV   → Hex-Rays 伪 C 导出          ★统一命名规范约束
                            │                       → 回写 IDB 二遍反编译
                            └──────────┬─────────────────┘
                                       ▼
                              [S4 图谱构建]
                              伪 C 文件树 → CBM index_repository
                              → SQLite 图 (含向量)
                                       │
                     ┌─────────────────┼──────────────────┐
                     ▼                 ▼                  ▼
              上游漏洞挖掘 AI      图谱查询 API        Web 前端
              (MCP stdio/CLI)    (FastAPI 转发 CBM)   (React/Vue + 图可视化)
```

**目录约定**（Ubuntu 上）：
```
~/firmware-graph/
├── codebase-memory-mcp/      # 参考/二进制来源（已克隆）
├── emba/                     # 参考 + 提取用镜像（已克隆）
└── fwgraph/                  # 本项目代码（新建）
    ├── orchestrator/         # FastAPI 编排服务 + 任务队列
    ├── pipeline/
    │   ├── extract/          # S1 调 EMBA/unblob
    │   ├── decompile/        # S2 IDAPython 批处理脚本（分析/导出/回写）
    │   ├── ailift/           # S3 漏斗筛选 + DeepSeek 并发 + 命名规范约束
    │   └── graph/            # S4 伪 C 落盘 + CBM 摄入 + 元数据注入
    ├── libc-sigs/            # 自制 FLIRT sig 与 TIL 类型库资产
    ├── config/               # naming_spec.yaml（命名规范）等全局约束配置
    ├── webui/                # 前端
    └── data/                 # 固件上传、解包结果、IDB、伪 C 树、CBM 索引（持久保留！）
```

---

## 2. 里程碑计划（建议 6 周）

### M0 — 环境搭建（2~3 天）
- ~~IDA Pro~~ ✅ 已装（9.1，无头 + IDAPython 可用）。
- VM 扩容建议：**内存加到 16G**（多个 idat 并发 + EMBA docker，7.2G 偏紧），磁盘 +100G。
- 安装：EMBA docker 镜像（`sudo ./installer.sh -d` 或仅 pull 镜像）、Python 3.11 venv（fastapi/uvicorn/httpx/asyncio/pyyaml；**注意与 IDA 用的系统 Python 3.14 分开**，编排服务不依赖 IDAPython，只 fork `idat` 进程）、CBM（`install.sh --ui`）、Redis（或先用 SQLite 队列，后期再换）。
- LLM 配置落盘：`fwgraph/.env` 写 `LLM_BASE_URL=https://api.siliconflow.cn/v1`、`LLM_MODEL=deepseek-ai/DeepSeek-V4-Flash`、`LLM_API_KEY=<key>`。
- 验证：CBM `cli index_repository` 索引一个样例 C 目录 + `search_graph` 查询通；`TVHEADLESS=1 idat -A -B` 批处理一个 ARM/MIPS 样例 ELF 通。
- 验收：一键脚本 `scripts/setup.sh` 能复现全环境（IDA 除外，文档注明其已预装）。

### M1 — 解包子系统 S1（3~4 天）
- 编写提取专用 profile（屏蔽 S/L/F/Q 模块），FastAPI 封装：
  `POST /firmware`（上传）→ 起 EMBA docker → 轮询日志目录 → 解析 `csv_logs/p99_prepare_analyzer.csv` 产出 `manifest.json`（每个 ELF：路径/架构/大小端/MD5/所属固件）。
- 架构过滤：只把 ELF（MIPS/ARM/x86/PPC…）送下游，跳过文本/图片。
- 验收：3 个公开固件（如 D-Link、TP-Link、Netgear 各一）解包出 rootfs 且 manifest 正确。

### M2 — 反编译子系统 S2（8~10 天，**核心难点**，引擎 = IDA 9.1）
目标：**整体大致还原且有意义 + 攻击面路径优先保质量**，两者兼顾。
1. **IDA 无头批处理流水线**：编排层按 CPU 核数起 N 个 `idat` worker（起始 N=3，每个都是独立进程），逐 ELF：`idat -A -S/export_pseudocode.py <elf>`。脚本内：`ida_auto.auto_wait()` 等自动分析完成 → 遍历 `idautils.Functions()` → `ida_hexrays.decompile(ea)` 导出每函数伪 C。失败/超时的二进制记录降级原因进 manifest。
2. **libc 签名识别（还原度的关键，IDA 原生能力 + 自制增强）**：
   - IDA 自带 FLIRT 默认先跑一遍（识别编译器运行时与常见 libc 模式）；
   - 用 **FLAIR**（`pelf`/`sigmake`）自制签名资产：交叉编译固件常见 libc（uClibc/uClibc-ng、musl、glibc，按 ARM/MIPS/x86 × 大小端 × 常用版本）生成 `.sig`，放入 IDA `sig/` 目录按需加载；静态链接二进制这是恢复 libc 真名的主力；
   - 动态链接二进制走 import 表 + `.dynstr` 恢复外部符号（直接有名字，最简单的一类）；
   - 可选：评估 **Lumina** 拉取社区函数元数据作为第三来源（取决于 license 权限，M2 中期验证）。
   - 未命中的保留 `sub_XXXX` 交给 M3 漏斗裁决。
3. **类型信息**：按需加载对应架构 TIL；对命中签名的函数应用 libc 原型——正确原型 → 正确参数/返回值（Hex-Rays 输出质量的杠杆）。对结构体访问模式明显的函数用 IDAPython 预建常用 struct。
4. **攻击面标注（优先级依据，非唯一目标）**：
   - 导出时对每个函数打攻击面标签：`calls_dangerous`（strcpy/system/popen/sprintf/execve…）、`network_facing`（socket/recv/http 解析特征）、`entrypoint`（main/init/导出符号/CGI handler）、`auth_related`（password/crypt/token 字符串引用）。
   - 标签只决定 M3 漏斗的送 AI 优先级，不影响其他函数的基础还原待遇。
5. **整体意义性兜底（非 AI、低成本规则命名，作用于全部函数）**：AI 之前先跑一遍启发式命名 pass——按字符串交叉引用（如函数内引用 `"Usage:"`/URL/设备节点）、所调 libc 组合（`socket`+`bind`+`listen` → 候选 `net_init_server`）、导出符号/段位置（`.init`/`.fini`、中断向量）、NVRAM/环境变量键名等特征给出规则名。保证即使不送 AI 的函数也尽量拿到**有语义的备选名**，`sub_XXXX` 残留率作为整体质量指标压到最低。
6. 导出格式：每二进制一个目录，每函数一个 `.c` 文件，行首注释编码元数据（`// addr=0x401000 arch=arm bin=httpd tags=calls_dangerous`），另产 `symbols.json`（全量符号映射 + 攻击面标签 + 反编译失败清单，供 M3 漏斗与回写）。
- 验收（双指标，Buildroot 交叉编译已知源码的 stripped 固件做基准对照）：
  - **重点**：危险函数调用点/攻击面函数恢复率 ≥90%（已知漏洞函数是否被识别并正确还原）；
  - **整体**：`sub_XXXX` 残留率 ≤15%（签名 + 规则 + AI 三管齐下的总有意义命名率），伪 C 可读性抽检（人工看抽样函数是否"大致还原且有意义"）；
  - 单二进制（~2MB）处理 < 10 分钟。

### M3 — AI 符号恢复与重命名 S3（5~7 天）
**设计原则一：漏斗筛选，绝不全量送 AI**（速度 + 成本双约束）。前提：M2 的规则命名 pass 已给全体函数做过一轮免费命名，AI 只处理规则和签名搞不定的。三层漏斗：
```
全部函数 ──▶ L1 跳过层：libc 已识别函数、thunk、<10 行的 trivial 函数、
             规则命名已给出高置信度名字的函数 → 不送
         ──▶ L2 必送层（攻击面优先）：带攻击面标签的函数
             （calls_dangerous / network_facing / entrypoint / auth_related）
         ──▶ L3 难恢复层（面向全部函数，不只攻击面）：满足任一启发式即送
             · Hex-Rays 反编译失败（decompile 返回空）或分析告警
             · 伪 C 中重模式占比高（CONCAT、__PAIR__、强制类型转换噪声密度超阈值）
             · 被 ≥N 个函数调用但仍无语义名（调用图枢纽）
             · 引用字符串但无语义名（字符串交叉引用有内容、函数名无内容）
             · 大体量函数（行数超阈值）且规则名置信度低
其余函数带着签名/规则命名结果直接进 S4，保证整体都有意义。
```
预期送 AI 比例 **10%~25%**，函数级断点续跑（结果落 SQLite，重跑只补未完成的）。

**设计原则二：统一命名规范约束（解决多并发合并后命名不统一）**：
1. **全局规范文件 `config/naming_spec.yaml`**，作为唯一事实来源，内容示例：
   - 格式：`<域>_<动作>_<对象>`，全小写蛇形，如 `http_parse_header`、`nvram_get_value`、`auth_check_token`；
   - 域词表（受控词汇，封闭式枚举）：`http/nvram/auth/net/crypto/fw/upnp/cgi/...`，禁止自造新域；
   - 禁用：`handle_thing`、`do_stuff`、`sub_*` 残留、拼音、大写驼峰；
   - 长度 ≤40 字符；同二进制内唯一；与已有 libc 名冲突时加 `_impl` 后缀。
2. **规范进 prompt**：每次请求的 system prompt 注入完整 naming_spec + **当前已占用符号表快照**（保证并发任务之间不重名、风格一致）。
3. **输出强约束**：要求模型返回严格 JSON `{name, confidence, reason}`，服务端用 JSON Schema 校验 + 规范校验器（正则/词表/长度/冲突）双检查，不合规自动重试一次再丢弃。
4. **合并仲裁**：所有 AI 建议先写 `name_registry`（SQLite，唯一约束 + 来源 + 置信度）；
   冲突处理规则——低置信度让高置信度；同分让先入库；批量任务结束后跑一遍**全局一致性检查**（近似名聚类，如 `http_parse_header` vs `http_header_parse` 取多数派并统一改写）。
5. **回写闭环（IDB 二遍反编译）**：仲裁后的命名用 `idat -A -S/apply_renames.py <idb>` 写回 IDB（`idc.set_name(ea, name)`，注意 IDA 合法字符），再跑导出脚本**第二遍反编译**——命名变好连带提升调用点伪 C 质量，且二遍导出用同一符号表，天然一致。IDB 全程保留在 `data/` 下，回写不丢分析成果。

**DeepSeek 并发客户端**：asyncio + httpx，信号量限流（起始 16 并发，按 SiliconFlow 限流响应自适应），指数退避重试；记录每函数 token 用量与成本。
- 验收：基准集上 AI 重命名准确率人工抽检 ≥80%；命名规范符合率 100%（校验器兜底）；含 AI 环节的整体流水线时长相对"全量送 AI"下降 ≥60%。

### M4 — 图谱构建与检索 S4（4~5 天）
- 伪 C 文件树 → `codebase-memory-mcp cli index_repository --repo-path ... --name <固件名> --mode full|fast`；大任务用 FAST 保留结构关系并跳过相似性/语义边。
- 元数据注入：索引后直接对 SQLite `UPDATE nodes SET properties=json_set(...)` 写入 addr/arch/binary/attack_surface_tags/ai_confidence（幂等脚本随流水线执行，重索引后重放）。
- 给上游漏洞挖掘 AI 的接口：
  - 首选 MCP stdio（15 个工具现成可用），或 FastAPI 包装一层 `POST /graph/query`（内部 fork `cbm cli`，上游 AI 走 HTTP 更方便）。
  - 预置攻击面查询模板：危险函数调用点、网络可达→危险函数的 `trace_path` 污点链、带 `calls_dangerous` 标签且低 AI 置信度函数列表（人工复核队列）、跨固件 `SIMILAR_TO` 找复用代码。
- 验收：对一个固件全量入库，`search_graph`/`query_graph`/`get_code_snippet` 全部可用；上游 AI 通过 MCP 能完成"找 system() 调用链"任务。

### M5 — Web 前端与对外访问（5~6 天）
- 前端（Vue3 或 React + Vite）：固件上传/任务进度（WebSocket）/函数列表（可按攻击面标签过滤）/伪 C 查看器（monaco-editor）/图谱可视化（先嵌 CBM UI iframe，不够用再上 force-graph 自研）。
- 对外访问：FastAPI 与前端 build 产物均绑 `0.0.0.0`；VMware NAT 下物理机直接访问 `http://192.168.141.135:8000`。
- CBM UI（端口 9749，只绑 127.0.0.1 且校验 Host 头）通过 nginx 反代 + `proxy_set_header Host 127.0.0.1:9749;` 对外提供。
- 简单鉴权（token/basic auth），因为这是对内网开放的服务。
- 验收：物理机浏览器完成"上传固件→看进度→按攻击面过滤函数→看伪 C→看图谱"全流程。

### M6 — 联调、压测与交付（3~4 天）
- 端到端回归（3 个真实固件 + 基准集）、并发压测（DeepSeek 并发与 idat worker 数调优）、失败恢复（任务断点续跑）。
- 文档：部署文档、API 文档、给上游 AI 的 MCP 接入说明、naming_spec 维护说明。

### M7 — 动态覆盖率追踪（3~5 天，**单二进制 qemu-user，不搞整系统仿真**）
目标：上游 AI 发一个包，能查到"这个包实际经过了哪些函数、走的什么路径"。
1. **目标选择**：从 manifest 选网络面二进制（network_facing 标签/监听端口特征），解析其在 rootfs 中的真实路径。
2. **qemu-user 运行**：`qemu-<arch>-static` 拷入 rootfs + chroot 运行目标（nvram 缺失先 best-effort，失败如实记录）；**覆盖率用 `-d exec -D log`**（qemu-user 原生支持，无需插件/调试版），记录执行过的 TB 地址。
3. **基线差分**：同一二进制跑两次——基线（启动后无请求直接 kill）vs 触发（启动→发一个包→kill）；覆盖率差集 = 该包的处理路径。发包用 curl/nc 打 localhost（qemu-user 网络直通宿主机）。
4. **地址→函数映射**：用 S2 导出的函数边界（addr+size）把执行地址集落成有序函数序列（按首次执行时间排序），产出 `trace.json`。
5. **入库与查询**：优先对接 CBM 原生 `ingest_traces`（先探查其输入格式）；不兼容则把 trace 落为 properties/自定义文件 + FastAPI 查询端点（op=trace_flow 返回有序函数名序列，与静态 CALLS 图可交叉验证）。
6. **自洽演示**：busybox 自带 httpd applet——`qemu-mips` chroot 跑 `busybox httpd`，curl 打一个请求，验证 trace 里出现 httpd 请求处理路径上的函数。
- 验收：busybox httpd 双架构（mips/arm）各产生一条有效 trace；trace_flow 查询返回非空有序路径；失败场景（起不来的二进制）有明确错误记录。

---

## 3. 风险与对策

| 风险 | 对策 |
|---|---|
| VM 内存 7.2G 偏小，多个 idat 并发 + EMBA docker OOM | M0 先扩内存；idat worker 起始限 3 个，单二进制超大时串行降级 |
| IDA 无头模式坑（TVision 报错、分析不退出、弹窗挂起） | 一律 `TVHEADLESS=1 idat -A`；脚本内 `auto_wait()` + 显式 `qexit()`；worker 加超时 kill；M0 先用真实 ARM/MIPS 样例冒烟 |
| DeepSeek 并发任务合并后命名风格漂移、重名 | naming_spec.yaml 单一事实来源 + prompt 注入已占用符号表 + JSON Schema/规范双校验 + name_registry 唯一约束 + 全局一致性检查（M3 设计原则二） |
| AI 处理慢、成本高 | 三层漏斗只送 10%~25% 函数；断点续跑；token 用量记账（M3 设计原则一） |
| SiliconFlow 限流/额度 | 并发自适应 + 指数退避；配置化可切换同端点其他模型（如 `deepseek-ai/DeepSeek-V3.2`） |
| EMBA 需特权 docker、镜像大 | 只用提取 profile；备选方案 B（镜像内直调 unblob）/C（pip unblob）已调研 |
| CBM 无元数据字段、UI 不对外 | 路径/注释编码 + SQLite json_set 幂等注入；nginx 反代改 Host 头 |
| 冷门架构 Hex-Rays 无对应反编译器（如部分 DSP） | 主流 MIPS/ARM/PPC/x86 先行；不支持的记录 manifest 标记降级（可退回仅反汇编导出） |
| 伪 C 文件被清理导致 get_code_snippet 失效 | `data/` 目录持久化（含 IDB）+ 备份策略，写进运维文档 |

## 4. 近期行动项（本周）
1. M0 环境：扩 VM 内存、装 EMBA 镜像/CBM/Python venv，写 `setup.sh`，`.env` 落 LLM 配置；用真实 ARM/MIPS ELF 冒烟 `TVHEADLESS=1 idat -A -B`。
2. 下载 2~3 个公开固件做 M1 联调样本。
3. 用 Buildroot 编一个小型基准固件（带源码对照），供 M2 双指标度量。
4. 起草 `config/naming_spec.yaml` 初版（域词表 + 命名规则），M3 全程依赖它。
