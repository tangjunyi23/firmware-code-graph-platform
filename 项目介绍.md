# 固件代码图谱系统（fwgraph）— 项目介绍与交接文档

> 版本：v1.5（M0~M7、AS-1/AS-2/AS-3 和前端已完成；AILIFT 降为可选兼容能力；M8 真实固件试炼进行中）
> 编制日期：2026-08-02　适用对象：接手开发的下一个 AI / 工程师
> 运维速查：`fwgraph/README.md`；FLIRT 专题：`fwgraph/docs/m2b-notes.md`；本文件历史版在 git 之外无备份，以本版为准。

---

## 1. 项目概述

### 1.1 要解决什么问题
固件（路由器/IoT/工控/防火墙）大多是 **stripped 二进制**，逆向后函数全是 `sub_40ABCC` 类无名符号：
- 上游分析时"谁调用了 strcpy/system"无从查起——函数身份断链；
- 想看"一个网络包进来后经过哪些函数"，静态图只有全可能路径，没有实际执行路径；
- 反编译产物分散难检索，无法被上游 AI 直接消费。

### 1.2 系统做了什么
全自动流水线，把固件变成**尽量忠实、可追溯、可检索的函数级代码图谱与攻击面证据**，供上游分析 AI 使用。fwgraph 的职责是还原反编译代码、组织调用关系、标注候选 source/sink、压缩检索上下文并提供动态覆盖证据；它不替上游 AI 判定漏洞，也不把路径评分当作漏洞结论：

```
固件.bin ─▶ S1 解包      EMBA docker（unblob/binwalk/厂商解密）→ rootfs + 架构清单
        ─▶ S2 反编译     IDA Pro 9.1 无头（FLIRT 签名 + 规则命名）→ 伪 C + symbols.json
        ─▶ S3 可选标签   DeepSeek-V4-Flash → domain/libc_equiv（兼容能力，不是建图前置）
        ─▶ S3.5 可选等价 历史标签回填/传播 → "sub_XXXX 等价于 strcpy"
        ─▶ S4 图谱       伪 C 树 → codebase-memory-mcp → SQLite 图（节点/边/向量/元数据）
        ─▶ S5 动态追踪   qemu-user 覆盖率差分 → "这个包实际经过了哪些函数"
        ─▶ AS 攻击面     source/sink Top50 + trace 交叉验证 + URL→handler ROUTE 边
                              └─▶ 上游分析 AI（HTTP API / CBM MCP stdio）
```

### 1.3 关键指标（全部真实实测）
| 指标 | 数值 | 场景 |
|---|---|---|
| 反编译成功率 | 99.8%（6622/6634） | busybox mips32be + arm32le |
| AI 命名恢复 | +447（接受率 96%，合规率 100%） | 同上，2.9M token |
| libc 等价恢复 | 回填 41 + SIMILAR_TO 传播 83 | 零额外 token |
| 危险调用点查询（strcpy） | 0 → 95 | libc 等价层前后 |
| 动态追踪差分函数 | 58（mips）/ 62（arm） | busybox httpd 一个 GET |
| 图谱规模 | 19976 节点 / 50317 边 | 双 busybox |
| AS-2 动态验证 | 120 个 trace 函数 / 15 个验证函数 / 13 条完整验证路径 | `a27edd2f9562`，2 条有效 trace |
| AS-3 路由机制 | 阳性 fixture 2/2；busybox 0 条 | `/api/status`、`/goform/reboot` 精确命中 handler |
| AILIFT 标签回归 | 40/40 done，0 改名，0 请求错误 | `6ebb9777d437`，20 函数/二进制成本上限 |
| 小米 R3 真实图谱 | 337245 节点 / 1253251 边；111148 文件，0 skipped | `56c105fd7b9a`，CBM FAST 结构索引 |
| 端到端回归 | 15/15 PASS | `scripts/e2e_regression.sh`，最终状态 `routed` |
| 单元测试 | 217 passed | FAST 大图索引与 trace 错误保真修复后的最近一次本地/远端全量结果；M0~M7 历史基线为 193/193 |

### 1.4 当前状态与最终验收（接手必读）
截至 2026-08-02，M0~M7、AS-1/AS-2/AS-3 和前端目标已经全部实现并部署到 `192.168.141.135:8000`；M8 真实固件试炼仍在进行：

1. **AILIFT 是可选兼容能力，默认任务不运行**：接口和历史标签保留；若人工触发，LLM 也只能返回 `domain/libc_equiv/confidence/reason`，`new_name=NULL`，不回写 IDB、不二遍反编译。缺少这些标签不影响伪 C、结构图谱、攻击面或 trace 生成。
2. **AS-1 已正式落盘**：两组基线分别为 `a27edd2f9562` 的 40 source / 162 sink / 2718 candidate / Top50，以及 `65de42864035` 的 15 source / 24 sink / 140 candidate / Top50。CBM 注入均为 wanted=6623、matched=6615；缺少的 8 个是 parser 未建 Function node，不是注入遗漏。
3. **AS-2 已交叉验证并部署**：`a27edd2f9562` 有 2 条有效 trace、120 个 observed 函数、15 个 verified 函数、13 条同一 trace 覆盖完整链的 verified path；`65de42864035` 有 3 条有效 trace、120 个 observed 函数、10 个 verified 函数、0 条完整验证路径。失败/损坏 trace 会被跳过，二进制身份严格隔离。
4. **AS-3 已完成**：只接受非代码初始化段中的 `{URL 字符串指针, 函数入口指针}`。真实 IDA 9.1 阳性 fixture 精确恢复 `/api/status → status_handler` 与 `/goform/reboot → reboot_handler`；两个 busybox 基线均为 0 条，作为保守负例保留。
5. **前端已交付**：四页签包含任务、函数、攻击面、图谱；攻击面支持 source/sink、仅动态验证、路径展开和路由空态。浏览器已完成桌面及 390×844 移动端检查，并修复窄屏抽屉溢出及首次“任务→函数”懒加载竞态。
6. **M0~M8 当前验证**：历史 e2e `15/15`，Vite 8.2.0 构建成功；最新本地/远端全量均为 `217 passed`。PX4 raw IDA/图谱/攻击面已完成；小米 IDA/图谱/攻击面/routes 与真实动态链接 trace 已完成，最终 e2e/浏览器复验尚未完成。

---

## 2. 运行环境

### 2.1 硬件与系统
- VM：VMware，Ubuntu 26.04，`192.168.141.135`，用户 `tankuku`（凭据仅保存在受控环境中，不写入文档）
- 8 核 / 7.2G 内存 / 73G 可用磁盘。**内存是瓶颈**，建议扩 16G
- 物理机直连 `http://192.168.141.135:8000`（VMware NAT）；ufw inactive

### 2.2 核心依赖
| 组件 | 版本/位置 | 备注 |
|---|---|---|
| IDA Pro 9.1 | `/home/tankuku/ida-pro-9.1/` | **无头必须 `TVHEADLESS=1`**；IDAPython=系统 Python 3.14 |
| EMBA 2.0.3a | `~/firmware-graph/emba` + docker 镜像 | 只用 P 提取阶段，66 个 S/L/F/Q 模块黑名单 |
| codebase-memory-mcp 0.9.0 | `~/.local/bin/` | 图谱引擎，纯 C 单二进制 + SQLite |
| DeepSeek API | SiliconFlow，`deepseek-ai/DeepSeek-V4-Flash` | key 在 `fwgraph/.env`（裸写 `deepseek-v4-flash` 会报模型不存在，必须带 `deepseek-ai/` 前缀） |
| FLAIR | IDA 自带 `tools/flair/` | FLIRT 签名制作 |
| qemu-user | `qemu-user-binfmt` 包 | 26.04 没有 `qemu-user-static` 包名，二进制叫 `qemu-mips` 等 |
| Python venv | `fwgraph/.venv`（3.14） | **与 IDA 的系统 Python 分离，不要混用** |

---

## 3. 架构与关键决策

```
物理机浏览器/上游AI → http://192.168.141.135:8000
   orchestrator (FastAPI)：SPA 前端 + REST API + /cbmui 反代 + 后台流水线线程
   外部进程：EMBA docker（特权）/ idat ×3 / qemu chroot
   存储：fwgraph/data/（全部产物）+ ~/.cache/codebase-memory-mcp/*.db（图库）
```

重要决策记录（为什么这么选）：
- **IDA 而非 Ghidra**：Hex-Rays 质量更高，自带 FLIRT/TIL，用户已有 license
- **EMBA 只取提取阶段**：厂商加密固件解密器是其核心价值；`-F` 跳过宿主依赖检查
- **CBM 而非自建图库**：零依赖、C 语言一等公民、MCP stdio 正好给上游 AI
- **AI 漏斗只送 ~5% 函数**：全量送 AI 慢且贵；签名/规则/AI 三层各管一段
- **命名规范统一约束**：`config/naming_spec.yaml` 唯一事实来源（标签化后其 domains 词表继续用作 domain 标签词表）

---

## 4. 子系统详解

### 4.1 S1 解包（`pipeline/extract/` + `orchestrator/app/extractor.py`）
上传 → `sudo ./emba -l data/extracted/<job> -f <固件> -p scan-profiles/extract-only.emba -F` → 解析 `csv_logs/p99_prepare_analyzer.csv` → `manifest.json`（path/arch/bits/endianness/md5）。EMBA 容器名固定，全局锁串行。

### 4.2 S2 反编译（`pipeline/decompile/` + `orchestrator/app/decompiler.py`）
- `ida_export.py`：全函数伪 C → `functions/<addr>.c`（首行 `// addr= name= arch= size=`）+ `symbols_raw.json`（calls/strings/is_exported/反编译错误）+ `export_done.json`
- 编排：idat 并发=3，超时 30 分钟；IDB 落 `data/idb/<job>/<md5>.i64` 可复用（省 40%）
- `annotate.py`：攻击面四标签（calls_dangerous/network_facing/auth_related/entrypoint）+ 规则命名（4 规则带置信度）
- **无头三铁律**：`TVHEADLESS=1`；stdout 不可靠结果写文件；结尾 `qexit(code)`

### 4.3 FLIRT 签名资产（`libc-sigs/` + `docs/m2b-notes.md`）
- **Lumina 不可用**（license 被服务器拒绝；预留 `LUMINA_ENABLED=1`）
- 机制跑通，已产 5 个 sig（Bootlin×2 + OpenWrt 12.09×3）
- **核心发现：FLIRT 命中要求"年代×ISA×字节序×配置"四元匹配**，老固件收益仅 +2~+6；命名恢复主力是 AI（+447）
- 扩矩阵路线（OpenWrt 历代 SDK/Buildroot 历史链/厂商 SDK）见 m2b-notes §4

### 4.4 S3 AI 语义标签（`pipeline/ailift/`，`AILIFT_MODE=tag`）
漏斗（L1 跳过/L2 攻击面必送/L3 难恢复，封顶 `AI_MAX_FUNCS_PER_BIN=150`）→ DeepSeek 并发（16，tenacity 退避）→ `naming_spec.yaml` domain 封闭词表校验 → SQLite registry 断点续跑 → 原子更新 `symbols.json` 的 `domain/libc_equiv/ai_confidence/ai_reason`。
新模式不生成 `renames.json`、不调用 IDA apply、不做二遍导出，`new_name` 恒为 NULL；null 标签是合法回答，成功行直接为 `done`。最终 e2e 发送 40、完成 40、请求错误 0、改名 0。历史 +447 命名数据仅作为旧 job 兼容信息保留。

### 4.5 libc 等价层（`pipeline/ailift/backfill.py` 等）
三路：AI 直答 `libc_equiv` 字段；246 符号表零成本回填（util_strcpy→strcpy）；SIMILAR_TO 克隆簇**权威 donor 一跳传播**（无限制会逐轮外溢，已修）。效果：dangerous strcpy 查询 0→95。

### 4.6 S4 图谱（`pipeline/graph/`）
- ingest：伪 C 树（`<bin>_<md5>/<addr>_<name>.c`）→ git init → 小任务 CBM FULL / 大任务 FAST 结构索引 → SQLite `json_set` 注入 addr/tags/domain/ai_confidence/libc_equiv/trace/attack 字段（幂等）
- **伪 C 净化**：IDA 风格语法 tree-sitter 失败率 5.3%，只处理 `*__fastcall`、`__asm` 块、`_$V0` 等解析问题；标签模式不再改写函数定义和原始函数名
- **CBM cypher 陷阱**：缺失键数值比较按 0（必须 `> 0 AND < 0.7`）；缺失字符串键 `IN` 不匹配不报错；`=` 遇缺失键报错

### 4.7 S5 动态覆盖率追踪（`pipeline/trace/`，M7）
qemu-user + chroot + `-d exec` 覆盖率 + **基线差分**（baseline 驻留 vs trigger 发包）→ 地址→函数映射 → `data/traces/<job>/<id>/trace.json`。坑已内置处理：rootfs 挂 /proc、补执行位、argv[0] 用 `-0` 伪造、baseline 不可 TCP 探活。
- CBM `ingest_traces` 在 0.9.0 是占位桩，权威存储是自己的 trace.json，查询走 `trace_flow`
- 实测：busybox httpd 双架构差分 58/62 函数，GET 路径真实命中 util_strcpy
- 已知限制：动态链接 trace 已自动解析 loader/依赖并添加 `qemu -L /`；真实小米程序的 qemu 执行验收仍待 M8 完成；差分按路径超集理解

### 4.8 AS 攻击面与静态路由（`pipeline/attack/` + `pipeline/routes/`）
- AS-1：`attack_surface.yaml` 配置 source/sink、工控协议/串口/固件升级词表、无名函数高置信启发式与评分；单二进制调用图搜索 ≤8 跳并输出 Top50。
- AS-2：读取权威 trace.json，把 `observed_in_trace/verified_reachable/trace_ids` 写回 symbols、CBM 与 API；只有同一个成功 trace 覆盖整条链才验证路径，失败/损坏 trace 不参与。
- AS-3：IDA 只读扫描非代码初始化段中的 `{URL ptr, function-entry ptr}`，建立受 `producer=fwgraph.routes.v1` 管理的 `Route -[ROUTE]-> Function`；重跑幂等且不碰外部 Route 节点。

### 4.9 编排服务与前端
API（除 `/healthz` 全部 `Authorization: Bearer $ORCH_TOKEN`）：

| 端点 | 说明 |
|---|---|
| `POST /firmware` | 上传（自动接力提取+反编译） |
| `GET /jobs` `/jobs/{id}` `/jobs/{id}/manifest` | 任务 |
| `POST /jobs/{id}/decompile` `/ailift` `/graph` `/attack` `/routes` `/trace` | 各阶段触发 |
| `GET /jobs/{id}/ailift` `/graph` `/attack` `/routes` `/traces` `/traces/{tid}` `/functions` `/functions/{md5}/{addr}/source` | 产物 |
| `POST /graph/query` | search/cypher/trace/snippet/dangerous/trace_flow/attack_surface/routes |
| `GET /cbmui/*` | CBM UI 反代（Host 头重写） |

状态机：`pending→extracting→done→decompiling→decompiled→ailifting→ailifted→graphing→attacking→routing→routed`（trace 独立）。前端 Vue3 四页签，8000 单端口；任务详情、函数源码抽屉已适配窄屏。

---

## 5. 目录结构

```
Windows 母版 C:/Users/22522/Desktop/图谱/     Ubuntu VM ~/firmware-graph/
├── 开发计划.md（历史版本，以本文档为准）        ├── codebase-memory-mcp/
├── 项目介绍.md（本文档）                       ├── emba/
├── tools/                                     ├── toolchains/  # Bootlin×4+OpenWrt SDK×2
│   ├── remote_exec.py  # SSH 执行远程命令      ├── samples/     # busybox×2
│   └── sftp_upload.py  # SFTP 上传            └── fwgraph/      # ★ 项目本体（与母版一致）
└── fwgraph/                                       ├── orchestrator/app/  # main/extractor/decompiler/webui
    ├── orchestrator/                              ├── pipeline/{extract,decompile,ailift,graph,trace,attack,routes}/
    ├── pipeline/                                  ├── config/naming_spec.yaml
    ├── config/                                    ├── libc-sigs/
    ├── libc-sigs/                                 ├── webui/
    ├── webui/                                     ├── scripts/  # run_*.sh / e2e_regression.sh
    ├── scripts/                                   ├── docs/     # m2b-notes / e2e-report / 项目介绍
    ├── docs/                                      ├── .env      # ★ 全部配置（含 LLM key、sudo 密码）
    ├── README.md                                  └── data/     # ★ 必须持久保留
    ├── setup.sh                                       ├── firmware/<job>/   ├── extracted/<job>/
    └── requirements.txt                               ├── idb/<job>/        ├── pseudocode/<job>/
                                                       ├── cbm/<job>/        ├── traces/<job>/
                                                       ├── attack/<job>/     └── routes/<job>/
```

**数据铁律**：`data/` 不可清理——CBM `get_code_snippet` 查询时从磁盘读伪 C；IDB 复用省 40% 时间。

---

## 6. 运维手册

```bash
# 启动
bash ~/firmware-graph/fwgraph/scripts/run_orchestrator.sh   # :8000
bash ~/firmware-graph/fwgraph/scripts/run_cbm_ui.sh         # CBM UI :9749
# 停止
kill $(cat ~/firmware-graph/fwgraph/data/orchestrator.pid)
kill $(cat ~/firmware-graph/fwgraph/data/cbm_ui.pid)
# 健康检查
curl http://192.168.141.135:8000/healthz
# 测试
cd ~/firmware-graph/fwgraph && .venv/bin/python -m pytest orchestrator/tests -q   # 当前 210 项
bash scripts/e2e_regression.sh                                                   # 15 项；会调用真实 LLM
```

| 常见故障 | 处理 |
|---|---|
| EMBA exit 1 | 确认 `-F`；`docker rm -f emba emba_quest` 清残留 |
| idat 无输出/挂起 | `TVHEADLESS=1`；脚本须 `qexit()`；worker 超时 killpg |
| chroot qemu 静默退出 | rootfs 未挂 /proc 或无执行位（prepare_rootfs 幂等处理） |
| CBM 查不到片段 | 伪 C 被删，恢复 data/ 重跑 ingest |
| 元数据查询异常多 | cypher 缺失键按 0，补 `> 0 AND` 守卫 |
| 中文文件名上传乱码 | 用 ASCII 临时名上传，再 `python3 os.rename` 改中文 |

安全注意：服务无 TLS 仅 token，**内网工具勿暴露公网**；`ORCH_TOKEN` 必须从 `fwgraph/.env` 加载，禁止在文档或脚本中写入明文；交接后应轮换并重启服务。

---

## 7. 已知限制（如实清单）
1. Lumina 不可用（license），预留开关
2. FLIRT 四元敏感，老固件收益低，扩矩阵见 m2b-notes §4
3. 动态链接 trace 已自动解析 `PT_INTERP`、`DT_NEEDED`、RPATH/RUNPATH，并为动态 ELF 添加 `qemu -L /`；小米真实程序的 qemu 执行结果仍待 M8 完成
4. 差分纯度受时序影响，按路径超集理解
5. SIMILAR_TO 小函数克隆可能误判，传播仅一跳已收敛
6. CBM ingest_traces 是桩（0.9.0）
7. VM 内存 7.2G，idat 并发 3 据此定
8. AS-3 当前只覆盖相邻二指针路由表；多字段、哈希、压缩或运行时注册的厂商格式需要新增适配器，不能通过放宽规则制造命中
9. M8 尚未完成真实厂商/工控固件的完整试炼；EMBA 对 VxWorks/RTOS 与需要基址加载的原始 blob 支持仍有限

---

## 8. 最终开发状态与后续边界

> 用户目标原话：①"最大程度展现攻击面用来提供给上游 AI"；②"后续设备还可能涉及工控设备、防火墙设备，也要进行兼容"。这里的“展示攻击面”是证据组织和上下文压缩目标，不是自动完成漏洞挖掘。
> 这两项目标对应的代码、数据、API、前端与验证已经完成。后续工作是扩大真实固件覆盖面，不是继续完成 AS-1/2/3。

### 8.1 AILIFT 可选兼容能力（接口保留，默认不运行）
**原则：建图不依赖 AI 标签；只有人工显式触发时才打标签，且不改函数真名、不回写 IDB、不二遍反编译。**
1. LLM 严格返回 `libc_equiv/domain/confidence/reason`，其中两个标签允许 null；domain 必须属于 `naming_spec.yaml`。
2. Registry 已迁移 `domain` 列；新行保留 `old_name`，`new_name=NULL`，旧命名行保持可读。
3. Runner 只做漏斗、LLM、校验、backfill 与原子 symbols 更新；无 `renames.json`、IDA apply 或二遍导出。
4. `GET /jobs/{id}/ailift` 返回 `tag_samples`；graph/functions/前端全链路消费 domain/libc 标签。
5. e2e 实证 40/40 tagged、40 done、40 请求、0 错误、0 新名字。
6. 小米、PX4 与后续真实任务默认跳过 AILIFT；历史标签不删除，缺失标签是合法且完整的任务状态。

### 8.2 攻击面显式化（AS-1/AS-2/AS-3 已完成）
**原则：攻击面作为一等公民进图谱；载体是标签与边，函数真名不动；所有逻辑对 sub_ 无名函数同样有效（地址/调用关系/伪 C 文本是主数据，名字只是辅助）；词表配置化以兼容工控/防火墙。**

**AS-1 源/汇标签 + 攻击路径预计算 + attack_surface 查询：已正式落盘并部署**
1. `config/attack_surface.yaml`（新，分类法配置化）：
   - sources：`network`（recv/accept/socket 名或 network_facing tag）、`fieldbus_ics`（**工控**：modbus/dnp3/s7comm/bacnet/opcua/iec104/profinet/ethercat 字符串，端口 502/102/20000/44818/47808/2404）、`serial`（**工控串口**：/dev/ttyS、ttyUSB、termios）、`env`、`nvram`、`file`、`argv`
   - sinks：`cmdexec`（system/popen/execve）、`memunsafe`（strcpy/sprintf/gets/memcpy 名或 libc_equiv）、`fmt`（printf 系非常量格式串）、`filewrite`、`fwupdate`（/dev/mtd、ubi、fw_write——**工控/防火墙升级面**）
   - **无名模式识别（stripped 主力）**：直接在伪 C 文本做启发式，如"循环逐字节赋值且终止条件含 \0"→`memunsafe_like`；只做置信度高的 3~5 条
   - scoring：源权重（network/fieldbus 3 > serial/nvram 2 > env 1.5 > file/argv 1）、汇权重（cmdexec 3 > memunsafe 2.5 > fmt/fwupdate 2 > filewrite 1.5）、长度惩罚、途经 strlen/strncmp 节点降权
2. `pipeline/attack/` 新包：`surface.py`（打 asrc/asink 标签）、`paths.py`（intra-binary 调用图 BFS 前向≤8∩后向，去环去重排序，输出 `attack_paths.json`：summary + Top50 路径含完整函数链与评分）、`inject.py`（注入 CBM properties：`on_attack_path/path_ids/asrc/asink`，幂等）
3. 编排：`POST /jobs/{id}/attack` + graph 完成后自动接力（`AUTO_ATTACK=1`）；`/graph/query` 新 op `attack_surface` → 排序路径清单（名字优先 ai_name 附 addr）
4. 已完成验证：
   - M0~M7 最终 VM 全量测试 **193/193 PASS**；M8 FAST 大图与 trace 错误保真修复后的本地/远端全量均为 217 passed（仅既有 FastAPI/Starlette 弃用警告）
   - `a27edd2f9562` 正式结果：source=40（argv 2/file 25/network 13），sink=162（filewrite 26/memunsafe 117/memunsafe_like 24），candidate=2718，Top50
   - `65de42864035` 正式结果：source=15（argv 2/network 13），sink=24（memunsafe_like 24），candidate=140，Top50
   - 这两组数字允许分类重叠，因此各分类之和可能大于唯一函数数
   - 两组 CBM 注入均为 wanted=6623、matched=6615；8 个缺口没有 parser Function node。busybox 数字如实保留，**不得为好看放宽规则**
5. 已修复的重要问题：
   - 名称归一化不能把 `sub_1000/sub_2000` 去尾变成同一个 `sub`；工具地址名必须完整保留，否则会产生假调用边
   - `fmt` 不能仅凭调用 printf 系函数命中，必须确认格式参数不是字符串字面量
   - YAML 正则使用块标量/正确引号，避免 `\s` 被 YAML 当成非法转义

**AS-2 动静交叉验证：已测试、落盘并部署**
- 新增 `pipeline/attack/cross.py`：读取权威 `data/traces/<job>/<trace>/trace.json`；失败/损坏 trace 跳过，只消费 `status=ok*`
- 设计语义：trace 命中函数标 `observed_in_trace`；同时位于静态攻击路径且被 trace 命中的函数标 `verified_reachable`；只有同一个 trace 覆盖路径完整函数链时，整条路径才标 `verified_reachable=true`
- `observed_in_trace/verified_reachable/trace_ids` 已接入 symbols、CBM、functions API、attack_surface 与前端；trace 成功后会刷新已有攻击面产物
- 基线结果：`a27edd2f9562` 为 2 trace / 120 observed / 15 verified / 13 完整路径；`65de42864035` 为 3 / 120 / 10 / 0
- 落盘前备份位于 `data/backups/as2-20260802-prewrite/`；symbols 与 DB 有 SHA256，两套 DB 均通过 `PRAGMA integrity_check`

**AS-3 Web 路由→处理函数 ROUTE 边：已完成并部署**
- `ida_routes.py` 只读扫描非代码初始化段中的 `{URL string ptr, function-entry ptr}`，建立受 `producer=fwgraph.routes.v1` 管理的 Route 节点与 ROUTE 边；反向指针误报已排除，注入重跑幂等
- `POST/GET /jobs/{id}/routes`、`/graph/query op=routes` 与 `AUTO_ROUTES=1` 已上线
- IDA 9.1 阳性 fixture 2/2：`/api/status → status_handler`、`/goform/reboot → reboot_handler`；busybox 基线与最终 e2e 均为 0 routes，作为有效保守负例保留
- 写入前 DB 备份位于 `data/backups/as3-20260802-prewrite/`，两套 DB 均通过完整性检查

### 8.3 工控/防火墙兼容性备忘（贯穿 AS 工作）
- 词表已含工控协议/串口/升级面；qemu 映射按需补 `qemu-ppc/qemu-sh4`（`pipeline/trace/qemu_cov.py` 的映射表，13 种已有）
- IDA 导出本身架构无关；EMBA 对 VxWorks/RTOS 类固件提取能力有限，遇到时按 manifest 降级处理
- 原始 blob 已支持 PX4 v6xrt 的 manifest 驱动 ARMv7-M loader；其他 RTOS/SoC 仍需新增可验证的 board profile 与 loader 适配器

### 8.4 M8 真实样本试炼（当前进行中）
- 小米 R3 路由器：任务 `56c105fd7b9a` 最终状态 `routed`。274/274 个 MIPS ELF 完成 IDA 导出，0 失败/0 超时，共 132,618 functions、111,148 decompiled。
- 小米图谱：111,148 个伪 C 全部进入 CBM FAST 结构索引，337,245 nodes / 1,253,251 edges / skipped_count=0；Function 节点 100,762，元数据 matched 100,762。数据库 `integrity_check=ok`，保留 CALLS 228,637、USAGE 488,996、WRITES 198,157；不生成 SIMILAR_TO/SEMANTICALLY_RELATED。
- 小米攻击面：source 1,585、sink 6,875、candidate 上限 5,000、Top50；无 trace，因此 observed/verified/完整链均为 0。274/274 IDB routes 扫描成功、route 0，作为保守负结果保留。
- PX4 v6xrt：正式任务 `aaae7834a0cd` 已完成 raw IDA、图谱、攻击面和 routes 验收，最终状态 `routed`
- 小米动态链接 trace：`sysapihttpd` 真实启动参数来自 init 脚本，为 `-c /tmp/sysapihttpdconf/sysapihttpd.conf`；`uhttpd` 在 `/etc/config/misc` 中明确关闭。trace `1a70349a5a08` 使用 MIPS little-endian qemu、`/lib/ld-uClibc.so.0` 与 13 个已解析直接依赖，对真实监听端口 8098 发送 `GET /`。
- trace 结果为 `ok_trigger_error`：服务接受连接并进入请求路径，但在完整 HTTP 响应前断开，错误 `RemoteDisconnected` 被保存在 `trigger_result`，不能解释为 HTTP 成功。baseline 418 functions、trigger 440、diff 22；请求差分序列含 `accept4/getsockopt/inet_addr/recv` 与原始 `sub_*`。
- trace 后攻击面交叉验证为 1 trace / 22 observed / 1 verified function / 1 完整链。该路径是 `sub_41D85C` 的单节点链（edge_count=0）：它因调用 accept/accept4 与 memcpy 同时成为 network source 和 memunsafe 候选 sink，只表示运行时观察到这个连接初始化函数，绝不表示存在漏洞。
- AILIFT：小米任务未运行且不再计划运行；图谱、攻击面和 routes 均已证明不依赖标签。兼容接口与历史数据继续保留。
- 环境安全：EMBA/trace sudo 密码不进入命令行；`.env.example` 的 `TRACE_SUDO_PASSWORD` 已置空。

### 8.5 其他后续（M8 完成后的次优先级）
- 选取 D-Link/TP-Link、一份工控固件和一份防火墙固件，按哈希去重后做分层试炼
- 按真实样本增加 AS-3 路由表适配器，每种格式保留阳性/阴性 fixture，继续坚持保守匹配
- 扩充 FLIRT 的年代×ISA×字节序×配置矩阵，并补充 MCP stdio 的上游 Agent 配置示例
- 对 VxWorks/RTOS、非 ELF raw blob 和需要基址加载的固件增加明确的降级 manifest 与 loader 适配器

### 8.6 接手后的执行顺序
1. 如需把 `ok_trigger_error` 推进到完整 HTTP 响应，补齐 sysapihttpd 依赖的 USB root、`RR_PATH_STUB`、FastCGI/Lua 后端等厂商运行环境；不得伪造响应或把连接接受等同于业务成功。
2. 不运行小米 AILIFT；继续以地址、原始 IDA 名称、伪 C、调用/usage、字符串、route 与 trace 作为上游 AI 的主证据。
3. 保留两个失败 trace 与 `1a70349a5a08` 的部分成功证据；后续 trace 必须继续区分传输错误、空差分和完整响应。
4. M8 真实结果完成后，再扩展 D-Link/TP-Link、工控和防火墙样本，并为每个新路由格式添加阳性/阴性 fixture。
5. 最后重跑本地/远端全量测试、e2e、构建、API 和浏览器验收，更新本文件和 M8 报告。

---

## 9. 给下一个 AI 的操作约定

1. **远程操作**：所有工作在 Ubuntu VM 进行，通过 `tools/remote_exec.py "<cmd>"`（≤280s，长任务 nohup `</dev/null >/dev/null 2>&1` 全分离）与 `MSYS_NO_PATHCONV=1 python tools/sftp_upload.py <本地> <远程路径>`；本地母版 `C:/Users/22522/Desktop/图谱/fwgraph` 与 VM 保持一致
2. **不要本地执行 git 命令**（项目未建 git 仓库；用户未要求时不要 git init/commit；VM 上 data/cbm 目录树内的 git init 是 ingest 脚本行为，允许）
3. 改代码后重启 orchestrator 生效；M8 代码改动后的最近一次本地/远端全量测试为 **217 passed**，历史 e2e 基线为 **15/15**；任何后续改动后都必须重新保持全绿
4. LLM 调用花真实额度：回归时 `AI_MAX_FUNCS_PER_BIN=20` 封顶
5. 老 job（a27edd2f9562、65de42864035）数据是验证基准，勿删
6. 中文文件名上传陷阱：ASCII 临时名 + python os.rename
7. **不要使用 planning-with-files**；本文档是当前进度和后续行动的唯一交接记录
8. 当前服务 PID `78062`，cwd、命令行、8000 listener 与 `/healthz` 已受控核验；已加载 CBM FAST 大图与 trace 错误保真代码，健康接口返回 `status=ok`

---

## 10. 最终交接检查清单
- [x] VM 服务健康，物理机可访问 `http://192.168.141.135:8000/`
- [x] AS-1/AS-2 基线正式落盘、备份并通过幂等与 DB 完整性检查
- [x] AS-3 阳性 fixture 2/2；busybox 0-route 负例保留
- [x] AILIFT 标签模式在新 e2e job 上实证 40/40 且无改名
- [x] API、桌面与 390×844 移动端前端已实测
- [x] 历史全量 pytest 193/193、e2e 15/15、Vite build 成功、npm audit 0
- [x] M8 FAST 大图与 trace 错误保真修复后的本地/远端全量测试为 217 passed
- [x] README、e2e-report 与本文档已更新
- [x] `data/`、两个基线、e2e job、IDB、伪代码、trace、CBM DB 与备份均保留
- [x] 小米 R3 `56c105fd7b9a` 完成 274/274 IDA 导出并生成 `decompile_summary.json`
- [x] orchestrator 受控重启并确认已加载 PX4、动态链接 trace 和 AILIFT job 总预算代码
- [x] PX4 v6xrt 正式 API 任务 `aaae7834a0cd` 完成 raw IDA、图谱和攻击面验收
- [x] 小米 `56c105fd7b9a` 完成 graph/attack/routes，最终 `routed`，DB 完整性通过；未运行 AILIFT
- [x] 小米 `sysapihttpd` 完成真实动态链接 qemu trace：22 个请求差分函数；HTTP 主动断开明确保留为 `ok_trigger_error`
- [ ] M8 最终全量测试、e2e、构建、API、浏览器验收和报告完成

## 11. 开发历程备忘
| 里程碑 | 结果 |
|---|---|
| M0 环境 | CBM/venv/EMBA/IDA 无头冒烟全通 |
| M1 解包 | EMBA 提取封装，集成冒烟通过 |
| M2 反编译 | 双架构 99.8%；无头三铁律 |
| M2b 签名 | Lumina 被拒；FLIRT 机制通、四元敏感成文 |
| M3 AI 恢复（历史模式） | +447 命名零违规；旧数据兼容保留 |
| M4 图谱 | 19976 节点/50317 边；伪 C 净化；元数据注入 |
| M5 前端 | 8000 单端口；CBM UI 反代 |
| M6 回归（历史检查点） | e2e 13/13 + README |
| libc 等价层 | dangerous 0→95；传播收敛修复 |
| M7 覆盖率 | qemu-user 差分；双架构实证命中 util_strcpy |
| AILIFT 可选兼容 | domain/libc 历史链路保留；新真实任务默认跳过，建图不依赖标签 |
| AS-1 | source/sink、无名启发式、Top50、CBM/API；双基线正式落盘 |
| AS-2 | trace 交叉验证；120 observed、15/10 verified、13/0 完整路径 |
| AS-3 | IDA 路由扫描、Route/ROUTE 图模型与 API；阳性 2/2、busybox 0 |
| 前端收尾 | 攻击面四页签、标签/trace 展示、桌面/移动端验收；修复抽屉和懒加载竞态 |
| 最终回归 | pytest 193/193；e2e 15/15；build 成功；npm audit 0 |
| M8 真实样本试炼 | 进行中：小米 R3 与 PX4 v6xrt 的 IDA/图谱/攻击面/routes 已完成；小米 trace 已取得 22 个真实请求差分函数，完整 HTTP 环境与工控/防火墙样本尚未完成 |

---

## 12. M8 当前进度与交接清单（2026-08-02）

### 12.1 已完成并已同步到 VM

- 新增 PX4 `PX4FWv1` 容器解析器：严格校验 JSON、Base64、zlib、尺寸和 vector；支持样本池中 12 个唯一 board profile。
- `.px4` 上传路径绕过 EMBA，生成 raw manifest；manifest 保留原始镜像哈希、MD5、board、SoC、RTOS、基址、入口和 loader provenance。
- raw ARM IDA 导出使用 `-pARM:ARMv7-M`、按 16-byte paragraph 换算的 `-b` 和 `-TBinary file`；脚本在 auto-analysis 前固定 32-bit 并以 reset entry bootstrap；`symbols.json` 保留 raw format、RTOS、board、SoC 和 loader 元数据。
- EMBA sudo 和 trace sudo 均不再把密码放入命令行或普通日志；有密码时通过 stdin，无密码时使用 `sudo -n`。
- AILIFT 新增 `AI_MAX_FUNCS_PER_JOB`，所有二进制先执行零 token 漏斗，再进行全局候选排序；保留全部 IDA 证据，只限制 LLM 调用。
- 动态链接 trace 已解析 `PT_INTERP`、`DT_NEEDED`、RPATH/RUNPATH，并验证 loader 和直接依赖位于同一 rootfs；动态 ELF 显式使用 `qemu -L /`。
- 真实小米 `uhttpd` 已完成只读依赖解析验证：interpreter 为 `/lib/ld-uClibc.so.0`，5 个直接依赖均成功解析。
- 远端全量测试最近一次结果为 **217 passed**；PX4 聚焦测试为 **10 passed**。
- PX4 v6xrt 本地解析已验证：board ID 35、MIMXRT1176、镜像基址 `0x30020000`、vector offset `0x2000`、reset entry `0x300223a8`。

### 12.1.1 PX4 v6xrt 正式验收结果（2026-08-02）

- 正式 API 任务：`aaae7834a0cd`；容器 SHA-256：`4c15ca8a966e03c9a3bb35179952907a74fe6e3094840145c68474ed81514ab6`。
- raw manifest 保留 `PX4FWv1`、board 35、`px4_fmu-v6xrt`、`MIMXRT1176`、NuttX、镜像 SHA-256/MD5、基址 `0x30020000`、vector offset `0x2000`、初始 SP 和入口 `0x300223a8`。
- IDA 正式导出：`1/1` binary 成功，`12,754` functions，`12,750` decompiled，`4` 个函数保留为真实反编译失败证据；耗时约 `694s`。伪 C 首个架构标签为 `arm32le`，不再出现错误的 `arm6464le`。
- 图谱完成：`12,750` 个伪 C 文件复制，CBM `38,540` nodes / `168,885` edges；元数据注入 wanted `12,750`、matched `12,660`，缺口是 parser 没有 Function node，不是静默丢失。
- 攻击面完成：source `6`（serial）、sink `14`（fwupdate `2`、memunsafe_like `12`）、静态 path `0`；routes `0`。这些是保守的证据结果，不因“好看”放宽规则。
- 期间发现并修复四个 raw loader 问题：IDA `-b` 使用 16-byte paragraph；raw ARM 需要 `ARMv7-M` 和 32-bit bitness；无 ELF 符号时必须以 profile 验证的 reset entry bootstrap 函数；每次重试必须删除旧 `export_done.json`，raw `0` functions 必须失败而不能算成功。旧失败任务 `d149f363d41d`、`2291391fccb1`、`60690a364678` 原样保留作回归证据。

### 12.2 当前运行中的任务和资源状态

- 小米 R3 job：`56c105fd7b9a`；最终状态 `routed`，274/274 IDA、graph、attack、routes 均完成，当前无 IDA/CBM worker。
- EMBA 已成功提取约 3382 个文件，其中 274 个 MIPS ELF；128 个 library-like，146 个非库程序。
- VM 根盘约 98 GB，当前剩余约 16 GB（84% 使用）；现有小米 pseudocode/IDB/CBM DB 不得擅自清理。
- 当前 orchestrator PID 为 `78062`，已加载 CBM FAST 大图与 trace 错误保真代码并完成小米正式验收。
- 本地临时副本 `.m8-inputs/miwifi_r3_all_55ac7_2.11.20.bin` 是经 SHA-256 校验的工作副本，原始固件未修改，不要擅自删除。

### 12.3 接手后的严格执行顺序

1. 评估是否补齐 sysapihttpd 的 USB root、RR 规则、FastCGI/Lua 后端，使 `GET /` 从 `ok_trigger_error` 推进到完整响应；不影响现有 22 函数观测证据。
2. 不运行 AILIFT，不生成 AI 标签；失败与部分成功 trace 均原样保留。
3. 再扩展 D-Link/TP-Link、工控和防火墙样本，并为每个新路由格式添加阳性/阴性 fixture。
4. 最后重跑本地/远端全量测试、e2e、构建、API 和浏览器验收，更新本文件和 M8 报告。

### 12.4 不可违反的交接约束

- 不删除或覆盖原始固件、`.m8-inputs` 副本、两个攻击面基线、最终 e2e 任务 `6ebb9777d437`、IDB、伪代码、trace、CBM DB 和 prewrite 备份。
- 不把 token、密码或 LLM key 写进命令、日志、前端资源、截图、文档或普通数据库字段。
- 不重新启用 AI 改名、IDA 回写或二遍反编译；真实任务默认不运行 AILIFT，兼容接口即使人工触发也只能写 `domain/libc_equiv/confidence/reason` 标签。
- 不把 busybox 的零路由结果或任何失败 trace 改成阳性；保守负例和失败证据必须原样保留。
- 不在本地初始化 Git、回滚用户文件或清理未明确授权的数据。
