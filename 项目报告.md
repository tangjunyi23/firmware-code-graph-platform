# 固件攻击面分析与 AI 漏洞挖掘平台（FWGraph）项目报告

> 编制日期：2026-08-28  
> 仓库：`firmware-code-graph-platform`（分析机 `~/src/firmware-code-graph-platform`）  
> 当前入口：`http://192.168.108.129:8000`（本机 systemd 直跑，明文 HTTP）  
> 事实来源：`README.md`、`fwgraph/README.md`、`进度-项目.md` / `进度-前端.md` / `进度-后端.md`、git 历史、本机运行状态。  
> 历史快照（不要当现行口径）：`项目介绍.md` v1.5（2026-08-02）、`HANDOFF.md`（2026-08-03）、`开发计划.md`。

---

## 1. 一句话与定位

FWGraph 把一份固件变成**可审计的函数级证据**：解包、反编译、代码图谱、攻击面、单 ELF 动态覆盖，再交给工作台里的挖掘 agent 产出带**调用链 + PoC** 的 finding。

它不是自动漏洞判定器，也不做整机仿真。source/sink、路径评分、空差分、socket accept 都只是证据索引；漏洞结论必须来自 agent 调工具后的观测，且服务端强制 `call_chain` 与 `poc`。

```
固件.bin
  → EMBA 解包（extract-only）
  → rootfs_elf / IDA 反编译（伪 C + 汇编 + symbols）
  → CBM 代码图谱 + CFG/AST
  → 攻击路径 / 静态路由 / 外部输入 / 逐输入攻击面
  → qemu-user 差分覆盖、qemu-exec、AFL / frida、协议 fuzz
  → 工作台 dsh-web 挖掘 agent → findings + 中文漏洞报告
```

---

## 2. 产品目标与明确不做

### 2.1 目标

| 目标 | 含义 |
| --- | --- |
| 证据保真 | 不改 IDA 真名、不回写 IDB、不覆盖原始伪代码 |
| 上下文压缩 | 上游 AI 先吃 brief / Top 攻击路径，而不是 28MB 函数清单 |
| 动静结合 | 静态路径用单 ELF qemu 覆盖、stdin/文件、协议 payload、PoC 执行交叉验证 |
| 可交付产品 | 登录、权限、审计、配额、报告中心、Docker 迁移包 |
| 工控可扩展 | 协议模板 fuzz（软件侧），不接硬件总线卡 |

### 2.2 明确不做

- 整机 / 整固件系统仿真
- 自动把空差分、连通、accept 写成漏洞
- 把 AI 重命名当可引用证据（AILIFT / ai_enrich 已于 2026-08-21 拆除）
- 博智式硬件能力（RS232/485/CAN 业务卡、DI/AI、继电器电源托管、TFT）

---

## 3. 当前系统形态（2026-08-28）

### 3.1 运行环境

| 项 | 现状 |
| --- | --- |
| 部署机 | Ubuntu 26.04 VM，用户 `tankuku` |
| 访问 | `http://192.168.108.129:8000`（`fwgraph.service` 用户级 systemd，Linger=yes） |
| 登录 | 账号密码；首登强制改密；会话 token `fws-` |
| 编排器 | FastAPI + Vue3 SPA（`webui/dist` 同源托管） |
| 图谱 UI | `cbm-ui.service` `:9750` + `cbm-ui-public` nginx `:9749`，编排器 `/cbmui` 反代 |
| 挖掘引擎 | 按会话拉起 DeepSeek Harness（`~/deepseek-harness`，profile `fwgraph-web`） |
| 沙箱 | 镜像 `fwgraph-sandbox:local`（991MB），按需 `docker run` |
| 解包 | `embeddedanalyzer/emba:2.0.3a`（约 36GB），特权兄弟容器 |
| LLM | OpenAI/Anthropic 兼容网关，工作台模型 `deepseek-v4-flash` |
| 测试基线 | orchestrator **529 项：528 通过 + 1 跳过**（`AGENTS.md`） |

本机冒烟（2026-08-28）：`/healthz` 11 个任务；仪表盘 findings 34、high_risk 21、traces 55（有差分 6）、surfaces 58。

### 3.2 模块状态

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| M1 解包 | 可用 | EMBA extract-only；可走 docker 官方镜像 |
| M2 反编译 | 可用 | ELF → `rootfs_elf`（idalib）；raw/PX4 → `idat` |
| M3 AILIFT | 已删除 | 2026-08-21 拆除，建图不依赖标签 |
| M4 CBM 图谱 | 可用 | 大树 FAST 模式，省略相似性/语义边 |
| M4b CFG/AST | 可用 | `.asm` → CFG；伪 C → tree-sitter AST |
| M6 攻击路径 | 可用 | source/sink BFS Top50 + 可选 LLM 分诊 overlay |
| M6a 外部输入 | 可用 | identification.json，证据四级 |
| M6b 逐输入攻击面 | 可用 | `AS-*.json` + `AS-AUTH-*.json` |
| M7 qemu 差分 | 可用 | `via=net/stdin`、`input_path`、`payload(s)_hex` |
| qemu-exec | 可用 | 单次 PoC；启动即崩标 `crash_kind=startup` |
| fuzz / frida | 可用 | ARM/MIPS 整二进制 AFL 优先；x86 frida |
| M-ICS 协议 fuzz | 可用 | 6 种模板 + 软件监视器；公网默认禁止 |
| M8 vulnagent | 可用 | 只走 dsh-web；`record_finding` 强制调用链+PoC |
| 产品化 | 可用 | 用户/会话/审计/仪表盘/报告/自动链 |
| Docker 交付 | 完成 | 编排器 + 沙箱 + 数据卷 + 迁移包 |

### 3.3 用户可见入口（现行）

顶栏：**许愿挖掘 / 工作台 / 前置任务 / 固件解密 / 协议逆向 / 仪表盘 / 分析视图 / 报告中心 / 系统管理**。

分析视图：函数、攻击面、输入面、图谱。

工作台是挖掘主入口：选已完成前置任务 → 发提示词 → dsh-web 会话。编排提示、能力验收不对用户展示。

---

## 4. 按时间段的开发内容

时间按 git 提交与进度文档对齐。早期里程碑号（M0–M8）来自 `开发计划.md` / `项目介绍.md`，后续以产品迭代为主。

### 4.1 2026-07-31 ～ 08-02　地基：图谱流水线 M0–M7

**Git：** `Initial commit`（07-31）→ `Add firmware code graph platform and handoff`（08-02）。

落地内容：

- 仓库与目录约定：`fwgraph/`（编排 + pipeline + webui）、`tools/`、`data/` 持久产物。
- **S1 解包**：EMBA docker + `extract-only.emba`（黑名单关掉非 P 模块），产出 `manifest.json`。
- **S2 反编译**：IDA 9.1 无头 `idat -A -S ida_export.py`；伪 C + `.asm` + `symbols.json`；FLIRT 资产在 `libc-sigs/`。
- **S4 图谱**：伪 C 树 → codebase-memory-mcp；大任务 FAST，避免 VM 在 semantic_edges 被 SIGKILL。
- **S5 / M7**：qemu-user + chroot 覆盖差分；`ok_trigger_error` 保留已观测覆盖，不因 HTTP 断开丢证据。
- **AS-1/2/3**：source/sink 路径、trace 交叉验证、保守静态路由表。
- 前端初版：任务 / 函数 / 攻击面 / 图谱四页签。
- 交接文档：`项目介绍.md` v1.5、`HANDOFF.md`。

同期验证（历史数字，仍作对照）：

| 样本 | 结果 |
| --- | --- |
| busybox 双架构 | 反编译 6622/6634；httpd GET 差分 58/62 函数 |
| 小米 R3 `56c105fd7b9a` | 274/274 ELF；337,245 节点 / 1,253,251 边；source 1585 / sink 6875 |
| PX4 v6xrt `aaae7834a0cd` | raw ARMv7-M loader；12,754 函数；38,540 节点 |
| 单元测试 | 当时全量 217 passed |

原则在这一阶段就定死：**证据不是 verdict；0 route / 0 path / 空差分如实保留。**

### 4.2 2026-08-03 ～ 08-06　攻击面产品化 + 第一代挖矿 agent

**Git：** 08-06 三条——攻击面流水线与 vulnagent、正式 README、AI 增强对比页、会话截断修复。

落地内容：

- 攻击面路径预计算进自动链（graph 后 attack / routes）。
- **vulnagent**：Managed Agents 风格工具集，对平台 HTTP API 挖矿。
- Web 增加挖掘相关视图；会话输出截断与 resume 修复。
- 一度加入「AI 增强对比」（伪代码 overlay）。该能力后来在 08-21 **整段拆除**，挖矿只引用 Hex-Rays 原文。

### 4.3 2026-08-15 ～ 08-17　产品化、安全、工控协议 fuzz、上下文预算

**Git：** `ICS protocol fuzzing, light pro theme, context-budget retrieval, productization`（08-17）。进度文档把大量实现记在 08-15。

**产品化后端**

- 用户/会话：PBKDF2、7 天滑动会话、`fws-` token。
- 登录防爆破（5 次锁 10 分钟）、弱口令黑名单、首登强制改密。
- owner 隔离（他人数据 404）、全量审计、`/logs` 仅 admin。
- 仪表盘、系统信息/配置白名单、报告中心（综合报告 + DOCX/PDF）。
- 一键自动链：`POST /firmware?auto=1` → 解包→反编译→图谱→攻击面→报告。
- 配额：trace / fuzz / frida / protofuzz 按 job×日 429。

**安全收口**

- `ORCH_TOKEN` 轮换；URL `?token=` 只收会话 token。
- TLS 自签默认开（后来本机 systemd 单元走明文 8000，Docker 路径仍可 HTTPS）。
- findings 服务端校验：md5/addr/CWE、static-only 置信度封顶 0.7、状态机 draft/verified/disputed/retracted。

**管线置信度（MX12 `90e7e89dc221`）**

- 外部输入 5→39（四级证据；syslogd 幻影消除）。
- AS 克隆组 39→0；GoAhead 路由从 PLT 桩修到真实 handler。
- CFG 假 ret 边 29.7%→0.38%；AST ERROR 10.1%→0.09%。

**M-ICS 协议模糊测试**

- 模板：Modbus TCP / S7 / OPC UA / DNP3 / MQTT / HTTP。
- 确定性变异 + TCP/ICMP 监视器 + 复播统计。
- 授权确认闸门、公网默认禁止。
- 前端「协议挖掘」：真机 IP 或从 identification 预填。

**挖矿上下文预算**

- `GET .../functions/{md5}/{addr}/brief`（约 1KB）。
- `attack_surface brief=true`（top20 12KB→7.1KB）。
- cfg/ast 截断；builtin 历史压缩。

**前端**

- 登录控制台、简易/专业模式、九阶段工作台。
- 主题先科技蓝暗色，08-15 改为浅蓝专业亮色（后又在 08-22/24 回到深色产品壳）。

**对照样本**

- DSM DS120j：40 条公网输入、15 个加密 SPK 记入 `unreadable`。
- MX12：169 ARM ELF 全管线贯通。

测试基线升到约 478～481 项。

### 4.4 2026-08-19　配置层 + Docker 沙箱（Phase 1–2）

**Git：** 统一 config；trace/fuzz/frida 不可信执行进 docker；Anthropic 网关 `x-api-key`。

落地内容：

- 路径/密钥全部走 `orchestrator/app/config.py` + `fwgraph/.env`。
- `SANDBOX_BACKEND=docker`（可组件级覆盖）；镜像不可用则回退并写中文警告。
- 为 Phase 3 容器化铺路。

### 4.5 2026-08-21　引擎换挡：rootfs_elf + DeepSeek Harness，拆掉 AI 标注

**Git：** `Unify firmware analysis on rootfs_elf and DeepSeek Harness.`

这是架构分水岭。

**反编译**

- ELF 不再走 `idat -S ida_export.py`。
- 强制 `tools/ida-no-mcp/rootfs_elf/ida_worker.py`（idalib），`IDA_WORKERS=3`。
- `rootfs_elf_adapt.py` 把 `function_index.jsonl` / `decompile/*.c` 适配成原 `symbols.json` 布局。
- raw/PX4 仍用旧导出。
- 路由扫描找不到 `data/idb/*.i64` 时回退 ELF，单二进制失败不中断整 job。

**挖矿引擎**

- 工作台只启动 dsh，不再回退 builtin。
- 对话动画对齐 DeepSeek Harness（思考扫光、工作中 shimmer、流式光标）。
- 挖掘过程从「事件流」搬回工作台。

**拆除**

- M3 AILIFT 全套 API / 自动链步骤 / 前端 EnrichTrigger。
- ai_enrich overlay。
- 自动链变为：解包 → 反编译 → 图谱（内含 attack/routes/surfaces/graphext/报告）。

**证据纪律（借鉴 Firida，不搬 MCP）**

- Evidence Address = job + md5 + 规范地址。
- 动态可达性只允许 `observed_in_window` / 单条 trace 的 verified。

**图谱**

- 增量摄入；攻击路径邻接 = IDA 名边 ∪ CBM CALLS ∪ CFG call。
- 规则路径之后 LLM 只做 P0/P1/P2/noise 分诊，不拔高 verified。

**产品壳**

- 工作台与专业分析合并；事件流替代旧「漏洞挖掘」页。
- 加密/空固件不再误报「反编译全部失败」。

### 4.6 2026-08-22　工作台产品化 + Archer C7 全自动链与首轮真实 finding

前端连续重构，后端把挖掘会话做成「能停、能删、能进对话」。

**界面**

- 工作台复刻 Harness 三栏壳；去掉 DeepSeek 品牌。
- 顶栏标签替代双左侧栏；会话列表回到左侧。
- Inter / JetBrains Mono；品牌 FWGraph。

**会话产品**

- 前置任务 vs 工作台拆分：上传分析在前置；工作台只挖已完成任务。
- 发送即进当前对话；底部输入；思考/工具折叠；停止/删除/归档。
- 审批：全部同意 / 需要审批。
- 用户气泡只留用户原话；挖洞方法走 persona + skill。
- 报告中心只保留任务漏洞报告；finding 必须 call_chain + poc。
- dsh host 注入自签 CA，否则 `fw_*` 全失败。

**Archer C7 自动链（job `3a1f3c822c22`）**

- 固件 `ArcherC7v2_en_us_3_15_4_up(260427).bin`，`auto=1`，终态 `routed`。
- 133 MIPS ELF；输入识别去掉 binwalk 抠片；跳过整库 `source.c`（samba 会吃光 1800s）。
- 图谱约 23201 函数 / 69952 节点；攻击路径 50（AI 分诊 20）；15 个攻击面 + 5 条授权链。

**首轮真实挖掘**

- 会话 `s-mt4dtbug-e1c6`：2 条严重命令注入（CWE-78），调用链 + curl PoC。
- Docker 覆盖：不盖 `/etc`/`lib`；guest 路径绑 rootfs；忽略编译机 RPATH。
- 编排器重启后僵尸 `running` trace 超时收尸。
- 连续静态工具不跑 trace 时，第 3 次起强制提示 `fw_request_trace`。

### 4.7 2026-08-23　无人值守动态闭环：stdin、协议 payload、轮次硬顶

**Git：** dsh-web hunt workbench；Archer 会话/finding 进仓库；README 同步。

动态能力补齐（仍不做整机仿真）：

| 能力 | 作用 |
| --- | --- |
| `via=stdin` / `input_path` | 解析器/CLI 覆盖；修 docker 重定向到 `rootfs/tmp` |
| `payload` / `payload_hex` / `payloads_hex` | SSH/SMB 等多步字节注入 |
| `fw_qemu_exec` | 单次 PoC；超时≠崩溃；启动 SIGSEGV=`startup` |
| identification 补 md5 | 避免 agent 把 dropbear 当成「无 md5」停手 |
| 非 HTTP trigger | ftp/ssh/dhcp 不再误用 HTTP GET |
| 特权端口映射 | guest 22 → 宿主 10022，避免抢 sshd |
| MIPS fuzz hook | 补 `TARGET_MIPS` + `AFL_ENTRYPOINT`；函数级 handshake 仍常失败，整二进制能跑 |

挖掘治理：

- `max_turns` 按 `session.prompt` 计数；到顶询问继续/结束；stop 后不能被 mux 拉活。
- 会话 trace 预算 24→96→192；日配额随 payload 实验上调。
- 空差分不再重跑 28MB symbols 的攻击路径（RSS 曾涨到 1.8GB+）。
- 工具返回 `hunt_next` 给模型下一刀；编排/验收文案对用户隐藏。
- 输入框「停止生成」≠ 结束会话。

仓库策略：`vulnagent/sessions/`（4 个）+ `findings/`（18 条）进 Git；解包/图谱约 1.2GB 留分析机。

参考会话：主挖掘 `s-mt4rda4n-55d3`；stdin 验收 `s-mt5k23tt-aaed`。

### 4.8 2026-08-24　工作台打磨、配额、SysV shm、新手教程

面向真实无人值守卡点：

- 网关空工具名 `unknown tool ""`：在 dsh 流上还原 name；Harness → `0.1.1-rc.2`。
- 损坏会话 history：隔离坏日志，编排器开新会话。
- 任务选择器被顶栏裁切：Teleport + 可滚列表。
- `fw_request_trace` 并行 409：busy 立刻返回；监督脚本不 cancel 在飞 trace。
- `fw_get_trace` 含 `undefined` 导致 lossless JSON 失败：压成纯 JSON。
- 刷新丢会话：sid 进地址栏。
- 新手教程 9 步；`UI_ONBOARD_TOUR` 可关。
- GitHub finding 按 ELF md5 挂到本机任务 `6df0ba4135c9`。
- httpd qemu 启动 SIGSEGV：shm 段 mode 000 → 预置 0666 + `--ipc=host`。
- 浏览 squashfs 路径、md5 前缀、call_trace 502 降级、配额再放宽。

前端：思考一行可展开、自由滚动、专家/许愿双模式、深色驾驶舱、协议逆向提到顶栏、固件解密顶栏、前置九步看板。

### 4.9 2026-08-25　交付层：解密、协议逆向、Docker 三镜像、IDA 投放

（进度-后端 / 前端 08-25，部分尚未单独打 git tag。）

- **上传自动解密**：状态 `decrypting`；`pipeline.fwdecrypt`；解不开仍交 EMBA。
- **协议逆向六块**：标识、算法识别、深度分析、算法逆向、流量解码、攻击面评估；只读固件产物，不抽公网密钥。
- **Docker Phase 3**：`fwgraph-orchestrator` / `fwgraph-sandbox` / `fwgraph-data`；命名卷；迁移包 `install.sh`。
- IDA 可丢进 `deploy/docker/ida-drop/` 或 `data/ida/` 自动识别。
- 编排器镜像内置 rootfs_elf；EMBA 仍独立 36GB 镜像。
- `fw_request_fuzz` 解析 `~/AFLplusplus`；`fw_call_trace` 映射 in/out。
- 工作台：打开会话先读 `events.sse`，不空等 host；粒子背景、教程 v2、专家模式去掉协议挖掘菜单项。

### 4.10 2026-08-28　函数页汇编 404（本轮）

现象：分析视图选函数再点开，报 `加载伪代码失败: no assembly export for this function`。

原因：08-21 改 rootfs_elf 后只适配了 `.c`。Archer 任务 `beb9836a7b28`：**39609 个 `.c`、0 个 `.asm`**，graphext `cfg_functions=0`。

修复：

- `ida_worker` 按 `ida_export.py` 同行格式写 `functions/<addr>.asm`。
- 适配层改写 header、从 index 推断 size；缺 IDA 产物时 objdump 回填。
- `scripts/backfill_asm.py` 补已有任务。
- 前端：汇编 404 时回退伪代码并注明。

回填结果：`beb9836a7b28` 39627 条汇编；`6df0ba4135c9` 38334 条。接口实测同一函数伪代码/汇编均 200。

---

## 5. 流水线与证据口径（现行）

### 5.1 自动链状态

`pending → decrypting → extracting → decompiling → graphing → attacking → routing → identifying → surfacing → routed`

（历史 `ailifting` 已取消；旧 `ailifted` 任务补跑直接进 graph。）

### 5.2 挖掘工具纪律

- 静态：`fw_list_*` / `fw_get_function_source` / `fw_attack_surface` / `fw_get_identification` / `fw_browse_firmware` …
- 动态：`fw_request_trace` / `fw_get_trace` / `fw_qemu_exec` / `fw_request_fuzz` / frida
- 入库：`record_finding` 必须 `call_chain`（`→`）+ `poc`；static-only 置信度封顶 0.7
- 空差分、连通、accept **不是漏洞**
- 没喂 payload 的 SIGSEGV = 环境，禁止放弃该 ELF，必须改 via 再 trace

### 5.3 仓库里有什么 / 没有什么

| 在 Git | 仅分析机 |
| --- | --- |
| 源码、工作台、dsh 插件/skill、测试 | `.env`、TLS 私钥、口令 |
| Archer 会话与 18+ 条 finding JSON | `fwgraph/data/` 解包、伪代码、图谱、trace |
| 文档与进度 | EMBA/IDA/qemu/沙箱镜像、LLM key |

clone 下来可以读报告，不能直接接着动态挖。

---

## 6. 代表性验证（按样本）

| 时间 | 样本 | 结论 |
| --- | --- | --- |
| 08-02 | busybox mips/arm | 流水线与 httpd 差分成立 |
| 08-02 | 小米 R3 | 全量 IDA+FAST 图谱+攻击面；动态仅 `ok_trigger_error` |
| 08-02 | PX4 v6xrt | raw Cortex-M loader 与图谱成立，攻击路径 0 是诚实负结果 |
| 08-15 | DSM DS120j | 加密 SPK 记账，不假装解开 |
| 08-15 | US_MX12V2 | 169 ARM ELF 全自动链；输入/攻击面/CFG/AST 质量修复 |
| 08-22 | Archer C7 `3a1f3c822c22` | 自动链 `routed`；真实 CWE-78 finding |
| 08-23 | Archer 无人值守 `s-mt4rda4n-55d3` | 80 轮，十余条多为 static-only；stdin 验收通过 |
| 08-25 | 本机任务 `beb9836a7b28` / `6df0ba4135c9` | 持续挖掘与报告中心挂载 |
| 08-28 | 同上 | 汇编导出补齐，函数页可看反汇编 |

仪表盘（08-28 本机）：11 job、34 findings、21 high_risk、4 份报告、55 traces（6 条有差分）。

---

## 7. 已知限制（必须写进报告）

1. **网络口大量空差分**：缺处理函数覆盖，不是缺整机仿真。应用 `via=stdin` / 文件 / payload，不要上全系统 QEMU。
2. **MIPS 函数级 AFL**：persistent handshake 经常失败；整二进制 AFL 可用。
3. **surfaces 角色分类**仍是启发式；busybox applet 锚不住就降级，不漫游整二进制。
4. **CFG 跳表**无法从 `.asm` 恢复，只标 `indirect_jump`。
5. **加密容器 / 厂商 SPK** 无私钥只能记账。
6. **AppArmor** 禁 unprivileged userns 时，trace 需显式 root chroot。
7. **Docker 镜像 glibc**：宿主编译的 cbm / afl-qemu-trace 与 ubuntu:24.04 不一定兼容；容器内 cbm-ui 可能起不来，编排器仍可用。
8. **本机 systemd 未开 TLS**（与文档默认 HTTPS 不一致）；LAN 访问走 HTTP :8000。
9. 全量 pytest 有 1 个已知跳过/偶发污染项，单跑通过。
10. 前端主 chunk >500kB，未做进一步代码分割。

---

## 8. 未来开发

分三档：立刻能做的产品缺口、增强动态证据、中长期方向。都不包括「做整机仿真」或「让模型不跑工具就下结论」。

### 8.1 近期（建议下一迭代）

| 项 | 理由 |
| --- | --- |
| 把 08-24～08-28 的 VM 改动整理提交（需确认） | git 最新正式提交停在 08-23，其后解密/协议逆向/Docker/汇编均在工作区 |
| candidate 输入升级为 observed | qemu 实探测端口，减少 34 条无端口噪声 |
| M7 在 MX12 / Archer httpd 上再跑一轮 | 把静态 Top 路径标成 observed/verified |
| surfaces 按 applet 过滤 busybox 调用链 | 降角色分类噪音 |
| `test_findings_api` 全量污染隔离 | 稳住 529 基线 |
| 本机 systemd 与文档 TLS 口径对齐 | 要么开 `ORCH_SSL`，要么文档写明 HTTP |
| 新任务跑完 graphext 后确认 CFG 非 0 | 汇编回填后旧 CFG 不会自动重算，需补跑 `POST /jobs/{id}/graphext` |

### 8.2 中期（动态与工控）

| 项 | 范围 |
| --- | --- |
| M-ICS-3 | identification 服务清单 → 一键协议 fuzz 预案；挖矿工具 `fw_request_protofuzz`（配额） |
| M-ICS-4（可选） | 未知协议：pcap 聚类 / 字段推断 → 生成模板 |
| AS-3 路由适配器 | 按厂商多字段/哈希表加阳性+阴性 fixture，继续保守匹配 |
| 函数级 fuzz 启动可达 | 固件 daemon 的 chroot/网络/shm 预置清单化（已做 httpd shm，可推广） |
| guest stdout/stderr 尾部 | 诊断 qemu 早退，不覆盖旧 trace |
| 挖掘六阶段 hunt 与 dsh 轮次编排彻底打通 | 后端待办仍记 builtin hunt 残留 |

### 8.3 长期（样本面与交付）

| 项 | 范围 |
| --- | --- |
| 分层试炼 | D-Link/TP-Link、一份工控、一份防火墙；哈希去重 |
| VxWorks / 非 ELF blob | 明确降级 manifest + 可验证 loader profile（PX4 已是样板） |
| FLIRT 矩阵 | 年代×ISA×字节序×配置；Lumina 换 license 再开 |
| 迁移包体验 | 离线 EMBA load、IDA drop、Node 22 工作台一键提示 |
| 前端性能 | 主包拆分、图谱大数据集虚拟化 |

### 8.4 明确不排期

- 全系统 QEMU / 整机仿真
- 重新启用 AILIFT 改名或 IDB 回写
- 为「看起来有结果」放宽 source/sink/route 规则
- 硬件协议卡、继电器、采集屏

---

## 9. 目录与运维备忘

```
firmware-code-graph-platform/
├── fwgraph/                 编排器 + pipeline + webui + data/
├── vulnagent/               dsh 插件、skill、sessions/findings
├── tools/ida-no-mcp/        rootfs_elf 反编译 worker
├── deploy/docker/           编排器/沙箱/数据镜像与 compose
├── 进度-项目.md / 进度-前端.md / 进度-后端.md
└── 项目报告.md              本文
```

启动（现行本机）：

```bash
systemctl --user status fwgraph.service   # 已 enable + linger
# 或：bash fwgraph/scripts/run_orchestrator.sh
```

改 Python 后重启用户服务；改前端 `cd fwgraph/webui && npx vite build`（dist 即时生效）。

---

## 10. 结论

项目在约四周内（2026-07-31 → 08-28）从「固件→图谱」做成了**可登录的攻击面分析平台 + 工作台 AI 挖掘**。

前两周完成可审计静态证据链（解包、Hex-Rays、CBM、攻击面、单 ELF 覆盖）。中间一周完成账号、配额、报告、工控协议 fuzz 和 Docker 沙箱。最近十天把主路径换成 **rootfs_elf + DeepSeek Harness 工作台**，并在 Archer C7 上跑通自动链和带调用链/PoC 的真实 finding。最后补上 rootfs_elf 缺失的汇编导出，函数页不再 404。

下一步应优先：**提交 08-23 之后的代码、把 candidate 输入做成可观测、给已回填汇编的任务补跑 CFG、继续在真实守护进程上打 stdin/payload 而不是上整机仿真。**
