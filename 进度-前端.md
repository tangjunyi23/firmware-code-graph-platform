# 前端进度

> 最后更新：2026-09-21 · 配套：`进度-项目.md`（总览）、`进度-后端.md`。


## 快速模式文案产品化（2026-09-21）

快速模式向导与选项全面去口语化：第 1 步「要挖哪个固件？」→「选择目标固件」，
第 2 步「希望挖到什么？」→「选择检测目标」，第 3 步「确认出发」→「确认并启动」；
步骤条「选固件/选目标/确认出发」→「选择固件/设定目标/确认启动」；「新的愿望」
→「新建任务」；「开始挖掘」→「启动挖掘」；上传区、复用任务、补充说明的
引导语同步改写为书面表述。检测目标选项（`pipeline.js WISH_OPTIONS`）：
「尽量全挖」→「全面检测」（副标「按攻击路径风险评分自动覆盖全部类型」）、
「认证绕过」→「身份认证绕过」、「信息泄露」→「敏感信息泄露」、
「预认证 RCE」→「预认证远程代码执行」（副标去掉「未登录就能打到」等口语）。
任务书 `buildWishTask` 同步书面化，目标前缀「用户希望挖到：」→「本次挖掘目标：」；
`SidebarJobs`/`CommandPalette` 的会话标题解析正则兼容新旧两种前缀，旧会话
不受影响。无头浏览器三步走查通过，`test_webui` 30 项全过。

## 模拟观察台执行流可读性整修（2026-09-18）

`MessageList`/`dshClient` 在模拟场景下的显示问题一次修：`fw_emul_*` 工具行
折叠标题原全是「工具」——现逐个映射中文名（建环境/启动服务/探活/发布就绪…）
并在折叠行带原始工具名等宽小字；摘要改语义化参数（路径/服务/端口/目标），
结果可解析时补「→ 通过/不通/已就绪」。思考行完成态由「思考中」改「思考过程」；
工具行加状态圆点（绿 ok/红 error/灰 stopped）与完成时刻，思考行显示开始时刻；
`turn/end` 渲染成「第 N 轮完成 · 时刻」分隔线（interrupted 仍显示已停止）。
「显示更早」改分批放行（每批 80），大会话不再瞬间全量渲染。

「查看」按钮原点了没反应（EmulView 没接 `@inspect`）——观察台补调用详情
抽屉（输入/输出 JSON、状态、时刻）。`highlight.js` 改原文分词、逐段转义：
此前在转义后文本上高亮，`&#39;` 实体内的 `#`/`39` 被拆成 token，用户直接
看到 "Let&#39;s" 字样。

EmulView：「正在做什么」优先取正在运行的工具（此前引用 agent 总结原文，
出现「进行中 · 已完成…」自相矛盾）；步骤条对 stopped/failed 环境不再把
「底座」亮成进行中；目标行绑定所选会话的请求（此前全局最新请求会串台）；
会话 chips 显示任务摘要（原裸 hex ID），失联会话（running 但宿主已死）
标「失联」并给「停止」按钮；日志面板自动滚底、上滚暂停跟随；服务行点击
过滤、再点取消；执行流面板高度跟随视口。开场任务气泡固定排最前（宿主
history 不含 user 事件，mux 订阅后补发曾排到末尾）。

## Lucide 线型图标（2026-09-01）

侧栏、登录、工作台图标换成 Lucide（ChatGPT / Cursor 同类 stroke 1.75）。发送钮圆形品牌蓝。分析页按钮原先已是 Lucide。

## 产品化浅色氛围底（2026-09-01）

全站底板改为品牌蓝光斑 + 48px 淡网格。光斑 12/16 秒漂，网格 18 秒平移，登录卡呼吸光。尊重 `prefers-reduced-motion`。仪表盘 KPI 微抬光、LIVE 点脉冲；登录卡/输入卡淡品牌光晕。侧栏毛玻璃。不做粒子或深色赛博。

## 切页抖动（2026-09-01）

去掉主栏 fade 过渡（离开页绝对定位叠在 padding 切换上会跳）。主栏 padding 固定为 0，留白放 `.page-pane`；对话/态势页再清掉 pane 内边距。切页不再收起侧栏其它分组。`html` 预留滚动条槽。

## 对话列表单行截断（2026-09-01）

工作台侧栏固件名和会话标题没有 `ellipsis`，长 task（「用户希望挖到：命令注入…」）会换行叠到下一项。改为单行省略；固件名去掉 `.bin`；会话标题去掉「用户希望挖到：」前缀。


## 漏洞库写中文描述和漏洞点（2026-09-01）

条目增加 `vuln_point`（入口→函数→危险调用）和中文 `description`。详情页分两块展示；收录表单必填。示例三条已补中文。


## 报告预览代码块改浅色（2026-09-01）

报告中心 Markdown 的调用链 / PoC `pre` 仍是 `#0f172a` 黑底，浅色页上整块发黑。改为 `--fw-surface-2` 底、`--fw-text` 字。


## 漏洞库页（2026-09-01）

侧栏大项「漏洞库」。检索 CVE/CNVD、按当前任务查 N-day、手工收录与 JSON 导入。示例写入 Archer C7 / 小米 R3 两条。对话高亮识别 CNVD/CNNVD 编号。


## 小功能收入大项（2026-09-01）

功能栏只留大项。新对话下挂前置任务；固件工具下挂解密/协议/事件流；分析下挂函数/攻击面/输入面/图谱。点「新对话」进挖掘，点子项进小功能。


## 点击只走功能栏（2026-09-01）

页面跳转只留左侧功能栏。对话页不再藏栏、会话轨不再复制工具入口；仪表盘/解密/协议页去掉「去某某」按钮。分析页一律扁平列出。

## 清言式对话编排（2026-09-01）

挖掘主页只留一条会话轨：新建对话、工具（前置/解密/协议/态势/分析/报告）、历史会话。产品栏在对话页隐藏；其它页用「返回对话」回来。中间空态「今天挖哪一块固件？」。`npx vite build` 已过。

## 工作台与前置任务并组（2026-09-01）

侧栏把「挖掘会话」和「前置任务」收进「工作台」分组，默认展开。深链 `?page=jobs|prepare` 不变。

## 左侧栏 + 智谱浅色产品壳（2026-09-01）

顶栏功能移到左侧 232px 导航：品牌、专家/许愿、一级菜单、分析视图/系统管理内联展开、底部用户。全站改浅色令牌（底板 `#f7f8fa`、品牌蓝 `#134CFF`），去掉粒子连线、启动 splash、标题 glitch、赛博网格和雷达脉冲。工作台默认浅色。`npx vite build` 已过；浏览器核对登录页、工作台、仪表盘。

## 组件健康图 + 引擎文案去品牌（2026-08-28）

仪表盘组件健康改为环形在线/离线 + 分组条（核心管线 / 模糊测试 / 大模型接口），列表用中文名，不再展示 `deepseek-v4-flash`。系统设置引擎标签改为「固件漏洞挖掘引擎」；LLM 模型输入框仍绑定 `LLM_MODEL`（值保持 deepseek-v4-flash）。`npx vite build` 已过。


## 函数伪代码/汇编高对比高亮（2026-08-28）

分析视图代码块原先套 atom-one-light，叠在深色底上注释和默认字发灰。改为深色高对比：伪代码关键字青、函数粉、字符串绿、数字橙、注释浅灰；汇编地址青、助记符粉、寄存器绿。`npx vite build` 已过。

## 函数页缺汇编回退伪代码（2026-08-28）

分析视图 / 攻击面点开函数再切「汇编」，rootfs_elf 任务没有 `.asm` 会 404 显示 `加载伪代码失败: no assembly export for this function`。现 404 时保留汇编档并显示伪代码，注明没有汇编导出。`npx vite build` 已过。

## 事件流日志去乱码（2026-08-25）

EMBA ANSI 颜色、终端复位、进度条 `\r` 在事件流里会显示成乱码。接口先清洗再返回；页面按级别分列（标记 / 时间 / 正文），常见步骤译成中文。

## 对话高亮 / 思考脉冲 / 报告排版（2026-08-25）

数字、`0x` 地址、CWE/CVE、箭头和符号在工作台流出与成稿里高亮。
思考中改为与看板相同的雷达脉冲 + 青色光晕。
会话报告改成表格 + 代码块；报告页 Markdown 带危害等级徽章和更清晰的标题/引用。

## 工作台背景与新手教程（2026-08-25）

工作台透出全站粒子与网格。新手教程改为 v2：专家/许愿、顶栏、前置九步、解密、协议逆向、工作台挖掘、仪表盘、报告。

## 前置任务九步实时播报（2026-08-25）

专家模式前置任务主区改为九步看板：解密到攻击面带勾选动画、进度条和旋转环；右侧进度播报随状态变化出中文。

## 专家模式顶栏去掉协议挖掘（2026-08-25）

专家模式功能栏不再列出「协议挖掘」。

## 固件解密顶栏（2026-08-25）

顶栏一级入口「固件解密」显示自动解密进度、步骤和日志。
前置任务 / 许愿挖掘上传后自动跑，不必再点一次。

## 协议逆向六块能力（2026-08-25）

顶栏「协议逆向」拆成协议标识、加密算法识别、加密算法深度分析、算法逆向、流量实时解码、攻击面评估。
粘贴报文边输入边解码；算法与攻击面只读固件产物，不抽公网密钥、不破密文。

## 协议逆向提到顶栏（2026-08-24）

「协议逆向」从分析视图子菜单提出，作为顶栏一级入口，独立铺满内容区。

## 粒子背景 / 开场 / 数字动画（2026-08-24）

全站固定粒子连线 canvas；登录后一次 splash 校准动画；看板与许愿 KPI 数字滚动。尊重减少动态。

## 赛博驾驶舱与协议逆向（2026-08-24）

仪表盘增加 AI 命中率、进行中任务（进度/漏洞数/状态）、威胁雷达、厂商排行、中文扫描日志、历史报告。
报告名跟固件走，并分专项 / 季度 / 协议。新增「协议逆向」页：报文解码、算法线索、固件内密钥材料发现。

## 许愿模式驾驶舱（2026-08-24）

许愿过程显示总进度环、前置/轮次百分比、二进制/攻击面/动态/入库卡片。
右侧可切换查看阶段、路径来源、trace 差分和入库条目；步骤条也能点开。

## 专家 / 许愿双模式（2026-08-24）

原工作台与分析视图收为「专家模式」。顶栏可切到「许愿模式」：上传固件、勾选挖洞方向、点开始后自动跑前置再拉起动态挖掘会话。

## 全站统一深色产品语言（2026-08-24）

壳层、登录、看板、分析页、报告、工作台共用 `theme.css` 令牌。
Element Plus 走 dark css-vars；工作台默认 dark 并对齐产品底色。

## 仪表盘量化看板（2026-08-24）

仪表盘改为指挥舱式深色看板：KPI、危害/可达/任务三张饼图、14 日折线、类型条形图。
图表为自绘 SVG（无新依赖），点进 KPI 可跳到对应模块。

## 思考行钉底 + 打字机不进 Vue（2026-08-24）

当前回合的思考行固定在对话最下方，标题闪烁，预览逐字流出。
节点 `markRaw`，打字机只改 `textContent`，不再每个字触发 Vue 整树更新。

## 工作台输出跟上推流（2026-08-25）

去掉 1 字 / 22ms 节流。SSE 增量下一帧整段画出；`turn/end`、历史回填、页签隐藏、会话已结束立刻 `flushType`，刷新后不再慢慢吐已完成的字。

## 工作台逐字流出与流畅度（2026-08-24）

助手正文按到达的增量流出（不再人工拖慢）。
工具卡中文标题。侧栏 20s 静默刷新且页签隐藏时跳过；目录列表未变则不替换数组。
对话只渲染最近 80 条，去掉 `content-visibility` 以免滚动跳变。

## 工作台隐藏「动态闭环完成」编排复盘（2026-08-24）

`isInjectedHuntHint` 再滤 `动态闭环完成` / `crash_kind=startup` 环境说明。

## 系统设置可改每日动态配额（2026-08-24）

系统配置增加 TRACE/EXEC/FUZZ 每日配额数字项，保存进 `settings.json`。

## 工作台刷新保住会话；思考一行展开；自由滚动（2026-08-24）

对话 sid 写入 `?sid=` 和 sessionStorage，刷新不再回到工作台首页。
Thinking 默认一行预览，点开看全文。对话不再跟着生成进度硬跳，回底部用缓动。

## 首次登录新手教程（2026-08-24）

登录后（改完强制密码）自动走 9 步：顶栏功能、前置任务上传分析、工作台选任务并发掘、分析视图、报告中心。
完成/跳过写入 `localStorage` `fwgraph_onboard_v1:<用户名>`；头像菜单「新手教程」可重放。
系统设置「界面」有「登录后自动弹出新手教程」（`UI_ONBOARD_TOUR`，默认开）。关掉并保存后不再自动弹出。

## 工作台 Thinking / Read 去掉前置图标（2026-08-24）

折叠态不再显示思考图标和 Read 首字母；展开时仍用下拉箭头。

## 损坏会话的 history 502 不当成死会话（2026-08-24）

`session.history` 若返回校验失败 / history unavailable，工作台按空历史继续，不再把整页打成「历史加载失败」。

## 工作台任务选择器不再裁掉顶部（2026-08-24）

「选择已完成的前置任务」菜单改 Teleport 到 body，按视口向下展开并限制高度。
原先向上弹出，被顶栏和 overflow:hidden 裁掉列表最上面一条（最新完成的任务）。

## 停止生成 ≠ 结束会话（2026-08-23）

- 输入框「停止生成」只 `session.cancel`，会话保持 running。
- 侧栏「停止对话」仍走 `/stop`。
- 已经误标 done 的会话，聊天框发送走 `POST /resume`，不再 409 `session already finished`。

## 编排/验收提示不进用户气泡（2026-08-23）

- `isInjectedHuntHint` 额外过滤能力验收、`平台已修好`、`记下 trace_id` 这类编排注入（含已落盘历史）。
- 队列面板同样过滤，避免待发送时露出来。

## 轮次上限询问条（2026-08-23）

工作台在 `awaiting_continue`（或已到顶的 done 会话）显示「已达 N/M 轮，是否继续挖掘？」；继续走 `/continue`，结束走 stop。
> 修改前端后必须同步本文档（约定见根目录 AGENTS.md）。

## 编排修复提示不当作用户气泡（2026-08-23）

- `isInjectedHuntHint` 额外过滤以 `【编排】` 开头的看门狗续跑提示。
- `npx vite build` 已过。

## 工作台隐藏误注入的方法提示（2026-08-22）

- 用户气泡过滤 `record_finding` 字段教程、`callChain` 纠正、`已经入库。继续挖` 这类不是用户打的提示。
- `npx vite build` 已过。

## 工作台审批可点、左下角策略切换（2026-08-22）

- 拒绝/允许按 harness 的 client-response 提交（rpcId + sessionId + approvalId + outcome），点了会真正落地。
- 输入框左下角两档：「全部同意」自动放行动态工具，「需要审批」弹出面板。选择会记住。
- `npx vite build` 已过。

## 工作台三点菜单、Thinking 一行、开始即进会话（2026-08-22）

- 会话行改为三点按钮，点击后竖向菜单：停止（进行中）/ 归档；已归档分组可「删除已归档」。
- 输入框右下角在生成中再点即停止（不必清空草稿）。
- Thinking 默认一行预览，逐字流出，点开才看全文；同一回合的思考合并成一条，回填时不逐条渲染。
- 点开始后立刻切到当前对话（会话 202 先返回），不必再点左侧栏。
- `npx vite build` 已过。

## 工作台停止/删除对话（2026-08-22）

- 左侧会话行悬停或选中时显示停止（进行中）和删除。删除会确认，已入库的漏洞仍留在任务报告里。
- `npx vite build` 已过。

## 工作台进入会话、底部输入与逐字输出（2026-08-22）

- 开始挖掘后直接进入该会话，不必再点侧栏。
- 输入框钉在主栏底部，不再跟着消息滚到屏幕上方。
- 直播增量用 rAF 节流滚动；正文按字符队列逐字显示；思考只占一行 `Thinking`，去掉 Deep diving。
- `npx vite build` 已过。

## 工作台思考/工具流式折叠（2026-08-22）

- 按 harness 真实事件：`assistant/chunk.data.chunk` 解包；block 键带 turn+step，避免多步挤成一条静态 Think。
- 先回填历史再开 mux；用 seq 去重而不是只升水位，工具结果从 `message.source.callId` 配对。
- 流式时自动展开 Think、随 `rev` 滚到底。报告列表只展示任务漏洞报告。
- `npx vite build` 已过。

## 工作台不再展示挖洞剧本 / Deep diving 卡死（2026-08-22）

- 发送挖掘只提交用户输入；挖洞方法不出现在气泡里。
- 回填对话时忽略 harness 注入的非用户 `user/message`（运行时快照等）。
- 会话已结束时清掉 `running`，避免 `turn/start` 无 `turn/end` 时一直 Deep diving。
- history 若报 `invalid payload for session.history` 不再把回合标成运行中。
- `npx vite build` 已过。

## 前置任务 / 工作台拆分（2026-08-22）

- 顶栏新增「前置任务」：上传固件 + 一次提示词，跑解包到攻击面并保存进度。
- 工作台只做漏洞挖掘：选已完成前置任务，输入提示词后发送；去掉只读/动静切换和固件上传。
- Search/Read 工具卡不再因 `turn/start` 清索引或 `tool/call` 回放而一直显示运行中。
- `npx vite build` 已过。

## 工作台对话回到左侧栏（2026-08-22）

- 工作台去掉顶栏「新会话 / 任务」下拉；「新建对话」、搜索、按固件分组的会话列表和设置固定在左侧 280px 栏。
- 应用顶栏导航不变。三列栅格按左侧栏宽度计算中间区与详情拖动手柄。
- `npx vite build` 已过。

## 顶栏下拉被裁切（2026-08-22）

- 「分析视图」「系统管理」菜单挂到 `body`（fixed），不再被顶栏 `overflow` 裁掉。

## 产品化字体与视觉（2026-08-22）

- 全局字体：Inter Variable（西文/数字）+ 系统黑体（中文），代码用 JetBrains Mono；去掉中文大字距。
- 登录页改为品牌标 + FWGraph 产品名；顶栏使用同一标，用户区改为圆形字头。
- 主色统一 `#2563eb`，卡片圆角 12、字号 13.5–15、统计数字等宽。

## 顶栏标签导航（2026-08-22）

- 去掉双左侧栏：应用菜单收到顶部标签（分析视图 / 系统管理点击展开）。
- 工作台会话列表已改回左侧栏（见上节）。

## 去掉 DeepSeek 品牌元素（2026-08-22）

- 侧栏左上角 wordmark 改为「FWGraph」，折叠轨不再用鲸鱼图标。
- 首页去掉鲸鱼、标题「探索未至之境」、预览版徽章和蓝色 glow；空态只留选固件芯片与输入卡。

## 工作台复刻 DeepSeek Harness Web UI（2026-08-22）

- 工作台 tab 按 harness 三栏壳重做：左侧栏（FWGraph 标题 / 新会话 / 固件文件夹树 / 设置）、中间会话（选固件芯片 + 22px 胶囊输入卡）、右侧工具详情。
- Token 作用域在 `.wb-root[data-wb-theme]`，浅色/深色可在设置里切换，不碰 Element Plus 主题。
- 输入卡对齐 InputBar：虚线「选择固件」空态、+ 上传/拖放固件、只读分析/动静结合芯片、蓝色圆形发送/停止。
- 对话流对齐 ChatView：用户气泡、Think 扫光行、工具 IN/OUT 卡、Inspect、Deep diving... shimmer、markdown。
- 固件分析进度用 harness 风格卡片嵌在会话流里；流水线结束后自动起 dsh-web 挖掘会话。
- 工作台页隐藏 FWGraph 顶栏，主栏铺满；`npx vite build` 已过。

## 工作台新会话（2026-08-21）

- 工作台增加「新会话」：清空本地对话/流程图/挖掘时间线，回到上传空态；服务器上的旧任务仍在近期列表，可点回去。

## DeepSeek Harness 对话动画 + 引擎统一（2026-08-21）

- 挖矿会话只走 dsh（DeepSeek Harness fwgraph profile），设置页不再提供 builtin。
- 工作台对话对齐 dsh Web UI：思考行可展开、进行中扫光、`工作中` 文字 shimmer、流式文本光标、工具行配对结果。流式 `thinking`/`text` 按 block 原地更新。

## 挖掘过程回到工作台（2026-08-21）

- 「事件流」只保留流水线日志；挖掘 SSE 从该页移除。
- 工作台在流水线结束后本页启动挖掘：顶部 Grok Build 风格工作条（闪烁绿点、「工作中」、1 个智能体、耗时、当前工具/目标路径闪烁），下方时间线展示思考、工具调用、结果与输出。
- 发送后不再跳转事件流；报告优先读会话 `report.md`（含中文调用链与 PoC）。

## 合并模式 + 事件流（2026-08-21）

- 去掉简易/专业切换；侧栏统一为工作台、事件流、仪表盘、分析视图、协议挖掘、报告。
- 删除「漏洞挖掘」页；工作台负责上传与提示词。
- `EventsView`：任务列表 + 流水线日志轮询。

## 工作台任务持久化 + 进度流程图（2026-08-21）

- 点击发送后顶部立刻出现 8 步流程图（解包→解析→反编译→图谱→攻击路径→路由→输入→攻击面），按 `/jobs/{id}` 状态点亮，当前步脉冲。
- 上传用 XHR 进度百分比，不再卡在「正在上传…」一句；`done`/`decompiled` 不当成整条流水线结束。
- 任务写入 `localStorage`（`fwgraph_home_run`），刷新后恢复流程图并继续轮询。

## 简易模式主页：聊天窗（2026-08-21）

- 去掉 Low/High/XHigh 档位；上传走默认全自动（`POST /firmware?auto=1`）。
- 空态把输入框抬到画面中部偏上；有对话时 dock 底部留白约 10vh，不再贴底。
- 交互：建议词/上传/发送/近期任务 hover 上浮、active 缩放；气泡 TransitionGroup 入场；附件滑入；发送钮就绪态与旋转 loading。

## 攻击路径 AI 分诊展示（2026-08-21）

- `AttackView` 路径表增加 `ai_review.priority` 徽章；详情抽屉展示 reason/CWE，标明「模型分诊，不是漏洞结论」。
- 系统设置增加 `AUTO_ATTACK_AI`。

## 删除 AI 标注 / AI 增强（2026-08-21）

- 删除 `EnrichTrigger.vue`、`CodeDiff.vue`；函数/攻击面源码只保留伪代码与汇编。
- 函数列表去掉 AI 徽章、语义置信度列、「运行 AI 增强」按钮。
- 任务详情去掉「语义标签（M3）」块；简易模式步骤条改为九阶段（去掉「AI 语义标注」）。
- 系统设置去掉「单任务 LLM 函数上限」（`AI_MAX_FUNCS_PER_JOB`）。

位置：VM `~/firmware-graph/fwgraph/webui`（Vue 3 + Vite + Element Plus + marked + @element-plus/icons-vue，构建产物 `webui/dist` 由 orchestrator 托管）。

## 配置层适配（2026-08-19）

- `vite.config.js`：7 条 dev proxy 目标统一收 `VITE_PROXY_TARGET` env（默认 `http://127.0.0.1:8000`），仅影响 dev server，生产仍由 FastAPI 同源托管。

## 安全适配轮（2026-08-15）

- **深链收紧**：`?token=` 只接受 `fws-` 会话 token；主令牌样式（fwgraph- 前缀）显示红色拒绝提示「主令牌不允许通过 URL 传递」；消费后立即 `history.replaceState` 抹地址栏。
- **首登强制改密**：登录/令牌登录/刷新恢复三条路径都查 `must_change_password`，为 true 弹不可关闭改密框；改密统一 ≥10 位前端校验。
- **登录错误**：401 读 detail（「用户名或密码错误」而非误导性「登录状态已失效」）；429 锁定文案（含剩余秒数）透传。
- **任务中心**：专业模式加「属主」列（窄屏隐藏，owner 字段后端已下发）+ 行内/抽屉红色「删除」按钮（popconfirm 确认，`DELETE /jobs/{id}`，409 running 错误透出）。
- **VulnView**：finding/会话 `status` 命中白名单（draft 灰/verified 绿/disputed 琥珀/retracted 红删除线）时渲染徽章；运行态同名字段用白名单规避误渲染。
- 注意：新前端即时生效后，任何依赖 URL 传主令牌的自动化会断（Bearer header 与手动粘贴不受影响）；截图/调试改为先 `curl /auth/login` 拿 fws- token 再 `?token=` 深链，headless Chrome 需加 `--ignore-certificate-errors`（自签 TLS）。

## 产品化控制台（本轮重构）

- **品牌**：固件攻击面分析平台 FWGraph（index.html title 已同步）。
- **登录页**：用户名+密码（`POST /auth/login`，会话 token 存 localStorage+cookie），底部折叠「使用访问令牌登录」（旧 ORCH_TOKEN 兼容入口）；居中发光卡片 + 标语。
- **主壳**：左侧边栏（玻璃拟态、激活项青色左竖条+辉光、图标菜单、窄屏折叠为图标栏）+ sticky 顶栏（品牌、模式切换、用户菜单）+ 内容区（页面切换 0.25s 淡入；注意：**不能用 `<transition mode="out-in">` + keyed 异步组件**——会死锁不加载 chunk，已改默认同时进出模式）。
- **模式切换**：顶栏 el-segmented「简易模式/专业模式」，持久化 `fwgraph_mode`，默认简易；深链 `?mode=simple|pro` 可覆盖。简易模式侧边栏只有 工作台/漏洞挖掘/报告中心（+admin 的系统管理）；专业模式全量。当前页在新模式不可用时自动跳默认页。
- **顶栏用户菜单**：用户名+角色徽章、修改密码 dialog（`/auth/password`）、退出登录（`/auth/logout`）。
- **深链**：`?token=`（消费后抹除）、`?page=`/`?tab=` 等价（dashboard/jobs/functions/attack/inputs/vuln/protofuzz/graph/reports/users/logs/settings）、`?mode=`。
- **权限**：非 admin 不显示系统管理组，越权 page 自动重定向；api.js 401 统一清会话回登录页（`setUnauthorizedHandler`）。

## 页面清单

| Page | 视图 | 模式 | 说明 |
| --- | --- | --- | --- |
| dashboard | DashboardView（新） | 专业 | 7 统计卡（任务/运行中/外部输入/攻击面/AI 发现/会话/报告）+ 运行中任务表（4s 轮询）+ 组件健康 + 快捷操作 |
| jobs | JobsView（改造） | 双 | 见下「任务中心双模式」 |
| functions / attack / inputs / graph | 原四视图 | 专业 | 归入侧边栏「分析视图」分组。函数抽屉/攻击面源码弹窗为 伪代码/汇编 双态；2026-08-21 已删除 AI 增强入口与对比视图 |
| vuln | VulnView | 双 | 流式思考链/双模式按钮，见下 |
| protofuzz | ProtofuzzView（新） | 专业 | 工控协议模糊测试（2026-08-15）：左侧「新建协议测试」卡——**真机连接**（手填 IP/端口/协议模板/传输层/监视器/高级参数）与**工控固件联动**（选任务→`GET /jobs/{id}/identification` 服务清单→点行预填端口/协议，服务→模板启发式映射 modbus/s7/opc/dnp3/mqtt/http）双入口；红色授权确认勾选框（不勾不能启动）；右侧运行列表（运行中有运行 3s 轮询+脉冲点）；详情抽屉（描述+进度条+结果分布标签+故障表 确认/疑似+复现率+恢复判定+最近用例表 2s 轮询+停止/生成报告/marked 预览）。菜单项在「漏洞挖掘」之后，icon Connection |
| reports | ReportsView（新） | 双 | 左列表（kind 徽章 综合/挖掘/**协议测试(pf-)**）+ 右侧 marked 渲染 + 下载/导出（fetch→blob；**导出 DOCX / 导出 PDF** 按钮，loading 防重复，文件名取 Content-Disposition） |
| users | UsersView（新，admin） | 双 | 增删改、角色/禁用/重置密码，后端保护错误透传 |
| logs | LogsView（新） | 专业 | 运行日志查看器（文件/行数选择、3s 自动刷新、自动滚底）+ 审计日志 tab（admin，`GET /audit`） |
| settings | SettingsView（新） | 专业 | 系统信息+组件健康+资源进度条+配置表单（布尔 switch / 引擎 select / 数字输入，非 admin 只读） |

## 任务中心双模式（JobsView）

- **专业模式**：保留原有全部能力（详情抽屉、open-functions emit 等）；新增上传「全自动分析」开关（`POST /firmware?auto=1`）、auto 徽章、「生成/查看综合报告」按钮（先 GET，404 则 POST 生成后弹窗预览）、终态任务「补跑」（`POST /jobs/{id}/auto`）。
- **简易模式（工作台）**：大上传卡（恒 auto=1，文案「上传固件，全自动完成分析并生成报告」）+ 任务卡步骤条（九阶段：解包固件→解析文件系统→反编译→代码图谱→攻击路径分析→路由识别→输入识别→攻击面导出→完成；failed 红态显示 error）+ 完成摘要（N 外部输入 / M 攻击面）+「查看报告」「去漏洞挖掘」按钮；3s 轮询；小白文案不暴露技术细节。

## 输入面（InputsView）

- 数据源：`GET /jobs/{id}/identification`、`GET /jobs/{id}/surfaces[/{sid}]`。
- 左栏：输入列表（ID/服务/协议/地址:端口/输入类型计数/AS 标签）+ 折叠的排除项/不可读项；右栏：基本信息（处理链 libs 标签）、分发链（含 busybox argv[0] 多态分发）、**路由链时间线**（listen/accept/parse/normalize/dispatch/handler 分色）、终点 handler 卡、载体绑定表、授权链 chips（点开看 AS-AUTH 链与旁路注记）。
- 操作：重新识别（POST inputs）、重新导出攻击面（POST surfaces），轮询状态直到完成；空态/404/401 均有提示。

## 漏洞挖掘（VulnView）

- 事件契约兼容双引擎（builtin / dsh）：`thinking`/`text` 带 `{stream, block}` 流式快照，`tool_call`/`tool_result` 按 id 配对，`model_usage` 增量累计。
- 效果：**流式思考链**（同 block 原地生长、青色呼吸辉光 + `▌` 光标、落定后可折叠「思考链 N 字」）、工具卡状态机（运行中琥珀脉冲 spinner → 完成绿条/失败红条/残留灰化）、sticky token 用量条（rAF 缓动计数器）、LIVE 脉冲徽标、新卡 180ms 淡入、rAF 节流自动滚动（受开关控制）。
- **纯静态挖掘 / 动静结合挖掘** 两个模式按钮（选中青色辉光），POST 带 `mode`；会话卡显示模式徽章。
- 会话列表自动打开最新会话；事件筛选 tab：全部/思考/回复/工具/用量/错误。

## 主题：浅蓝专业亮色（2026-08-15 由科技蓝暗色改版）

- 机制：移除暗色（index.html 的 `html.dark` 与 dark css-vars 导入均删除），`:root` 覆写 Element Plus 浅色 `--el-*` 变量；所有视图/组件的硬编码暗色经批量映射脚本一次性替换（`webui` 内不再出现 `#050b18/#0b1830/#9fd8ff` 等暗色值），霓虹 text-shadow/glow 全部移除。
- 色板：页面底 `#eef3fa`（顶部极淡蓝晕，无网格），卡片/浮层纯白 `#ffffff`，代码区 `#f4f8fd`；主色 `#2b6ce5`（实心按钮+悬停 `#2559c7`）；文字 `#1c2b3a/#3d5470/#64748f`；语义色按专业平台校准：success `#16a34a`、warning `#d97706`、danger `#dc2626`、info `#64748f`；边框 `#d7e2ee/#e2eaf3/#eaf0f7`；滚动条 `#c6d5e6`。
- 细节：侧边栏白底+激活项 `#e9f2fd` 底 + `#2b6ce5` 左竖条；顶栏白色毛玻璃；登录页浅蓝渐变底+白色卡片+首字蓝色点缀；hljs 代码高亮 atom-one-dark → atom-one-light；图谱 canvas 浅底 `#f4f8fd`、节点描边改深色、File 节点改灰蓝 `#8fa8c9`（原橙色满屏太刺眼）；CodeDiff 行级增删改浅色绿/红 tint；VulnView 事件流改浅色控制台风格。
- 已 12 页截图目检：login/dashboard/jobs（专业+简易工作台）/functions/attack/inputs/graph/vuln/protofuzz/reports/users/settings。

## 待办/已知项

- 主 chunk 超 500kB 警告（既有，未做代码分割优化）。
- 动画时序只做了结构化验证，建议实机肉眼过一遍。
