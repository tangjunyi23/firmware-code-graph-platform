# 前端进度

> 最后更新：2026-08-19 · 配套：`进度-项目.md`（总览）、`进度-后端.md`。
> 修改前端后必须同步本文档（约定见根目录 AGENTS.md）。

位置：VM `~/firmware-graph/fwgraph/webui`（Vue 3 + Vite + Element Plus + marked + @element-plus/icons-vue，构建产物 `webui/dist` 由 orchestrator 托管）。

## 配置层适配（2026-08-19）

- `vite.config.js`：7 条 dev proxy 目标统一收 `VITE_PROXY_TARGET` env（默认 `http://127.0.0.1:8000`），仅影响 dev server，生产仍由 FastAPI 同源托管。

## 安全适配轮（2026-08-15）

- **深链收紧**：`?token=` 只接受 `fws-` 会话 token；主令牌样式（fwgraph- 前缀）显示红色拒绝提示「主令牌不允许通过 URL 传递」；消费后立即 `history.replaceState` 抹地址栏。
- **首登强制改密**：登录/令牌登录/刷新恢复三条路径都查 `must_change_password`，为 true 弹不可关闭改密框；改密统一 ≥10 位前端校验。
- **登录错误**：401 读 detail（「用户名或密码错误」而非误导性「登录状态已失效」）；429 锁定文案（含剩余秒数）透传。
- **任务中心**：专业模式加「属主」列（窄屏隐藏，owner 字段后端已下发）+ 行内/抽屉红色「删除」按钮（popconfirm 确认，`DELETE /jobs/{id}`，409 running 错误透出）。
- **VulnView**：finding/会话 `status` 命中白名单（draft 灰/verified 绿/disputed 琥珀/retracted 红删除线）时渲染徽章；运行态同名字段用白名单规避误渲染。
- 注意：新前端即时生效后，任何依赖 URL 传主令牌的自动化会断（Bearer header 与手动粘贴不受影响）；截图/调试改为先 `curl /auth/login` 拿 fws- token 再 `?token=` 深链，headless Chrome 需加 `--ignore-certificate-errors`（自签 TLS）。

## 产品化控制台（本轮重构）

- **品牌**：固件攻击面分析平台 FWGraph（index.html title 已同步）。
- **登录页**：用户名+密码（`POST /auth/login`，会话 token 存 localStorage+cookie），底部折叠「使用访问令牌登录」（旧 ORCH_TOKEN 兼容入口）；居中发光卡片 + 标语。
- **主壳**：左侧边栏（玻璃拟态、激活项青色左竖条+辉光、图标菜单、窄屏折叠为图标栏）+ sticky 顶栏（品牌、模式切换、用户菜单）+ 内容区（页面切换 0.25s 淡入；注意：**不能用 `<transition mode="out-in">` + keyed 异步组件**——会死锁不加载 chunk，已改默认同时进出模式）。
- **模式切换**：顶栏 el-segmented「简易模式/专业模式」，持久化 `fwgraph_mode`，默认简易；深链 `?mode=simple|pro` 可覆盖。简易模式侧边栏只有 工作台/漏洞挖掘/报告中心（+admin 的系统管理）；专业模式全量。当前页在新模式不可用时自动跳默认页。
- **顶栏用户菜单**：用户名+角色徽章、修改密码 dialog（`/auth/password`）、退出登录（`/auth/logout`）。
- **深链**：`?token=`（消费后抹除）、`?page=`/`?tab=` 等价（dashboard/jobs/functions/attack/inputs/vuln/protofuzz/graph/reports/users/logs/settings）、`?mode=`。
- **权限**：非 admin 不显示系统管理组，越权 page 自动重定向；api.js 401 统一清会话回登录页（`setUnauthorizedHandler`）。

## 页面清单

| Page | 视图 | 模式 | 说明 |
| --- | --- | --- | --- |
| dashboard | DashboardView（新） | 专业 | 7 统计卡（任务/运行中/外部输入/攻击面/AI 发现/会话/报告）+ 运行中任务表（4s 轮询）+ 组件健康 + 快捷操作 |
| jobs | JobsView（改造） | 双 | 见下「任务中心双模式」 |
| functions / attack / inputs / graph | 原四视图 | 专业 | 归入侧边栏「分析视图」分组。**AI 增强已整合进分析视图，不再单设 tab**（2026-08-15）：独立 EnrichView 删除，新增共享组件 `EnrichTrigger.vue`（「运行 AI 增强」按钮+参数对话框+5s 轮询，完成 emit 刷新）嵌入攻击面与函数两个视图；函数抽屉/攻击面源码弹窗均有 伪代码/AI 增强/汇编 三态切换，AI 态带「重命名仅供参考、证据以原始伪代码为准」提示，函数视图 AI 态用 CodeDiff 并排对比并在列表/抽屉显示 AI 徽章（数据源 `GET /jobs/{id}/aienrich` 目录）；两处源码查看默认回到 hexrays 原始伪代码 |
| vuln | VulnView | 双 | 流式思考链/双模式按钮，见下 |
| protofuzz | ProtofuzzView（新） | 专业 | 工控协议模糊测试（2026-08-15）：左侧「新建协议测试」卡——**真机连接**（手填 IP/端口/协议模板/传输层/监视器/高级参数）与**工控固件联动**（选任务→`GET /jobs/{id}/identification` 服务清单→点行预填端口/协议，服务→模板启发式映射 modbus/s7/opc/dnp3/mqtt/http）双入口；红色授权确认勾选框（不勾不能启动）；右侧运行列表（运行中有运行 3s 轮询+脉冲点）；详情抽屉（描述+进度条+结果分布标签+故障表 确认/疑似+复现率+恢复判定+最近用例表 2s 轮询+停止/生成报告/marked 预览）。菜单项在「漏洞挖掘」之后，icon Connection |
| reports | ReportsView（新） | 双 | 左列表（kind 徽章 综合/挖掘/**协议测试(pf-)**）+ 右侧 marked 渲染 + 下载/导出（fetch→blob；**导出 DOCX / 导出 PDF** 按钮，loading 防重复，文件名取 Content-Disposition） |
| users | UsersView（新，admin） | 双 | 增删改、角色/禁用/重置密码，后端保护错误透传 |
| logs | LogsView（新） | 专业 | 运行日志查看器（文件/行数选择、3s 自动刷新、自动滚底）+ 审计日志 tab（admin，`GET /audit`） |
| settings | SettingsView（新） | 专业 | 系统信息+组件健康+资源进度条+13 键配置表单（布尔 switch / 引擎 select / 数字输入，非 admin 只读） |

## 任务中心双模式（JobsView）

- **专业模式**：保留原有全部能力（详情抽屉、open-functions emit 等）；新增上传「全自动分析」开关（`POST /firmware?auto=1`）、auto 徽章、「生成/查看综合报告」按钮（先 GET，404 则 POST 生成后弹窗预览）、终态任务「补跑」（`POST /jobs/{id}/auto`）。
- **简易模式（工作台）**：大上传卡（恒 auto=1，文案「上传固件，全自动完成分析并生成报告」）+ 任务卡步骤条（十阶段：解包固件→解析文件系统→反编译→AI 语义标注→代码图谱→攻击路径分析→路由识别→输入识别→攻击面导出→完成；failed 红态显示 error）+ 完成摘要（N 外部输入 / M 攻击面）+「查看报告」「去漏洞挖掘」按钮；3s 轮询；小白文案不暴露技术细节。

## 输入面（InputsView）

- 数据源：`GET /jobs/{id}/identification`、`GET /jobs/{id}/surfaces[/{sid}]`。
- 左栏：输入列表（ID/服务/协议/地址:端口/输入类型计数/AS 标签）+ 折叠的排除项/不可读项；右栏：基本信息（处理链 libs 标签）、分发链（含 busybox argv[0] 多态分发）、**路由链时间线**（listen/accept/parse/normalize/dispatch/handler 分色）、终点 handler 卡、载体绑定表、授权链 chips（点开看 AS-AUTH 链与旁路注记）。
- 操作：重新识别（POST inputs）、重新导出攻击面（POST surfaces），轮询状态直到完成；空态/404/401 均有提示。

## 漏洞挖掘（VulnView）

- 事件契约兼容双引擎（builtin / dsh）：`thinking`/`text` 带 `{stream, block}` 流式快照，`tool_call`/`tool_result` 按 id 配对，`model_usage` 增量累计。
- 效果：**流式思考链**（同 block 原地生长、青色呼吸辉光 + `▌` 光标、落定后可折叠「思考链 N 字」）、工具卡状态机（运行中琥珀脉冲 spinner → 完成绿条/失败红条/残留灰化）、sticky token 用量条（rAF 缓动计数器）、LIVE 脉冲徽标、新卡 180ms 淡入、rAF 节流自动滚动（受开关控制）。
- **纯静态挖掘 / 动静结合挖掘** 两个模式按钮（选中青色辉光），POST 带 `mode`；会话卡显示模式徽章。
- 会话列表自动打开最新会话；事件筛选 tab：全部/思考/回复/工具/用量/错误。

## 主题：浅蓝专业亮色（2026-08-15 由科技蓝暗色改版）

- 机制：移除暗色（index.html 的 `html.dark` 与 dark css-vars 导入均删除），`:root` 覆写 Element Plus 浅色 `--el-*` 变量；所有视图/组件的硬编码暗色经批量映射脚本一次性替换（`webui` 内不再出现 `#050b18/#0b1830/#9fd8ff` 等暗色值），霓虹 text-shadow/glow 全部移除。
- 色板：页面底 `#eef3fa`（顶部极淡蓝晕，无网格），卡片/浮层纯白 `#ffffff`，代码区 `#f4f8fd`；主色 `#2b6ce5`（实心按钮+悬停 `#2559c7`）；文字 `#1c2b3a/#3d5470/#64748f`；语义色按专业平台校准：success `#16a34a`、warning `#d97706`、danger `#dc2626`、info `#64748f`；边框 `#d7e2ee/#e2eaf3/#eaf0f7`；滚动条 `#c6d5e6`。
- 细节：侧边栏白底+激活项 `#e9f2fd` 底 + `#2b6ce5` 左竖条；顶栏白色毛玻璃；登录页浅蓝渐变底+白色卡片+首字蓝色点缀；hljs 代码高亮 atom-one-dark → atom-one-light；图谱 canvas 浅底 `#f4f8fd`、节点描边改深色、File 节点改灰蓝 `#8fa8c9`（原橙色满屏太刺眼）；CodeDiff 行级增删改浅色绿/红 tint；VulnView 事件流改浅色控制台风格。
- 已 12 页截图目检：login/dashboard/jobs（专业+简易工作台）/functions/attack/inputs/graph/vuln/protofuzz/reports/users/settings。

## 待办/已知项

- 主 chunk 超 500kB 警告（既有，未做代码分割优化）。
- 动画时序只做了结构化验证，建议实机肉眼过一遍。
