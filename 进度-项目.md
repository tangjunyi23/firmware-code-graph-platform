# 项目当前进度

> 最后更新：2026-08-23 · 本文档与 `进度-前端.md`、`进度-后端.md` 配套。
> 约定：每次完成修改后必须同步更新对应文档（见根目录 AGENTS.md）。

## 本轮：会话与 finding 进仓库（2026-08-23）

Archer C7 挖掘产物入库：`vulnagent/sessions/`（4 个会话，含 events.sse/report）和 `vulnagent/findings/`（18 条 JSON + index.jsonl）。不含 `.env`、固件、解包树。`runner.pid` 仍忽略。

## 本轮：暂停后续跑；httpd 启动崩溃不得跳过（2026-08-23）

输入框停止生成不再结束会话。qemu_exec 启动 SIGSEGV 视为环境，agent 必须接着 trace，不能换题。httpd `/tmp` 配置与 trace 对齐预置。

## 本轮：动态结果留给 agent 下一刀；编排提示不进对话（2026-08-23）

stdin/文件差分已经能打出覆盖。工具现在返回 `hunt_next`（读差分函数 / 换 via=stdin / 复现 crash），后续静态查询也会带上这条，直到入库。编排/能力验收文案对用户隐藏。

## 本轮：agent 测 stdin；docker 重定向修到 rootfs/tmp（2026-08-23）

专用会话 `s-mt5k23tt-aaed`（max_turns=10）由挖掘 agent 自己打工具：`input_path` 已通；`via=stdin` 先因容器 tmpfs 失败，修 docker 重定向后同一 agent 重测 `f55640905b63` → `ok_empty_diff`。不做全系统仿真。

## 本轮：stdin 差分接通，不做全系统仿真（2026-08-23）

网络 qemu-user 空差分是缺处理函数覆盖，不是缺整机仿真。已把 via=stdin / input_path 接到 trace、插件、skill、persona。MIPS 函数级 AFL 仍只保证整二进制。

## 本轮：到轮次上限询问是否继续（2026-08-23）

max_turns 到顶后暂停并询问。用户点「继续挖掘」再放宽 80 轮；点「结束本轮」才 stop 出报告。

## 本轮：给无人值守挖掘加硬边界（2026-08-23）

dsh-web 的 `max_turns=80` 从未计数，watchdog 无限续跑。现按 prompt 计轮次，到顶 409 并 stop；已结束会话不能被 mux/prompt 拉活。payload_hex 奇数位/0x 前缀不再 400。

## 本轮：watchdog 收工空闲静默续跑；会话 trace 192（2026-08-23）

收工写表后 50–80s 空闲是预期循环，不应每分钟打 STUCK。会话预算 96 在日配额仍有余时又卡死，提到 192。

## 本轮：会话 trace 预算提到 96（2026-08-23）

插件内存计数 48 用尽时日配额还有余。提到 96 并重启 dsh 清零。

## 本轮：空差分不再重跑攻击路径（2026-08-23）

每次 `ok_empty_diff` 都重新分析 28MB symbols，编排器 RSS 十几分钟涨到 1.8GB+。有差分才刷新 attack_paths。

## 本轮：多包协议会话 + 插件传输重试（2026-08-23）

无人值守会话反复用单包 payload 打 SSH/SMB，空差分后写「分段 KEX 不可行」收工；编排器内存涨到 60%+ 时插件 `fetch failed`。已提供 `payloads_hex` 同连接多步注入，插件对传输失败重试。测试 518 通过 + 1 跳过。

## 本轮：配额与 qemu-exec 超时误报（2026-08-23）

payload trace 打通后当日 24 条用尽；qemu-exec 把 8s SIGKILL 标成 crash。已把 trace 日配额调到 48，超时/应用退出不再当崩溃。

## 本轮：给挖掘 agent 补协议注入和 PoC 执行（2026-08-23）

Agent 收工理由是「缺 SMB/SSH 字节注入、缺可执行动态验证」。已提供 `payload`/`payload_hex` 的 qemu 差分 trigger，以及 `fw_qemu_exec` 单次跑固件看崩溃。测试 513 通过 + 1 跳过。

## 本轮：identification 补 md5、非 HTTP 动态探针（2026-08-23）

无人值守 `s-mt4rda4n-55d3` 扫完 dnsproxy 后把 dropbear/smbd 当成「无 md5」停手。identification GET 现在补 md5；ftp/ssh/dhcp 不再用 HTTP GET 当 trigger；docker 特权端口改映射到 10000+guest，避免和宿主 sshd 抢 22。收尾空闲缩短到约 25–50s 自动续跑。测试 506 通过 + 1 跳过。

## 本轮：第一轮挖掘结束并续跑（2026-08-23）

会话 `s-mt4k9rm7-b8ed` idle-reap 结束，入库 `F-mt4ljkg4-bede`（bpalogin 栈溢出，call_chain+PoC，static-only）。整二进制 fuzz 实际跑通（50746 execs），agent 当时误判为卡死。已修会话 GET 即时 findings、HTTP Host、fuzz running 提示、空闲 2h。测试 500 通过 + 1 跳过。

## 本轮：fuzz hook 与 HTTP trigger 打通（2026-08-23）

Agent 自己调了 `fw_request_fuzz`，MIPS hook 因未定义 `TARGET_MIPS` 编不过。已修 hook + `AFL_ENTRYPOINT`。docker trace 改为 chroot 进固件 rootfs。httpd 在 qemu-user 里会撞 SysV shm 后 SIGSEGV，HTTP 仍 RST，但覆盖和 fuzz 结果能回到会话。409 不再吞配额。

## 本轮：僵尸 running trace 收尸（2026-08-22）

编排器重启后 minidlna trace 一直 running。GET/LIST 超时收尸为 failed，agent 可重试。测试 491 通过 + 1 跳过。

## 本轮：挖掘会话自己跑通 qemu 覆盖（2026-08-23）

无人值守会话 `s-mt4k9rm7-b8ed` 自己调用 `fw_request_trace`/`fw_get_trace`。Docker 覆盖修好后 trace `ef55266d1f34` 为 `ok_empty_diff_trigger_error`（1183 TB / 30 函数）。HTTP trigger 仍有 RST，但动态结果已能取回并继续挖。测试 490 通过 + 1 跳过。

## 本轮：Archer C7 trace 跑通 docker/RPATH（2026-08-22）

httpd 空 coverage（docker 盖 `/etc` → rc=125）与 minidlna「unsafe library search path」已修：docker 不再盖 `/etc`/`lib`，guest 路径走 rootfs 宿主绑定；编译机 RPATH 忽略。NODYN 时静态结果反复提示立刻 `fw_request_trace`。

## 本轮：NODYN 时反复提示立刻 trace（2026-08-22）

静态工具连打但不跑 `fw_request_trace` 时，第 3 次起结果带 hint；persona 要求拿到首选入口 md5 后立刻动态验证。插件单测覆盖该提示。

## 本轮：动态验证真正跑工具、沙箱写文件（2026-08-22）

误注入的 `record_finding` 方法提示不再显示成用户气泡。模型必须调用 `fw_request_trace`/`fw_get_trace`，不能把空 trace 列表说成已验证。沙箱开放 bash 与写文件（会话 workspace），固件动态仍走 qemu trace。测试 487 通过 + 1 跳过；前端已构建，编排器已重启。

## 本轮：审批按钮可点、全部同意/需要审批（2026-08-22）

审批答复补上宿主要求的 rpcId 和 payload，拒绝/允许生效。输入框左下角可切换「全部同意」和「需要审批」。测试 486 通过 + 1 跳过；前端已构建。

## 本轮：三点菜单归档、Thinking 一行、开始即进会话（2026-08-22）

删不掉的会话：删除前 discard host 并重试清目录。侧栏三点竖向菜单可归档，已归档统一删除。输入框右下角再点即停。Thinking 一行预览可展开，逐字流出。点开始后当前窗口直接进入该对话。测试 485 通过 + 1 跳过；前端已构建，编排器已重启。

## 本轮：工作台可停止和删除对话（2026-08-22）

侧栏会话支持停止进行中的挖掘、删除整条对话。前端已构建，编排器已重启。

## 本轮：工作台进会话、底部输入、逐字流式（2026-08-22）

发送后直接进入新对话；输入框固定底部；滚动节流；回复逐字显示；思考行只显示 Thinking。前端已构建，编排器已重启。

## 本轮：一份漏洞报告 + 工作台思考/工具可见（2026-08-22）

报告列表只留任务漏洞报告。工作台按 harness 的嵌套 chunk 折叠思考和工具调用，历史先回填再接直播。前端已构建，编排器已重启。

## 本轮：Archer C7 真实挖掘产出调用链与 PoC（2026-08-22）

会话 `s-mt4dtbug-e1c6` 对任务 `3a1f3c822c22` 挖出 2 条严重命令注入（CWE-78），均有调用链和 curl PoC。任务报告与会话报告都已写入。修复：dsh host 带上自签 CA（否则 fw_* 全失败）；`record_finding` 真正把 call_chain/poc POST 出去。

## 本轮：挖掘提示不进对话、history 400、Deep diving 卡住（2026-08-22）

用户消息只保留用户指令；挖洞方法走 agent 系统层。`session.history` 由编排器补 sessionId。实测：新建挖掘、编排器重启后再拉历史均为 200，用户气泡只有用户原话，mux 无 bad-request，停止后状态为 done。测试 480 通过 + 1 跳过；前端已构建，编排器已重启。

## 本轮：前置任务与挖掘拆分（2026-08-22）

上传和分析改到「前置任务」tab 并保存进度；工作台只选已完成任务做动静结合挖掘。报告改为调用链 + PoC。编排器已重启，前端已构建。测试 477 通过 + 1 跳过。

## 本轮：Archer C7 全自动链跑通（2026-08-22）

用 `/home/tankuku/gujian` 的 Archer C7 固件走工作台同一条 `auto=1` 链。解包/图谱/攻击路径/路由/攻击面/报告均产出；输入识别去掉 binwalk 抠片；反编译跳过无用的整库 `source.c`，避免 samba 把超时吃光。任务 `3a1f3c822c22` 终态 `routed`。

## 本轮：工作台对话回到左侧（2026-08-22）

工作台「新建对话」和会话列表固定在左侧栏；应用菜单仍在顶栏。前端已重新构建。

## 本轮：产品化字体与界面（2026-08-22）

接入 Inter / JetBrains Mono，登录与顶栏品牌统一，字距和字号按产品控制台收紧。

## 本轮：顶栏标签替代双左侧栏（2026-08-22）

应用导航和工作台任务列表都收到顶部，点击标签展开，不再并排两列侧栏。

## 本轮：去掉工作台 DeepSeek 品牌（2026-08-22）

侧栏改为 FWGraph；首页去掉鲸鱼、「探索未至之境」和预览版徽章。

## 本轮：工作台复刻 DeepSeek Harness 前端（2026-08-22）

工作台 tab 按 deepseek-harness Web UI 三栏壳 + 胶囊输入卡重做；挖矿会话仍走 dsh-web host。固件任务在侧栏按文件夹分组。编排器已重启加载新 dist。

## 本轮：统一 DeepSeek Harness 引擎（2026-08-21）

工作台对话动画对齐 dsh（扫光思考行、shimmer「工作中」、流式光标）。编排器挖矿会话只启动 dsh，不再回退 builtin。

## 本轮：工作台展示挖掘过程（2026-08-21）

挖掘事件从事件流拆出，改在工作台实时显示思考/调用/输出（闪烁工作条）。漏洞报告用中文写清调用链与 PoC。

## 本轮：路由扫描适配 rootfs_elf（2026-08-21）

Archer C7 在 graph 后路由扫描因找不到 `data/idb/*.i64` 整 job 失败。现回退到 ELF，单二进制失败不中断。

## 本轮：反编译强制 rootfs_elf（2026-08-21）

自动反编译对 ELF 使用 `tools/ida-no-mcp/rootfs_elf`（`/home/tankuku/Allzip/rootfs_elf.tar.gz`），`IDA_WORKERS=3`。raw 镜像仍走旧 idat 导出。适配层把 `function_index.jsonl` / `decompile/*.c` 转成现有 `symbols.json` 布局。

## 本轮：聊天主页 + 分析档位（2026-08-21）

工作台与专业分析页合并为一套菜单；新增事件流页替代漏洞挖掘页。加密/空固件不再误报「反编译全部失败」。

## 本轮：借鉴 Firida 证据纪律（2026-08-21）

不拆 MCP、不搬 Synology/NAS 采集。上游挖矿 AI 现在按 **Evidence Address**（job + md5 + 规范地址）拼静态路径、伪代码和动态观察；动态只允许 `observed_in_window` / 单条 trace 的 verified；DT_NEEDED 未解析只作为 identification 质量缺口。

## 本轮：图谱增量摄入 + 调用图并集 + 攻击路径 AI 分诊（2026-08-21）

- CBM 摄入改为增量拷贝（跳过内容相同的 dest、删除过期 `.c`），大树可跳过 git。
- 攻击路径邻接表 = IDA 名字边 ∪ 同二进制 CBM `CALLS` ∪ CFG `call`（地址落入函数区间）；不把 `indirect_jump` 当调用。自动链 graphext 提前到 attack 之前。
- 规则算出 Top 路径后 LLM 只做分诊 overlay（`ai_review`：P0/P1/P2/noise + CWE hint），不改 IDA、不拔高 verified。挖矿 AI 经 `fw_attack_surface brief=true` 消费。

## 本轮：删除 AI 标注 / AI 增强（2026-08-21）

- 拆除 M3 AILIFT（`pipeline/ailift/`、`POST/GET /jobs/{id}/ailift`、自动链中的标注步骤）与 ai_enrich（`pipeline/decompile/ai_enrich.py`、`/jobs/{id}/aienrich*`、`?ai=1` overlay、配额 `aienrich`）。
- 自动链改为解包 → 反编译 → 图谱（graph 内仍接 attack/routes/surfaces/graphext/报告）；历史 `ailifted` 状态补跑直接进 graph。
- 前端去掉 EnrichTrigger、AI 增强源码视图、简易模式「AI 语义标注」步骤、设置页 `AI_MAX_FUNCS_PER_JOB`。
- vulnagent 去掉 `fw_request_reanalysis` 与 `kind=ai` overlay；挖矿仍只用 Hex-Rays 原始伪代码。
- 存量 job 的 `domain`/`libc_equiv`/overlay 文件仍在磁盘，只是不再生产、不再有入口消费 overlay。

## 运行环境

| 项 | 值 |
| --- | --- |
| 部署机 | Ubuntu 26.04 VM `192.168.141.135`（tankuku） |
| 访问入口 | **https**://192.168.141.135:8000 （TLS 自签默认开，`ORCH_SSL=0` 退回明文；证书 CN/SAN 由 `ORCH_CERT_CN`/`ORCH_CERT_SANS` 控制） |
| 登录方式 | 用户名密码（初始 admin / admin123，生产请改密；`FWGRAPH_STRICT_SECRETS=1` 可禁止默认口令引导）；旧 ORCH_TOKEN 仅 Bearer header 可用，**URL query 只收 fws- 会话 token** |
| 部署方式 | ① 宿主机直跑 `fwgraph/scripts/run_orchestrator.sh`（保留不变）；② Docker Compose（`deploy/docker/`，Phase 3 新增）：`docker compose -f deploy/docker/docker-compose.yml --profile build build`（含沙箱镜像）→ `docker compose -f deploy/docker/docker-compose.yml up -d`，数据在 `deploy/docker/data/`，详见 `deploy/docker/README.md` |
| 仓库位置（事实源） | VM `~/firmware-graph`（fwgraph/ + vulnagent/ + tools/） |
| 配置层 | Phase 1 起集中于 `fwgraph/orchestrator/app/config.py`（env 解析 + 路径推导）；所有主机相关路径均走 env，模板见 `fwgraph/.env.example`、`vulnagent/.env.example` |
| LLM 网关 | opencode go `https://opencode.ai/zen/go/v1`，模型 `deepseek-v4-flash`（chat/completions 端点，无需路由） |
| 重依赖 | IDA Pro 9.1、EMBA 2.0.3a、codebase-memory-mcp、qemu-user-binfmt、Node v22、pnpm 11.7、deepseek-harness、AFL++ afl-qemu-trace×4 架构、LibreOffice（报告 PDF 导出） |

## 模块状态总览

| 模块 | 状态 | 说明 |
| --- | --- | --- |
| M1 固件解包（EMBA） | ✅ 可用 | 修复两处：补 `extract-only.emba` profile（黑名单制，71 个非 P 模块全禁）；`_chown_output` 改走 `EMBA_SUDO_PASSWORD` |
| M2 定向反编译（IDA） | ✅ 可用 | MX12 实测 169/169 ARM ELF 全量导出 |
| M3 AILIFT AI 标注 | ❌ 已删除 | 2026-08-21 拆除；建图不依赖标签 |
| M4 CBM 代码图谱 | ✅ 可用 | |
| M6 攻击面分析 | ✅ 可用 | MX12：1466 source / 4898 sink / 50 条评分路径 |
| M6a 外部输入识别 | ✅ 可用 | `pipeline/inputs/` → `identification.json`；双验收门（公网可达、零遗漏记账） |
| M6b 逐输入攻击面 | ✅ 可用 | `pipeline/surfaces/` → `information/AS-*.json` + `AS-AUTH-*.json`；路由链/终点 handler/载体绑定/授权链独立 |
| M7 qemu-user 差分覆盖 | ✅ 可用（近期未用） | |
| 动态分析双引擎 | ✅ 可用（已并入挖矿） | x86→frida / arm,mips→qemu+AFL++ 函数级 fuzz；无独立 UI，作为动静结合挖矿模式的 agent 工具 |
| **沙箱执行层（Phase 2）** | ✅ 可用（沙箱镜像已随 Phase 3 实建 991MB） | `pipeline/sandbox/` + `deploy/docker/Dockerfile.sandbox`；fuzz/frida/trace 三处不可信固件执行 docker 化（`SANDBOX_BACKEND` 默认 docker，组件级可覆盖），docker/镜像不可用时回退原路径并在 meta 写中文警告 |
| **容器化部署（Phase 3）** | ✅ 已完成 | `deploy/docker/`：编排器镜像（node:22 构建 webui → python:3.12 运行时，实建 2.28GB）+ 沙箱镜像 + compose（8000:8000，sandbox 为 build-only profile）；TLS 自签证书持久化 /data/certs；编排器容器经 docker.sock 起兄弟沙箱容器、挂载源经 `SANDBOX_HOST_PREFIX` 改写端到端通过；冒烟 `/` 200、`/login` 200、`/jobs` 无 token 401 |
| M4b CFG/AST | ✅ 可用 | 函数级 CFG（.asm 基本块+边）+ 伪 C AST（tree-sitter）；MX12：67045 CFG / 55474 AST；query op cfg/ast |
| 挖矿双模式 | ✅ 可用 | 纯静态（禁动态工具）/ 动静结合（fuzz/frida/trace 开放），前置门禁=图谱+攻击面，双模式均出中文报告 |
| ai_enrich | ❌ 已删除 | 2026-08-21 拆除 overlay 与前端入口 |
| M8 vulnagent | ✅ 可用 | 默认 dsh 引擎（DeepSeek Harness 移植底座），内置引擎 fallback；六阶段 hunt 打法（xiaomin skill） |
| **产品化后端** | ✅ 本轮新增 | 用户/会话/审计（PBKDF2+7 天滑动会话）、系统信息与配置白名单、日志查看+审计、仪表盘聚合、报告中心（综合报告生成器+会话报告聚合）、一键自动链（auto=1 上传→解包→反编译→图谱→攻击面→报告） |
| **产品化前端** | ✅ 本轮重构 | 登录页+侧边栏控制台；仪表盘/报告中心/用户管理/日志审计/系统设置新页面；简易/专业双模式切换；简易模式上传固件→九阶段步骤条→全自动报告 |
| Web 前端主题 | ✅ 可用 | 科技蓝暗色；输入面页；漏洞挖掘页流式思考链/动效 |

## 关键验证记录

1. **DSM DS120j（gold 对照）**：inputs 阶段扫出 40 条公网输入、18 条排除、15 个加密 SPK（`5badbeef`，无私钥不可解）如实记入 `metadata.unreadable`；双门禁 0 错误。
2. **US_MX12V2.tar（测试集，job `90e7e89dc221`）**：全管线贯通——EMBA 提取 → 169 ARM 二进制 → IDA 反编译 → AILIFT → CBM 图谱 → 攻击面 50 路径 → 路由扫描 → inputs（5 个公网输入：vsftpd:21/telnetd:23/httpd:80/syslogd:514/miniupnpd:1900，busybox argv[0] 多态分发已识别）→ surfaces（5 AS + 3 AS-AUTH，static_only 0、门错误 0）→ 综合报告自动生成。
3. **漏洞挖掘 e2e**：dsh 引擎会话经 UI 同路径 API 启动，工具 0 报错；agent 对 AS-002(telnetd) 的分析反向纠正了静态角色分类噪音（识别出误标函数实为 wget applet 的 TLS 客户端代码）。
4. **测试基线**：319 项，318 通过 + 1 个已知测试间污染项（`test_large_tree_selects_fast_mode`，单独运行通过）。
5. **双模式实测**：静态会话零动态工具调用、产出 finding F-mssye7zm-1ef8（mldproxy argv 命令注入，static-only 如实标注）+ 标准报告。
6. **动态分析**：frida 本机 hook 实测命中；AFL++ 函数级 fuzz 合成 ARM 目标 30s/10 万 execs 实测通过；固件 daemon（goahead）因 init 需 chroot/网络环境而如实失败并给中文提示——函数级 fuzz 当前要求函数启动可达。
7. **产品化冒烟（2026-08-15）**：admin 登录→会话 token 全端点可用；普通用户越权 PUT /system/config、GET /users 被 403；`/audit` 记录完整；综合报告六章节真实数据；前端 17 张全页面截图目检通过（含简易模式工作台、窄屏折叠、非 admin 重定向）。
8. **图谱修复 + 报告导出（2026-08-15）**：graph layout 502 根因是 CBM UI 守护进程未运行，`run_orchestrator.sh` 已加幂等自启；layout 实测 200 返回 166,885 节点，前端图谱页渲染正常。综合报告扩为九章节；DOCX/PDF 导出实测中文排版正常（pdf 663KB/27 页）。
9. **全量安全/置信度修复（2026-08-15，审查驱动）**：ORCH_TOKEN 轮换（旧值泄露于访问日志 56 次，已作废）；`?token=` 只收 fws- 会话 token + access log 脱敏；TLS 自签默认开（SAN 含 127.0.0.1/localhost，vulnagent 经 NODE_EXTRA_CA_CERTS 对接实测 FETCH_OK）；owner 数据隔离（job/session/report，陌生人 404）；登录防爆破（5 次锁 10 分钟+时序抹平+≥10 位弱口令黑名单+首登强制改密）；/logs 收 admin；配额系统（trace/fuzz/frida/aienrich 按 job 日限 429）；findings 服务端校验 API（md5/addr/CWE/混类/置信度锚点封顶，实测 0.9→0.7 封顶+confidence_reported 留痕）+ 状态生命周期（draft/verified/disputed/retracted，retracted 不进报告）；dsh 摘除 bash/web/写文件等通用工具（profile 实 boot 断言 24 工具白名单），新增监禁版 fw_browse_firmware；六阶段打法 Phase3 剔除落库/Phase4 末行严格判定/Phase5 FINAL.md 补齐/记忆 ±0.05 写回。
10. **管线置信度修复实测（MX12）**：外部输入 5→39（证据四级分级，syslogd 幻影消除并留痕，dnrd UDP/53 observed 级找回）；AS 克隆组 39→0；httpd 终点 handler 从 PLT 桩修正为 libgo.so TendaAte 并恢复 8 条真实 GoAhead 路由；载体绑定真/占位分离（4 真 35 占位单列）；授权链 24 条结构证据链（含 anonymous_enable=YES 姿态）；门禁真失败化（gate2 独立复算 39+16=55 对账）。CFG 假 ret 边 29.7%→0.38%，新增 16776 条 indirect_jump 建模，补漏约 1.8 万条件返回边；AST ERROR 率 10.1%→0.09%。
11. **测试基线**：478 项，477 通过 + 1 已知污染项（`test_large_tree_selects_fast_mode` 单跑通过）。`test_findings_api.py::test_report_failure_goes_to_events_and_log` 全量下偶发污染性失败（单跑/整文件均过），待隔离。
12. **AI 增强闭环（2026-08-15）**：「增强对比 0 内容」根因=aienrich 是计费按需阶段且无前端入口 → 页面加「运行 AI 增强」按钮（attack_only 默认）；实跑暴露第二根因——推理模型 reasoning 吃光 4096 token 预算（17/18 empty content）→ 默认提至 16384。MX12 miniupnpd（攻击路径 17 处引用）attack_only 实跑：18 目标首轮 11 成功，32k 重试回收剩余 6 个（resume 机制自动跳过已成功项），最终 **18/18 成功、0 错误**；页面实测渲染 AI 摘要/置信度/重命名 diff 对比。
13. **AI 增强归位（2026-08-15）**：独立「增强对比」tab 移除，整合进攻击面与函数（调用点/源码）分析视图——共享组件 EnrichTrigger 内嵌两处工具栏，函数抽屉/攻击面源码弹窗三态切换（伪代码默认/AI 增强/汇编），AI 态带「重命名仅供参考」提示，函数视图 AI 态 CodeDiff 并排对比+列表 AI 徽章。挖矿侧证据口径收紧：`fw_get_function_source` 双引擎（builtin tools.js / dsh plugin）默认只吃 Hex-Rays 原始伪代码，AI overlay 须显式索取且响应附 caveat「不可引用其臆造符号」，定位降级为攻击面/调用链分析辅助。顺带修复一条过时测试（fw_browse_firmware 无扩展名文本已放行，断言改为 NUL 二进制拒读），vulnagent 27/27、orchestrator 470+1 已知项全绿。
14. **工控协议模糊测试 M-ICS-1/2 落地（2026-08-15）**：参考博智工控漏洞挖掘平台 V3.2 产品文档，软件化其协议 fuzz 主线（硬件部分明确不做：总线业务卡/DI/AI/继电器电源托管/TFT 屏）。新增 `pipeline/protofuzz/`：协议模板库 6 种（Modbus TCP/S7/OPC UA/DNP3/MQTT/HTTP，字段级 boundary/bitflip/fill/overflow/random 确定性变异，种子可复现）；纯软件监视器（TCP 探活/ICMP Ping/协议探测）；故障模型=传输层异常→监视器确认（confirmed/suspected）→复播 3 次统计复现率+服务恢复判定（博智循环分段的软件近似）；用例/故障 jsonl 落盘 `data/protofuzz/<run_id>/`。API `/protofuzz[/protocols|/{run_id}/stop|/report]`：acknowledge 强制授权确认、公网目标默认禁止（PROTOFUZZ_ALLOW_PUBLIC=1 解锁）、配额 PROTOFUZZ_DAILY=8（job 或用户维度）、全程审计、owner 隔离。报告注册进报告中心（pf- 前缀，kind=协议测试，支持 DOCX/PDF 导出）。前端新增「协议挖掘」页（专业模式）：**真机连接**（手填 IP/端口/协议）与**工控固件联动**（选任务→读 identification.json 服务清单→点服务行预填端口/协议，IP 手填）双入口；运行列表 3s 轮询、详情抽屉（进度条/结果分布/故障表/最近用例）、停止/生成报告/报告预览。实测：本地假 Modbus 服务 25 用例全响应、故障注入场景 9 故障正确分级、pf 报告 md/docx 导出正常。测试 477 通过 + 1 已知污染项。
15. **前端整体改版为浅蓝专业亮色（2026-08-15）**：去除科技蓝暗色（html.dark/dark css-vars/霓虹辉光/网格背景全部移除），`:root` 覆写 Element Plus 浅色变量，视图/组件硬编码暗色批量映射替换；白卡片+浅蓝主色 `#2b6ce5`+校准语义色；hljs 换 atom-one-light；图谱 canvas 浅底灰蓝节点；事件流浅色控制台化。12 页截图目检通过（含登录/简易工作台/协议挖掘）。
16. **挖矿 AI 上下文预算优化（2026-08-15，P1/P2/P3）**：实测论证图谱价值——裸函数清单 28.7MB vs attack_surface top20 仅 12KB（3 个数量级）。落地三刀：P2 函数 brief 端点（约 1KB 分诊卡：攻击面元数据+伪代码头+危险调用行号+callees，双引擎工具加 brief 模式并改描述为 brief-first）；P3 `attack_surface brief=true`（top20 实测 12KB→7.1KB）+ cfg/ast `max_nodes/max_depth` 截断；P1 builtin 引擎会话历史压缩（`compactHistory`：超 1200 字符的旧 tool_result 每轮调用前替换为首尾摘录占位，最近 6 条与任务不动，幂等，事件流可见；dsh 引擎自带 compaction 已核实无需处理）。顺带修复 test_aienrich/test_trace fixture 未隔离 FWGRAPH_DATA 导致真实配额被测试耗尽的隐患。测试：orchestrator 481 通过+1 已知项（新增 4 项），vulnagent 29/29。

## 已知限制（诚实记录）

- surfaces 角色分类仍是启发式；busybox 无法锚定 applet 时不再漫游整二进制，降级为 `applet_boundary: unresolved` + 低置信标注（诚实但信息量少）。34 条 candidate 级输入是诚实噪声（无端口证据）。
- CFG 跳表 case 边无法恢复（.asm 导出不含跳表数据），如实标 indirect_jump 不伪造。
- DSM 加密 SPK 无法静态解包（无厂商密钥），只能记账不能扫描。
- trace（M7）：VM 内核 `apparmor_restrict_unprivileged_userns=1` 禁了 user namespace，沙箱走 `TRACE_ALLOW_ROOT_CHROOT=1` 显式授权的 root chroot（风险已在 .env 注释与 meta 警告里写明）；userns 路径已实现，换内核/策略即自动生效。
- dsh 为 0.1.0-rc 预览版；profile 中 `bash-sandbox` 行不能禁（提供 shell 服务，禁了整树 boot 挂起）——通用工具靠 tools.guard 按名拦截。
- `test_findings_api.py` 有 1 个全量下偶发污染性失败（单跑必过），隔离待做。
- 容器化部署（Phase 3）：部署机 docker 29 无 buildx 时需先装用户级 buildx 插件（compose `--build-context` 依赖，已装 `~/.docker/cli-plugins/docker-buildx` v0.29.1）；buildkit 对 `COPY --from=<命名上下文>` 的缓存键不含上下文内容，切换 ida/cbmbin 可选上下文需 `--no-cache` 重建。
- 容器化部署（Phase 3）：cbm 二进制 glibc 不兼容（宿主编译需 GLIBC_2.43 > 镜像 trixie 的 2.41），容器内 cbm-ui 起不来，entrypoint 告警跳过、编排器照常；frida 本地沙箱的 `-v` 不经 `SANDBOX_HOST_PREFIX` 改写，容器化部署下本地 frida 插桩暂不可用（remote 模式无碍）。
- 容器化部署（Phase 3）：沙箱容器 uid 1000 需与宿主服务用户 uid 对齐（挂载文件权限）；容器内 `.env` 的宿主侧绝对路径（如 `EMBA_DIR`）无效，需按容器路径配置；fuzz hook 等宿主编译产物挂入沙箱镜像有 glibc 兼容性约束（宿主编译 → ubuntu:24.04 容器）。

## 下一步候选

- M-ICS-3：identification.json 服务清单 → 一键生成协议 fuzz 预案；动静结合模式给挖矿 AI 加 `fw_request_protofuzz` 工具（配额管控）
- M-ICS-4（可选）：未知协议逆向（pcap 聚类/字段推断 → 生成模板）
- 跑一次完整六阶段 `hunt`（challenge 循环已真实化）产出新一轮 findings
- MX12 httpd 深挖（8 条恢复的 GoAhead 路由已就位）
- candidate 级输入的观测手段（qemu 起服务实探测端口，把 candidate 升 observed）
- 把 VM 仓库改动整理提交推送到 GitHub（需用户确认）
